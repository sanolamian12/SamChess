# HANDOFF — 세션 인수인계

> **새 대화는 이 문서와 [`docs/작업계획.md`](docs/작업계획.md) 둘부터 읽는다.**
> 이 문서는 **지금까지 무엇이 어떻게 굳었는가**(정본 상태), 작업 계획은 **왜 그렇게
> 정했는가**(결정 이력)다. **진행 상황은 아래 §2 표와 §7 로드맵이 정한다** —
> 작업 계획 §2는 **끝난 트랙**(PPT 37~45)의 표라 다음을 고르지 못한다.
> 이어서 [`Design/GDD.md`](Design/GDD.md)(구현 기준 문서)와 [`README.md`](README.md)(폴더 구조·명령)를 본다.
> 지난 세션에 **무엇을 왜 그렇게 정했는지**와 **어디서 넘어졌는지**는 [`history/`](history/)에 있다.
>
> 최종 갱신 **2026-08-19** (PPT 37~45 트랙 끝 · Colyseus 트랙 **H1** 완료) · 프로젝트 루트 `C:\Users\user\Documents\SamChess`
>
> **Colyseus 트랙이 셋으로 갈렸다 — H1(전송 층) · H2(서버와 방) · H3(계정·로그인).**
> **H1이 끝났다** — 재생기가 더 이상 상태를 만들지 않고 `BattleTransport`가 「도착」시킨다.
> 다음은 **H2**이고 입구는 둘이다: `client/src/meta/matchmaking.ts`의 `searchOnline()`과
> `client/src/battle/transport.ts`의 `OnlineTransport` 자리.

## 새 대화를 여는 프롬프트

아래를 그대로 붙여 넣으면 된다. 읽는 순서와 하지 말아야 할 것이 다 들어 있다.

```
SamChess 작업을 이어간다. 먼저 이 순서로 읽고 시작해줘.

1. HANDOFF.md 전체 — 현재 상태의 정본
2. docs/작업계획.md — 결정 로그. §5가 「왜 그렇게 정했나」의 목록이고, §5-53~63이
   Colyseus 트랙의 것이다. §2 표(PPT 37~45)는 끝났고 ⬜는 별도 트랙 G1·G2·G3뿐이다
3. Design/GDD.md — 구현 기준. 특히 §3.3(시간 모델)·§3.9(전투 흐름)·§6.1(재화)·
   §7.1(전투력)·§8.6~§8.7(부대·출전/매칭)·**§10(데이터 모델 · 전선 계약)**·§12(결정 이력)

읽고 나서 지금 상태를 한 번 확인해줘:
  npm run extract && npm run typecheck && npm test

**진행 상태는 HANDOFF §2 표와 §7 로드맵이 정한다** — 작업계획 §2로는 다음이 안 정해진다.
할 일은 아래 [다음 세션] 블록에 있다.
설계안을 먼저 보여주고, 내가 확인하면 구현으로 넘어가자.

세션을 마치면 이 넷을 한다.
  · 진행 상태를 갱신 — 트랙 안이면 작업계획 §2, 밖이면 HANDOFF §7의 로드맵
  · 새로 확정된 것을 작업계획 §5 결정 로그와 Design/GDD.md에 옮겨 적기
  · 굳은 계약·밟은 지뢰를 HANDOFF.md(§6·§7)와 CLAUDE.md에 반영
  · history/{날짜}_{제목}.md 에 「왜 그렇게 정했고 어디서 넘어졌는가」를 남기기
```

**세션에 따라 보태는 줄.** 앞의 프롬프트 뒤에 이어 붙인다.

```
[다음 세션 — Colyseus H2: 대전 서버와 방]

H1(전송 층)이 끝났다. 재생기는 더 이상 상태를 만들지 않고, 판정 주체(BattleTransport)가
「이렇게 됐다」를 보내면 큐에 쌓았다가 연출 속도로 소화한다. 동작은 안 바뀌었고
회귀 17건이 전선 계약을 고정하고 있다.

  · 채울 자리는 둘이고 계약은 이미 서 있다
      client/src/battle/transport.ts   OnlineTransport 자리  ← LocalTransport와 같은 계약
      client/src/meta/matchmaking.ts   searchOnline()        ← 지금은 30초 뒤 null
    그리고 새로 짓는 것 하나
      packages/server/  room-logic.ts(순수·시각을 인자로) + BattleRoom + queue + clock
  · 전선에 실리는 것은 「로그를 뺀 스냅샷 + 이벤트 + 남은 ms」다 (GDD §10). 로그를 실으면
    전선에서 O(턴²)다 — 실측이 transport.ts 머리에 있다
  · 회귀의 ScriptedTransport가 이미 「묻지도 않고 미는 서버」를 흉내 내고 있다.
    OnlineTransport가 그 모양이 되고, 그때 그 이중 구현이 사라진다
  · H2에서 처음 도는 것 — waiting 단계 · 제어 20초(controlStartedAtMs 주입) ·
    북군(P2) 배치 프리셋 · 「다시 찾기」가 진짜 다른 사람을 물어 오는 것
  · 이미 정해 둔 것(작업계획 §5-58~64) — 로컬 개발만 · 로그인은 뒤 · 매칭 구간은
    안 넓힌다 · 거절은 두 사람의 사건 · 참가비는 「방이 열린 순간」에 확정
  · 이탈은 GDD §3.9의 표가 정한다 — 한쪽만 사라졌으면 배치 중이면 사라진 쪽만
    참가비를 잃고(남은 쪽은 환불·전적 없음), 정찰 이후면 항복과 같다.
    양쪽이 다 사라졌으면 단계와 무관하게 양쪽 다 환불한다.
    갈림길이 state.ready라 엔진에 이미 있는 값이고, 항복 갈래는 서버가 사라진 쪽 대신
    surrender를 적용하면 끝이라 룰 엔진도 보상 층도 안 건드린다
  · 사라진 쪽에게 환불하지 않는 이유는 「끊는 것이 거절보다 싸지면 안 되기」 때문이다
    (§5-65). 돌려주면 군량 0으로 상대를 물릴 수 있어 거절 −1이 무력해진다 —
    이 문장을 잃으면 언젠가 「사라진 쪽도 돌려주는 게 인정 아닌가」로 되돌려진다
  · 「끊긴 것」과 「가만히 있는 것」은 다르고 둘 다 정해졌다 (§5-67·68, GDD §3.3·§3.9) —
    한쪽만 손을 놓으면 상대가 [차례 넘기기]를 3번 눌러 이기고(누적·화면에 (2/3)),
    양쪽 다 손을 놓으면 유휴 상한 2분에 방을 접고 양쪽 환불한다. 겹치지 않는다
  · §5-67은 이 트랙에서 처음으로 룰 엔진을 건드린다 — BattleState에 진영별 넘기기
    횟수가 늘고 validate/승패 판정이 그걸 본다 (저장 형식은 그대로, 런타임 상태다).
    H1에서 안 만든 이유는 발동 조건이 controlStartedAtMs를 요구해서다 — 서버와
    같은 세션에서 만들어야 「도달할 수 없는 갈래」가 안 생긴다
  · 재접속 유예 = 1분이고, 끊긴 사람에게는 자동 [준비완료]를 걸지 않는다 (§5-69) —
    걸면 배치 중 이탈이 언제나 항복으로 판정되어 §5-64 표의 첫 줄이 영원히 안 돈다

먼저 읽을 것
  · history/2026-08-19_전송층_분리.md — 전선 계약이 왜 이 모양인가, 어디서 넘어졌는가
  · packages/client/src/battle/transport.ts — 채울 계약 그 자체
  · history/2026-08-18_출전매칭_문통합.md — 매칭 화면이 무엇을 전제하는가

깨지기 쉬운 자리
  · 상대를 「값으로」 받는 계약이 서버에서도 서는가 (BattleScreen은 상대를 안 만든다)
  · 시간 셋이 맞물려 있다 — 재접속 유예 60초 · 매칭 대기 상한 60초 · 유휴 상한 120초.
    유예가 3번 규칙의 하한(20×3)과 같고 유휴 상한보다 짧아야 성립한다
  · 같은 브라우저의 두 탭은 localStorage가 같아 계정이 하나다 — 개발용 접미사가 필요하다
  · ?match=online 흉내가 진짜와 갈릴 수 있다 — 지우고 스모크가 진짜 서버를 띄우는 쪽을 권한다
  · rngCursor는 서버가 유일한 소비자라 갈릴 수 없다 — 대신 단조 증가와 최종 cursor를 남긴다
  · 참가비·거절 군량을 서버가 다시 검증하는 것은 H3다 (계정이 서버로 가야 뜻이 있다).
    H2에서는 「아직 검증 안 한다」를 아는 채로 두고 적어 둔다
  · 타입 스트리핑은 데코레이터·파라미터 프로퍼티·enum을 못 받는다 —
    @colyseus/schema를 안 쓰기로 한 것이 그래서 취향이 아니라 제약이다
```

## 두 문서의 역할이 갈린다 ★

| | 무엇을 적나 | 언제 고치나 |
|---|---|---|
| **`HANDOFF.md`** (이 문서) | **굳은 것** — 확정 규칙 · 구조 · 계약 · 밟은 지뢰 | 계약이나 구조가 바뀌었을 때 |
| **`docs/작업계획.md`** | **앞으로 할 것** — 세션 순서 · 진행 상태 · 열린 질문 · 결정 이력 | **세션을 마칠 때마다** |
| `Design/GDD.md` | **사양의 정본** — 수치 · 규칙 | 기획이 바뀌었을 때 |

**작업 계획이 사양의 출처가 되면 안 된다.** 거기서 확정된 것은 GDD로, 굳은 계약은 여기로 옮긴다.

---

## 1. 무엇을 만들고 있나

**삼국 약식 체스** — 체스 6기물의 이동 규칙을 빌린 삼국지 장수 260명의 **ATB 전술 대전 게임**.
PC(웹) · Android · iOS 단일 코드베이스. 수집/육성 메타(도시·상점·가챠·랭킹) 포함.

기획 원본은 사용자가 만든 `docs/삼국 약식 체스.pptx`(19슬라이드)와 `docs/삼국지 약식체스.xlsx`(4시트).
이 둘을 통합·정규화한 것이 `Design/GDD.md`이며, **구현은 GDD를 기준으로 한다.**

### 핵심 특징 (놓치기 쉬운 것)

- **"턴제"라고 부르지만 실제로는 CTB(Count Time Battle) 스케줄러다.** 절대시간이 흐르고,
  유닛별 `waiting time`이 0이 되면 제어권을 얻으며, 제어 중에는 시간이 멈춘다.
  → 넷코드는 "수를 주고받는 턴제"가 아니라 **서버가 시계를 소유하는 권위 시뮬레이션**이어야 한다.
- 기물은 **이동 범위와 공격 범위가 분리**되어 있다. 이게 이 게임의 전술적 정체성이다.
- 크리티컬·환술 판정이 전부 확률이고 현금 가챠와 랭킹이 있다 → **모든 판정은 서버에서, 시드 기반 PRNG로.**

---

## 2. 지금까지 한 일

| 단계 | 상태 |
|---|---|
| 기획 문서 2종 검토 및 정합성 교차검증 | ✅ |
| 기술 스택 결정 | ✅ |
| `Design/GDD.md` 작성 (13장 통합 기획서) | ✅ |
| 룰 엔진 타입 계약 `packages/rules/src/types.ts` | ✅ |
| 미결 항목 12건 중 7건 확정 | ✅ |
| 모노레포 스캐폴딩 + 엑셀→JSON 추출 파이프라인 | ✅ |
| 데이터 정합성 회귀 테스트 12건 | ✅ |
| CTB 스케줄러 (`advanceTime`) + 시드 PRNG | ✅ |
| 행동 판정 (배치·이동·공격·명상·턴 종료·항복) | ✅ |
| 책략 18종 Effect DSL + 실행기 | ✅ |
| 고유기술 40종 (A/B/E 10 + S 30) | ✅ |
| 전투 회귀 테스트 101건 (총 113건) | ✅ 전부 통과 |
| GitHub 공개 저장소 | ✅ [sanolamian12/SamChess](https://github.com/sanolamian12/SamChess) |
| 기준 AI + 밸런스 통계 하네스 | ✅ 5000판 실측 (§7) |
| 무승부 규칙 (`time 6000` 판정승) | ✅ |
| Phaser 전투 씬 1차 (보드·타일·입력·행동 바·AI 대전) | ✅ |
| 화면 연동 스모크 테스트 (`smoke:ui`) | ✅ |
| 전투 씬 2차 — MP·WT 바 · 상단 HUD · 제어 모달 3상태 | ✅ |
| 전투 씬 2차 — 턴 3단계 · 책략/고유기술 시전 UI · 공격 범위 | ✅ |
| 전투 씬 2차 — 타일 배지 4종 · 장수 정보 팝업 | ✅ |
| 고유기술 연출 이미지 40장 대조 (`check_skill_art`) | ✅ 어긋남 0건 |
| **전투 씬 3차 — 디자인 가이드 반영 (PPT 20~25)** | ✅ 아래 §7 |
| 1:1 삭제 · 레벨업 100% · AI 보상 확정 | ✅ 미결 항목 **전부 해소** |
| 메타 규칙 패키지 `@samchess/meta` + 회귀 17건 | ✅ |
| 오프라인 사이클 — 계정 · 편성 · 전투 진입 · 결과 · 레벨업 | ✅ |
| 배치·정찰 단계 화면 | ✅ **오프라인 사이클 완결** |
| 장수 액션 시트 260명 (5자세 · `npm run actions`) | ✅ 2026-08-12 |
| 전투 연출 — 이동·공격·책략·퇴각 자세 + 시간표 테스트 15건 | ✅ 2026-08-12 |
| **전투 씬 4차 — 카드 스트립 · 플로팅 커맨드 패널 · 카메라 (PPT 27~29)** | ✅ 2026-08-12 (§7) |
| **시각 효과 30종 — 지속형 링 23 · 일회성 애니 7** | ✅ 2026-08-13 (§7) |
| **지형 그림 3종 — 화계·수계·성지를 칸에** | ✅ 2026-08-14 (§5 · §7) |
| **전투 화면 리디자인 — 판 지도 · 카드 벽보 액자** | ✅ 2026-08-14 (§5 · §7) |
| **배경음악 5곡 — 화면마다 한 곡** | ✅ 2026-08-14 (§5 · §7) |
| **간판·도시·자리 화면 (PPT 33~36) · 환경설정 · 다국어 틀** | ✅ 2026-08-15 (§5 · §7) |
| **배경의 미세한 움직임 — 2.6초마다 한 자세** | ✅ 2026-08-16 (§5) |
| **PPT 37~45쪽 작업 설계 · 진행 통제 문서 분리** | ✅ 2026-08-17 → [`docs/작업계획.md`](docs/작업계획.md) |
| **장수 일람 · 상세 · 고유기술 팝업 (PPT 37·38)** | ✅ 2026-08-17 (§5 · §7) — 트랙의 **A** |
| **부대 전투력 산출식 (PPT 44) — 모드당 2만 판 회귀** | ✅ 2026-08-17 (§7) — 트랙의 **D** |
| **레벨/스킬 관리 · 재설계 (PPT 39) — 저장 형식 v2 · 되접기** | ✅ 2026-08-17 (§5 · §7) — 트랙의 **B** |
| **전적 · 보상 개편 (PPT 40 + 45 보상표) — 저장 형식 v3 · 대전 이력** | ✅ 2026-08-18 (§5 · §7) — 트랙의 **C1** |
| **도시 관리 · 군량 시간 충전 (PPT 41) — 저장 형식은 필드 하나** | ✅ 2026-08-18 (§5 · §7) — 트랙의 **C2** |
| **부대 편성 · 배치 프리셋 (PPT 42·43) — 저장 형식은 필드 둘** | ✅ 2026-08-18 (§5 · §7) — 트랙의 **E** |
| **출전 · 매칭 + 문 통합 (PPT 45) — 저장 형식은 그대로** | ✅ 2026-08-18 (§5 · §7) — 트랙의 **F** |
| PPT 37~45 구현 (궁궐·병영 아홉 화면) | ✅ **끝** — A·D·B·C1·C2·E·F. 이력은 작업 계획 §2 |
| **전송 층 분리 (Colyseus H1) — 서버 코드 0줄** | ✅ 2026-08-19 (§6 · §7) — `BattleTransport` · 회귀 17건 |
| Colyseus 온라인 대전 — **H2** 서버와 방 | ⬜ **지금 차례** — `searchOnline()`과 `OnlineTransport` 둘이 입구다 |
| 계정·로그인 + 계정 권위 — **H3** | ⬜ H2 뒤. **여기서야 치팅이 막힌다** |

---

## 3. 확정된 기술 스택

원칙은 **"룰 엔진을 서버와 클라이언트가 같은 코드로 돌린다"**. 이게 스택 선택의 1순위 기준이었다.

| 레이어 | 선택 | 비고 |
|---|---|---|
| 언어 | TypeScript (모노레포, npm workspaces) | |
| 전투 렌더 | Phaser 3 | 20×25 그리드, 줌/스크롤, 픽셀아트 |
| 메타 UI | React + CSS | 도시/상점/랭킹은 DOM이 훨씬 빠름 |
| 룰 엔진 | 순수 TS (`packages/rules`) | I/O·`Date.now()`·`Math.random()` 금지 |
| 실시간 | Colyseus | 방 기반 권위 서버, 상태 동기화·재접속 내장 |
| 메타 API | Fastify | |
| DB | Postgres + Redis | |
| 인증 | Firebase/Supabase Auth + 카카오 로그인 | |
| 패키징 | Capacitor | 웹 빌드 하나로 PC·Android·iOS |
| 런타임 | **Node 22 타입 스트리핑** | `.ts`를 빌드 없이 실행. tsc는 타입검사·`.d.ts` 전용 |

Godot/Unity를 택하지 않은 이유: 룰 엔진을 서버(TS)와 클라(GDScript/C#)로 **두 번 구현**하게 된다.

---

## 4. 결정 로그 ★ 원본 문서에 없던 것들

원본 기획서에 명시가 없어 이번 세션에서 사용자와 확정한 규칙. **GDD에 반영 완료.**

| 항목 | 확정 내용 | GDD |
|---|---|---|
| **맵 크기** | `20행 × 25열` **고정**. 배치 구역만 참여 수 × 5열, 중앙 정렬 | §3.1 |
| **Bishop 공격** | 4방향(직교) 1칸. 이동은 대각 4칸 | §3.2 |
| **Knight 공격** | 4방향(직교) 1칸 (원래 8방향에서 **변경됨**) | §3.2 |
| **위협 범위** | Rock 41 / Queen 39 / Bishop 37 / Pawn 33 / King 25 / Knight 25 | §3.2 |
| **waiting time** | `WT = 190 − 통솔력` | §3.3 |
| **크리티컬** | 배수 **×2**, 확률 `clamp(0,100)`, 데미지 내림 | §3.5 |
| **탈진·질병** | 지속시간 **없음(영구)** — 「결계」로만 해제 | §3.7 |
| **환술 실패** | 저항당해도 **MP는 소모** | §3.7 |
| **동시 사망** | **먼저 행동한 쪽 승리.** DoT 동시 정산은 WT 오름차순 | §3.9 |

### `WT = 190 − 통솔력`이 왜 중요한가

이 값 하나로 여러 개가 맞물린다. 바꾸려면 파급을 확인할 것.

- 최속 유닛(관우·조조 통솔 100) → **WT 90**
- 그래서 스킬 지속시간 `time 90 / 190 / 290 / 490`이 각각 정확히 **1 / 2 / 3 / 5 사이클**
- 장비 「장판하뢰」의 `WT +110 = 한 턴 쉬는 효과`라는 원문 설명과 맞물림
- 부수 효과: **통솔이 낮으면 버프 지속 턴 수에서도 손해** (통솔의 2차 효과로 해석)

### 룰 엔진 구현 중 추가로 확정한 것 ★

전부 GDD §12 「룰 엔진 구현 중 확정」에 표로 정리했고 테스트로 고정했다. 특히 헷갈리기 쉬운 셋:

- **지속시간은 시전 시각부터** 센다. 턴이 끝나며 시간이 +1 되므로 `expiresAt`은 `턴 종료 시각 + N`이 아니다.
- **「초선」으로 조종당한 유닛은 제 편을 친다.** `legalTargetsFor`는 "적"을 유닛의 진영이 아니라
  **지시를 내리는 진영** 기준으로 판단한다. 이걸 유닛 진영 기준으로 되돌리면 초선이 무의미해진다.
- **「결계」가 가후 「좌도방술」을 이긴다.** 면역이 100% 적중보다 우선.

### 미결 항목 — 전부 해소됐다 (2026-08-04)

오프라인 사이클을 앞두고 마지막 셋을 정했다. GDD §12에 근거까지 적어 뒀다.

| 항목 | 결정 |
|---|---|
| **1:1 대전의 기물** | **1:1을 영구 삭제.** 3:3 / 5:5만 — King 하나뿐이면 기물 조합이라는 정체성이 빠진다 |
| **레벨업 실패 시 카드** | **실패를 없앤다 — 전 레벨 100%.** 난이도는 필요 카드 수로만 |
| **AI 대전의 보상** | ~~군량만. 카드는 없다 — 봇 파밍 방지~~ → **2026-08-18에 뒤집혔다.** 온라인과 **완전히 같다** — 문이 [출정하기] 하나로 합쳐지면 상대를 고를 수 없어서다 (§7 「전적 · 보상 개편」) |

> 파급 — `BattleMode`가 `'3v3' | '5v5'`가 됐고, `growth.json`에서 `successRate`를 뺐다
> (엑셀에는 남아 있으나 추출기가 걸러 내며 안내한다).
>
> **「오프라인만으로는 카드가 늘지 않는다」는 2026-08-18에 끝났다** — AI 대전도 카드를
> 준다. 개발용 지급(「카드 +5」·「금화 +10」)은 **상점이 붙을 때까지** 그대로 둔다.

---

## 5. 데이터 파이프라인 ★ 반드시 알아야 할 것

### 원본 엑셀은 절대 수정하지 않는다

정규화는 전부 `tools/extract_data.py`에서 처리한다. 엑셀을 고치면 `npm run extract`만 다시 돌린다.
추출기는 Python 표준 라이브러리만 쓴다(의존성 0).

```bash
npm install
npm run portraits           # assets/ → 웹용 그림 3종. 이미지는 git에 없다(아래 참조)
npm run actions             # assets/CharsAction/ → 액션 스프라이트시트 260장 (약 1분 30초)
npm run vfx                 # assets/SpecialStatus/ → 시각 효과 30장 (지속형 23 + 일회성 7)
npm run terrain             # assets/map/ → 지형 그림 3종 (화계·수계·성지)
npm run frames              # assets/map/ → 판 지도 · 카드 벽보 액자 · 산수 배경
npm run audio               # assets/Audio/bgm/ → 배경음악 6곡
npm run backgrounds         # assets/Backgrounds/ → 화면 배경 14장 (시간대 3 · 자리 4×2)
npm run extract             # docs/*.xlsx → packages/data/generated/*.json (검증 실패 시 exit 1)
npm run typecheck           # tsc --build
npm test                    # node --test — 308건

npm run dev                 # http://localhost:5173
npm run balance -- 5000     # 자동 대전 5000판 승률 통계 (약 20초). 기본 Lv1
npm run balance -- 5000 --level 9 --stat mix   # 성장을 넣고 재기 (hp|mp|at|mix)
npm run power -- 20000 --calib 5000            # 전투력 산출식 재도출 (약 8분, §7)
npm run smoke:ui            # 전투 화면 연동 스모크 (dev 서버가 떠 있어야 한다)
npm run smoke:meta          # 메타 연동 스모크 — 계정→편성→배치→정찰→전투→레벨업→저장
npm run shot -- --zoom 2.4  # 스크린샷 (dev 서버 필요). --zoom은 제어권 유닛에 카메라를 붙인다
```

`http://localhost:5173`는 이제 **간판 화면**으로 열린다 (2026-08-15). 「입장」을 누르면
계정이 없으면 도시 이름부터, 있으면 곧바로 도시로 간다. 전투만 바로 보려면 `?demo=1`을
붙인다 — 계정·편성을 건너뛰고 예전처럼 데모 편성으로 한 판이 뜬다. `smoke:ui`와 `shot`이 이 길을 쓴다.

URL 쿼리: `?demo=1&seed=3&mode=5v5&side=P2` · `?demo=1&auto=1`(양쪽 AI 관전) ·
`?demo=1&sp=15`(시작 SP — 고유기술 확인용)

> **`assets/`는 리포에 없다**(기획자 방침 — PNG 233MB). `assets/Chars/`가 로컬에 있어야 하며,
> 없으면 `npm run extract`가 초상화 대조를 건너뛴다고 알리고 통과한다 — 빌드는 정상이다.
> 에셋을 참조하는 검증을 새로 붙일 때도 **없으면 건너뛰기**로 처리한다.

> **로컬 `assets/`에는 지금 쓰는 3종만 남아 있다 (2026-08-07).** 그림을 만들어 낸
> 상류 자료 — `Images/`(Gemini 원본 카드)와 배경이 붙어 있던 예전 `Chars/` — 는
> **기획자가 통째로 별도 백업으로 옮겼다.** 지워진 것이 아니라 작업 폴더에서 빠진 것이다.
> `crop_chars.py` · `remove_char_background.py`를 다시 돌릴 일이 생기면 백업에서 꺼내
> `--src`로 지정한다. 백업 위치는 기획자만 알고 있으니 물어볼 것.

### 웹용 그림 3종 — `npm run portraits`가 한 번에 만든다

| 원본 | 출력 | 쓰는 곳 |
|---|---|---|
| `assets/Chars/*.png` 440×540 · 260장 **투명** | `public/portraits/{장수id}.png` 96×120 **투명** | 보드 타일 |
| `assets/CharsInBattle/*.jpg` ~808² · **260장** | `public/battle/{장수id}.jpg` 200² | 하단 패널·정보 팝업의 수묵화 |
| `assets/SpecialSkills/*.jpg` ~813×168 · 40장 | `public/skills/{기술id}.jpg` 폭 720 | 고유기술 발동 연출 배너 |

> **세 종류 다 260/260(연출은 40/40)으로 채워졌다 (2026-08-07).** 빠진 장수가 없다.
> 그래도 `ui/art.ts`의 물러나기(수묵화 → 타일 → 숨김)는 그대로 둔다 — 에셋이 리포에
> 없어서 받아 오기 전에는 셋 다 404다. 그림이 없다고 화면이 무너지지 않아야 한다.

> **`assets/Chars/`는 이제 배경이 없다 (2026-08-07 기획자 교체).** 원래 양피지 배경째
> 잘린 그림이었고 배경 제거본은 `Chars-noback/`에 따로 있었는데, **기획자가 제거본을
> `Chars`로 이름을 바꿔 원본 자리를 대신하게 했다.** 타일 원본은 이제 하나뿐이다.
> 유닛이 판 위에 서 있는 것처럼 보이고 진영 칸 색이 그대로 살아난다.

> `X`로 시작하는 파일(`X동소.jpg`)은 기획자가 걸러 둔 것이라 빌드에서 건너뛴다.
> 지금은 그런 파일이 없지만 규칙은 살아 있다.

### 액션 스프라이트시트 — `npm run actions` (2026-08-12 추가)

| 원본 | 출력 | 쓰는 곳 |
|---|---|---|
| `assets/CharsAction/{이름}.png` 또는 `.jpg` 1535×310 · 260장 | `public/actions/{장수id}.png` 550×110 | **보드 타일** |

원본은 **가로 5칸**이고 순서는 `대기 · 이동 · 공격 · 책략/명상 · 피격`이다. 출력은 110² 다섯
칸을 가로로 이어 붙인 것으로, Phaser의 `load.spritesheet`가 그대로 읽는다. 상태 전환은
`setFrame(n)` — 텍스처 교체가 없다.

> **보드 타일이 `portraits/`에서 이쪽으로 넘어왔다.** `portraits/`는 이제 정보 팝업의
> 폴백으로만 남는다(`ui/art.ts`의 `setOfficerArt`).

입력이 두 갈래이고 도구가 알아서 가른다.

| 입력 | 처리 |
|---|---|
| **마젠타 합성본** (JPG 108장) | 배경색을 파일마다 뽑아 `p = a·F + (1−a)·B` 를 풀어 알파를 **복원** |
| 알파 PNG (152장) | 알파를 그대로 믿는다 |

> **앞으로는 마젠타 합성본을 그대로 넣는 편이 낫다.** 배경제거를 먼저 돌리면 경계 픽셀이
> 「얼마나 배경이었는지」가 사라져 마젠타 테두리가 남는다 — 같은 장수 8명 비교에서 잔여
> 마젠타 **30,000px 대 100px** 이었다. 경위는 [세션 기록 §4](history/2026-08-12_액션시트_5자세_연출.md).

도구가 하는 일 — 가장자리 띠 제거 · 배경 제거 · **몸통 5개 찾기**(균등분할은 무기를 자른다,
경계 608곳 중 178곳이 침범) · 잔디 제거 후 타원 그림자 · **몸통 높이를 칸의 90%로 통일**
(원본은 239~305px로 28% 차이) · 발끝 정렬 · 110² 축소.

```bash
npm run actions                           # 260/260. 실패는 이름을 찍어 알린다
npm run actions -- --only 관우 --force     # 한 명만 다시
npm run actions -- --sheet 대조.png        # 눈으로 볼 대조 시트
```

### `assets/SpecialSkills/` — 고유기술 연출 이미지 40장 (2026-08-03 추가)

고유기술을 시전할 때 띄울 컷. **고유기술 40종과 개수가 정확히 일치한다.**
2026-08-04에 **화면에 붙었다** — 발동하면 판 위에 2초 뜬다(§7 전투 씬 3차).

**파일명 규약**(기획자 지정) — `장수이름 기술이름.jpg`.
마지막 띄어쓰기 앞이 보유자, 뒤가 기술명이다. A·B급처럼 여러 장수가 공유하는 기술은
이름을 **쉼표로** 잇고 마지막 이름 뒤에 띄어쓰기 + 기술명.

```
마초 용호상박.jpg
정봉,고람,양회,반장,조홍,장수,악진,주영,순우경,양안 일격필살.jpg
```

> **기술명에 공백이 있는 3종은 이 규칙과 충돌한다** — 「신재조영 심재촉」·「인중여포 마중적토」·
> 「화용도 의석조조」. 지금 파일들은 공백을 지워 붙여 쓰는 것으로 피해 갔으므로,
> 자동 매칭을 붙일 때는 **양쪽 다 공백을 제거한 뒤 비교**해야 한다.

**대조 결과 — 어긋남 없음** (2026-08-04). `npm run extract`가 이 대조를 자동으로 한다
(`check_skill_art()`). 처음 확인했을 때는 8건이 어긋나 있었고 기획자가 전부 정정했다 —
오타 2건(유언비책→유언계책, 신재조명→신재조영) · 이름 정규화 2건 · 십면매복의 장로 제거 ·
구분자 오타 1건. **그 과정에서 「장료지제」가 장수 이름 표준을 뒤집었다**(아래).

**이제 어긋나면 빌드를 막는다** (2026-08-04, 화면에 붙이면서 올렸다). 이름이 어긋난 기술은
발동 연출에서 배너가 빠져 글자만 뜨기 때문이다. 폴더 자체가 없으면 예전처럼 건너뛴다.

### `assets/SpecialStatus/` — 시각 효과 30장 (2026-08-13 추가)

기술·책략이 걸렸다는 것을 **그림으로** 알린다. 사양은 GDD §3.10 「시각 효과」.

| 원본 | 출력 | 쓰는 곳 |
|---|---|---|
| `{1..23}.png` 258² (`6`·`9`·`23`만 ~312²) | `public/vfx/{n}.png` 256² | **캐릭터 뒤에 깔리는 지속형 링** |
| `{A..G}.png` 550² (2×2) | `public/vfx/{A}.png` 1024×256 (4칸) | **판 한가운데 일회성 애니메이션** |

> **「오라」라고 부르지 않는다 ★** 엔진에 이미 오라가 있다 — `auraIncomingHalf` ·
> `aurasOn()`은 여포·허저의 **반경 판정**이고, 이쪽은 화면에 겹쳐 그리는 **그림**이다.
> 기획자와 **`visualEffect`**로 부르기로 했다. 엑셀 시트 이름만 「오라매핑」으로 남아 있으니
> 코드에서 `aura`라는 말이 보이면 그것은 반드시 반경 판정 쪽이다.

**그냥 복사하지 않는 이유** — 원본이 258²과 ~312²로 섞여 있고 링마다 여백이 달라,
그대로 쓰면 같은 자리에 뜨는데 크기가 제각각이다. 알파 경계상자로 잘라 정사각으로
채운 뒤 한 크기로 줄여 **「그림이 실제로 차지하는 지름」을 30장 모두 맞춘다.**
알파벳 4칸은 **공통 경계상자**로 자른다 — 칸마다 따로 자르면 밝기가 차오르는 동안
링이 커졌다 작아졌다 흔들린다.

**매핑표의 단일 출처는 `tools/extract_data.py`의 `STATUS_FX_*`** 이고
`visualEffects.json`으로 나간다. 해석은 `client/src/battle/visualEffect.ts`.
`npm run extract`가 시트·그림을 대조해 **어긋나면 빌드를 막는다** — 쓰이지 않는 그림이
있거나(처음 대조했을 때 `14.png`가 그랬다) 표가 없는 그림을 가리키면 걸린다.

### `assets/map/` — 지형 그림 3종 (2026-08-14 추가)

지형(GDD §3.8)을 **칸에 그림으로** 알린다. 그때까지는 말풍선 문장뿐이라
「E7에 수계가 생겼다」를 읽고 판에서 그 칸을 짚어야 했다.
사양은 GDD §3.10 「지형 그림」.

| 원본 | 출력 | 지형 |
|---|---|---|
| `assets/map/지형_불.png` 256² | `public/terrain/fire.png` 128² | **화계** — 위에 선 유닛의 HP가 준다 |
| `assets/map/지형_물.png` 256² | `public/terrain/water.png` 128² | **수계** — 진입 불가 |
| `assets/map/지형_성채.png` 256² | `public/terrain/holy.png` 128² | **성지** — 손권 「수성지주」 |

**한글 이름이 지형 id로 바뀌는 자리는 `tools/build_terrain.py`의 표 하나뿐이다.**
원본은 기획자가 읽을 이름이고 웹은 엔진의 `TerrainId`다 — URL에 한글이 남으면
서버·CDN마다 인코딩이 갈리고, 화면(`battle/terrain.ts`)은 `terrain/{id}.png`라는
규약만 알면 된다. 표에 없는 그림(`지형_돌`)은 **이름을 찍어 알리고** 건너뛴다.

> **「지형 그림」과 「지속형 링」은 다른 층이다.** 지형은 **칸**에 붙박이로 깔리고
> (판과 하이라이트 사이), 링은 **유닛**에 붙어 함께 움직인다. 성지 칸에 유닛이 서면
> 둘 다 뜬다 — 칸에는 성채 그림, 그 유닛에는 링 `17`. 「이 칸이 성지다」와
> 「이 유닛이 그 효과를 받는 중이다」로 뜻이 다르다.

> **`지형_돌`은 아직 연결하지 않았다** — 엔진의 `TerrainId`가 `fire`/`water`/`holy`
> 셋뿐이라 놓을 자리가 없다. 새 지형으로 넣을지는 기획자에게 물어야 한다(§7).

### `assets/map/` — 판 지도와 카드 벽보 (2026-08-14 리디자인)

기획자가 목업을 주고 **전투 화면의 바탕을 그림으로 갈았다.** 사양은 GDD §3.10
「화면 바탕 — 지도와 벽보」, 굽는 것은 `tools/build_frames.py`(`npm run frames`).

| 원본 | 출력 | 쓰는 곳 |
|---|---|---|
| `chessmap.png` 750² | `public/ui/chessmap.png` | **체스판 아래에 깔리는 지도** (Phaser, depth −1). 채도를 빼고 종이색으로 밀어 **눌러서** 굽는다 |
| `person.png` 1440×2912 | `public/ui/card-frame.png` 480×1040 | **카드 벽보 액자** — CSS `border-image`가 9분할로 두른다 |
| 〃 (액자 바깥 산수) | `public/ui/backdrop.png` 960×420 | 카드 스트립 뒤 배경 (CSS) |

**액자를 9분할로 굽는 것이 이 작업의 핵심이다.** 3:3이면 카드 세 장, 5:5면 다섯 장이
한 줄에 들어가 칸의 가로세로비가 판마다 다르다 — 그림을 통째로 늘이면 3:3에서 지붕이
옆으로 퍼진다. 네 귀퉁이를 고정하고 변만 늘이면 **지붕 두께와 기둥 굵기가 화면에서
언제나 같다.** 자르는 자리(`SLICE`)는 `style.css`의 `.uc-frame`과 **같은 값이어야
하고**, 도구가 돌 때마다 그 CSS 한 줄을 찍어 준다.

> **원본을 그대로 오려 쓰지 않는다.** 처마가 기둥보다 넓어서 몸통 폭으로 자르면 처마
> 끝이 잘린다 — 지붕 띠만 가로로 20% 눌러 몸통에 맞췄다(기와 무늬라 티가 안 난다).
> 나무 바깥의 크림색 배경도 판다. 다만 **흰 종이(248,247,245)와 크림색(244,239,233)의
> 차이가 12뿐**이라, 색만 보고 지우면 종이가 통째로 뚫린다 — 종이가 없는 **줄**에서만 판다.

> **셋 다 없어도 화면은 돈다.** 지도가 없으면 판이 예전의 어두운 격자로 물러나고
> (`BattleScene.drawBoard`), 액자는 `border-color: transparent`로 테두리만 사라진다.

> **지도의 세기는 손잡이가 밖에 있다** (기획자 지적 「배경 컬러가 세게 느껴진다」).
> `npm run frames -- --force --map-desat 0.6 --map-fade 0.5` 처럼 눈으로 보며 정한다.
> 기본값은 채도 −45% · 종이 +42%다. **화면에서 반투명으로 덮는 방식은 쓰지 않았다** —
> 덮으면 그 위에 그리는 하이라이트·기물까지 같이 뿌예진다.

### `assets/Audio/` — 배경음악 (2026-08-14 추가)

**화면마다 한 곡.** 기획자 지정 그대로다. `tools/build_audio.py`(`npm run audio`)가
`public/bgm/{id}.mp3`로 옮기고, 고르는 규칙은 `client/src/audio/bgm.ts`에 있다.

| 원본 | id | 언제 |
|---|---|---|
| `배경곡_1_메인.mp3` | `main` | 첫 화면 · 메인 · 장수 관리 — **아래 넷이 아닌 모든 화면** |
| `배경곡_2_장군선택.mp3` | `roster` | 3:3/5:5를 고른 뒤 기물·장수를 고르는 화면 |
| `배경곡_3_배치정탐.mp3` | `prep` | 전투 화면의 **배치·정찰** 단계 |
| `배경곡_4_전투.mp3` | `battle` | 전투 |
| `배경곡_5_전투종료.mp3` | `result` | 전투 종료 후 보상 화면 |
| `배경곡_6_크레딧.mp3` | `credits` | **화면이 아직 없다** — 굽기만 해 둔다 |

**전투 화면만 화면 이름으로 정하지 않는다.** 안에서 배치·정찰과 전투가 갈리는데
그 경계를 아는 것은 `Playback.phase`뿐이라 `BattleScene`이 고른다. 나머지는
`screens/App.tsx`가 화면 이름으로 고르고, **모르는 화면은 메인곡**이다 —
지정이 「넷이 아닌 화면」이라 상점·랭킹이 생겨도 손대지 않아야 그 말이 지켜진다
(`test/bgm.test.ts`가 그걸 고정한다).

> **브라우저는 사용자가 건드리기 전에 소리를 내주지 않는다 ★** 첫 화면에서 `play()`가
> 거부되므로 곡을 물고 있다가 **첫 클릭·첫 키**에 다시 튼다. 실측으로도 첫 화면에서는
> `blocked: 'main'`이었다가 클릭 뒤 풀린다. 이걸 모르면 「첫 화면만 소리가 안 난다」로
> 보이는데 원인이 코드에 없어 한참 헤맨다.

> **`M` 키로 음소거** (새로고침해도 기억한다). 화면에 단추를 두지 않은 것은 기획자가
> 지정한 자리가 아직 없어서다 — 여러 판 돌려 보는 동안 끌 길은 있어야 한다.

> **곡을 갈아 끼우면 `npm run audio`만 다시 돌리면 된다 ★** (2026-08-14). 이 도구는
> 다시 옮길지를 **시각이 아니라 내용으로** 정한다. 기획자가 3번과 4번의 순서를
> 바로잡느라 **내용을 맞바꿔** 저장했는데 파일 시각은 예전 그대로였다 — 시각만 보던
> 도구는 「이미 최신」이라며 조용히 건너뛰었고, 화면에서는 여전히 옛 곡이 나왔다.
> **소리는 눈으로 확인할 수 없어서 더 위험하다.** 지금은 갈아 끼운 곡을
> `prep(갈아끼움)`처럼 이름을 찍어 알린다.
>
> **그림 도구들(`terrain`·`frames`·`vfx`·`actions`)은 아직 시각으로 본다.** 원본을
> 맞바꾸고 시각이 그대로면 같은 함정에 빠지므로 그때는 `-- --force`를 붙인다.
> 그림은 화면에서 곧바로 보이기 때문에 아직 그대로 뒀다.

**아직 연결하지 않은 것 둘.** `effects/`는 **비어 있고**, `Specialskills/`는 5개 언어
(CA·EN·JP·KR·PT) × 3곡(가후지책·구벌중원·백기겁위영)뿐이라 고유기술 40종에 한참 못
미친다. `npm run audio`가 돌 때마다 둘의 개수를 찍어 알린다.

> **`Specialskills/KR/가후지잭_KR.mp3`은 오타로 보인다** — 다른 네 언어는 전부
> 「가후지**책**」이다. 음성을 붙일 때 이름으로 짝을 맞출 것이라 지금 정정하는 편이 낫다.

### `assets/Backgrounds/` — 화면 배경 (2026-08-15 추가)

간판·도시·자리 화면의 **바탕**이다. 사양은 GDD §8.1, 굽는 것은
`tools/build_backgrounds.py`(`npm run backgrounds`).

| 원본 | 칸 | 출력 | 고르는 기준 |
|---|---|---|---|
| `openBackground_big.jpg` 2066×1536 | 3 | `public/backgrounds/open-{day,dusk,night}.jpg` | **접속 시각** |
| `mainBackground_big.jpg` 2062×1536 | 3 | `main-{day,dusk,night}.jpg` | 〃 |
| `eachBackground_big.jpg` 2752×1536 | 4 | `place-1-{palace,barracks,market,extra}.jpg` | **도시 Lv1~4** |
| `eachBackground2_big.jpg` 2912×1440 | 4 | `place-2-…jpg` | 도시 Lv5 이상 |

> **`_big`이 있으면 그쪽을 쓴다 ★** (2026-08-15). 처음 받은 PNG는 폭이 804~900px이라
> 칸 하나가 **225~300px**이었고, 프레임(320~700px)에 늘려 깔면 눈에 띄게 깨졌다.
> 기획자가 같은 그림의 고해상도 `{이름}_big.jpg`를 더해 줬다. 고르는 순서는
> `_big.jpg → _big.png → .png`이고, **한쪽만 있어도 돈다** — 어느 것으로 구웠는지
> 실행할 때마다 찍어 준다(`… (저해상도)`).
>
> **출력이 JPG다.** 칸 하나가 687×1536이라 PNG면 열넉 장에 25MB가 넘는다 —
> 지금은 **4.2MB**다. 확장자는 도구와 `screens/backdrop.ts` **두 곳이 함께** 바뀌어야
> 한다. 한쪽만 고치면 화면은 뜨는 것처럼 보이고 그림만 404다(배경은 없어도 바탕색으로
> 물러난다). 도구가 계획에 없는 출력을 지우므로 폴더에 PNG·JPG가 섞이지는 않는다.

**칸 순서를 id로 바꾸는 표는 그 도구의 `STRIPS` 하나뿐이다** — `build_terrain.py`가
한글 파일명을 `TerrainId`로 바꾸는 자리와 같은 결이다. 화면은
`backgrounds/{id}.jpg`라는 규약만 알고, 「지금 어느 id인가」는
`client/src/screens/backdrop.ts`가 정한다.

> **등분만 하면 옆 칸이 몇 픽셀 딸려 온다 ★** `openBackground.png`는 804px인데 진짜
> 경계가 268·536이 아니라 **266·535**다. 등분해 자르면 낮 그림 오른쪽에 황혼의 주황
> 띠가 2px 남는데, 화면에서는 「오른쪽 끝에 이상한 줄이 있네」로만 보인다.
> 지금은 등분한 자리 언저리(±1%)에서 **색이 가장 크게 튀는 열**을 찾아 자르고,
> 옮겼으면 그 값을 찍어 알린다. 안쪽 가장자리는 1px 더 깎는다.

> **다시 구울지는 「시각」이 아니라 「내용」으로 정한다** — `build_audio.py`가 밟은
> 지뢰와 같은 자리라, 원본 해시를 `.sources.json`에 남겨 두고 내용이 달라졌을 때 굽는다.

> **넷째 칸(`extra`)은 이름만 잡아 뒀다.** 게시판(수배·공고) 그림인데 어디에 이을지
> 기획자가 「추후에 알려주겠다」고 했다 — 이름을 지어 붙이면 그게 뜻인 것처럼 굳는다.

#### 배경이 천천히 움직인다 (2026-08-15 추가)

기획자 지정 「2초나 3초 간격으로 약간씩」. 여덟 자세를 2.6초마다 하나씩 짚으며
아주 조금 밀고 당긴다(한 바퀴 약 21초). 자세표는 `screens/backdropMotion.ts`,
걸음을 세는 것은 `screens/useBackdropDrift.ts`, 옮기는 것은 CSS `transition`이다.

> **그림을 화면 div의 배경으로 두면 안 된다 ★** 원래는 `.scr-bg` 자신의
> `background-image`였는데, 거기에 `transform`을 걸면 **제목·기어·입력칸이 같이
> 흔들린다.** 그림만 `.scr-art` 층으로 떼어 냈고, 그러면서 쌓임 순서를 손으로
> 정하게 됐다 — **그림 0 → 어둠(`::before`) 1 → 내용 2.** 셋은 함께 봐야 뜻이
> 통한다. 하나만 지우면 어둠이 글자를 덮거나 그림이 어둠을 덮는다.

> **확대(`DRIFT_ZOOM` 1.06)는 장식이 아니라 안전 여백이다.** 배율 1인 그림을
> 옆으로 밀면 반대쪽에 바탕색 띠가 드러난다 — 「등분해 자르면 옆 칸이 딸려 온다」와
> 똑같이 화면에서는 「끝에 이상한 줄이 있네」로만 보인다. `밀어내는 거리 < (배율−1)/2`를
> **여덟 자세 전부** 테스트가 확인하고, 스모크가 「글자는 제자리인가」를 좌표로 잡는다.

> **자세를 난수로 고르지 않는다.** 굴리면 여백을 넘는 자세가 어쩌다 한 번 나오는데
> 그건 재현이 안 된다. `backdropMotion.ts`는 시계도 난수도 모르는 순수 함수다.

> **「동작 줄이기」를 켠 사용자에게는 움직이지 않는다** — `prefers-reduced-motion`.
> 걸음을 세지 않고(훅), 자세 전환도 없앤다(CSS). 배경이 흔들리면 어지러운 사람이 있다.

> **편성 화면(`.scr-dim`)은 그대로 멈춰 있다.** 거기 그림은 「여기가 병영이다」만
> 말하고 뒤로 물러나야 하는 자리라(거의 불투명한 어둠을 덮는다) 움직여도 보이지 않는다.

### 「장요」 → 「장료」로 통일 ★ (2026-08-04 기획자 결정)

한자 `張遼`의 바른 독음은 **장료**다. 엑셀 장수 시트와 초상화가 `장요`였고 스킬 시트만 `장료`라
지금까지는 초상화 파일명을 표준으로 삼아 `장료→장요`로 맞췄는데, **방향을 뒤집었다.**

바뀐 것 — `assets/Chars/장요.png` → `장료.png` · `NAME_FIXES`가 `"장요": "장료"` ·
장수 id `jang-yo` → **`jang-ryo`** · 웹용 초상화 재생성. 스킬 「장료지제」는 원래부터 맞았다.

> **이름을 바꾸면 id가 바뀐다.** id는 이름에서 만든 슬러그라서다(GDD §9). 아직 저장된 데이터가
> 없어 지금은 공짜지만, 메타(계정·보유 카드)가 붙은 뒤에는 마이그레이션이 필요하다.

### 자동 처리되는 정규화 (GDD §9)

| 처리 | 내용 |
|---|---|
| 이름 통일 | `관훙→관흥`, `장노→장로`, `장요→장료` — **표준은 `assets/Chars/` 파일명** |
| 소속 통일 | `장노군→장로군` |
| 중복 행 해소 | 스킬 시트에 한 장수가 여러 번 나오면 **장수 시트의 등급과 일치하는 행**만 채택 (하드코딩 아님) |
| id 부여 | 한글→로마자 슬러그. `관우`→`gwan-u`, `제갈량`→`je-gal-ryang`. 표시명이 바뀌어도 참조 유지 |
| WT 계산 | `wtBase = 190 − 통솔력` |

> 참고: `이곽`은 한때 "Chars 폴더 기준" 규칙 탓에 표준이었으나, 2026-08-07에 역사 표기
> `이각(李傕)`으로 되돌렸다 (초상화 파일명 · `NAME_FIXES` · id `i-gak` 전부 함께). GDD §9 참조.

### 검증된 사실

- 장수 **260명** = 초상화 **260장** 완전 대응 (정규화 후)
- 등급 분포 **S 30 / A 40 / B 55 / C 90 / D 44 / E 1**
- 고유기술 **40종** — S급 30종은 1인 전용, A급 4종·B급 5종은 공유, E급 1종(헌제)
- SP 코스트 B 4 / A 5 / S 6 / E 7
- 능력치 합계·최고점 계산 오류 0건
- 팀 등급 점수표 13행 전부 재계산 일치

### 데이터의 단일 출처

기물 마스크는 `packages/data/generated/pieces.json`이 유일한 출처다.
**Python 추출기와 TS 테스트가 각각 독립적으로 위협 범위를 재계산해 확정치와 대조**하므로,
마스크를 잘못 건드리면 양쪽에서 잡힌다.

---

## 6. 현재 상태

```
npm run extract   →  검증 통과 — 문제 없음
npm run typecheck →  exit 0
npm test          →  380/380 pass
npm run balance   →  5000판 20초 (결과는 §7)
npm run power     →  모드당 2만 판 488초 (결과는 §7 「부대 전투력 산출식」)
npm run smoke:ui  →  통과 — 확인 항목 22개 (dev 서버 필요)
npm run smoke:meta →  통과 — 계정·부대 편성(CRUD·배치 프리셋)·**출정하기(구성·부대·매칭 셋)**·
                      **거절 경계(안내문·바닥에서 사라짐)**·**참가비는 [전투준비]에서만**·
                      배치·정찰·전투·**항복→결과→계정 반영**·장수 일람·상세·기술 팝업·
                      레벨업 두 걸음·재설계·**v1·v2 되접기**·전적 관리와 필터·
                      도시 관리(군량 충전·증축·도시 전적)·저장 (dev 서버 필요)
```

테스트 380건의 내역 — 데이터 정합성 12 · 스케줄러/행동 39 · 책략 23 · 고유기술 19 + S급 28 ·
**전송 이음매(로그 이어 붙이기·스냅샷 크기·판정 동일성·마감·연출) 17** ·
**메타(계정·편성) 14** · **장수 일람(정렬·검색·요약·AT 범위) 13** ·
**부대 전투력(고정 표본·단조성·대칭·계약) 22** · **성장 스택·되접기·재설계·레벨 하향 29** ·
**전적·보상(12조합·기물별 합·필터·이력·v3 되접기) 29** ·
**도시(군량 충전·상한·시계 되감기·증축·판수 대 인원수) 15** ·
**부대(상한·이름·CRUD·엔진 통과·하향·배치 프리셋·되접기) 27** ·
**매칭(거절 경계·AI 상대의 전투력 전 구간·엔진 통과·결정성·단조) 14** ·
연출 시간표 20 · 카메라 14 · **시각 효과 20** · **지형 4** · **배경음악 5** ·
**화면 배경·다국어 9** · **배경의 움직임 4**.

생성물(`packages/data/generated/*.json`)은 **커밋 대상**이다. 엑셀 없이도 빌드가 되어야 하므로.

`tactics.json` 18종, `uniqueSkills.json` 40종 **전부 채워졌다.**
고유기술 40종 중 스크립트 핸들러가 필요한 것은 **소패왕전·차동풍 둘뿐**이고 나머지는 데이터로 접혔다.

### 룰 엔진 구조

```
packages/rules/src/
  types.ts      타입 계약 — Effect DSL, BattleState, Intent, FORMULA
  timing.ts     전투 단계의 **실시간** 제한 (배치·대기·정찰·제어·재생속도). 값만 있고 시계는 없다 —
                클라와 서버가 **같은 것을 본다**
  rng.ts        시드 PRNG. (seed, cursor) → 값 인 순수 해시 (카운터 기반)
  pieces.ts     기물 기하 — legalMoves / attackCells / threatRange
  state.ts      상태 원시 연산 — 조회 · 체력 · 사망 · 승패 · 공격 판정
  effects.ts    Effect DSL 실행기 + 환술 판정
  scripts.ts    서사형 고유기술 스크립트 (소패왕전·차동풍)
  ai.ts         기준 AI + 자동 대전 (밸런스 검증용). **책략은 칠 수 없는 턴에만 쓴다**
  battle.ts     CTB 스케줄러 · 검증 · 의도 적용. RulesEngine 계약 구현체 `engine`

packages/meta/src/            ★ 계정의 권위. rules와 같은 자리에 선다 (순수 함수, I/O 없음)
  types.ts        PlayerProfile · OfficerInstance · **GrowthStep** · RosterPick
  profile.ts      계정 생성(S·A·B·C·D 지급) · 카드 · 레벨업(실패 없음) ·
                  **성장 스택을 읽는 파생 함수 둘** · 증분 미리보기 · 재설계
  migrate.ts      **저장된 계정을 지금 형식으로 되접는다** — 버리지 않고 채워 넣는다
  city.ts         도시 — 군량 시간 충전(`syncGrain`) · 증축. **시각은 밖에서 받는다**
  officers.ts     장수 일람 — 줄 만들기 · 검색 · 정렬 · 등급 요약 · **AT 범위**(FORMULA 경유)
  power.ts        부대 전투력 · 예상 승률 · 매칭 구간 (pptx 44). 계수는 실측이고 상수다
  roster.ts       편성 검증 · RosterEntry 변환(**레벨 하향이 여기 걸린다**) · 군량
  match.ts        출전·매칭 (pptx 45). 거절 군량 경계 · **AI 상대를 전투력으로 만든다**
  squads.ts       부대 편성 · 배치 프리셋 (pptx 42·43). `validateRoster`를 감싼다
  rewards.ts      전투 보상과 전적 (AI는 군량만)

packages/client/
  index.html            #root 하나. 화면은 전부 React가 그린다
  src/main.tsx          React 붙이기 + `?demo=1` 갈림길
  src/screens/          메타 화면 (React) — App · 간판 · 새 계정 · 도시 · 자리 · 결과
  src/screens/OfficerListScreen.tsx   장수 일람 — 표·검색·정렬·레벨업 Flag (pptx 37·38)
  src/screens/OfficerDetailScreen.tsx 장수 상세 — 정보 · 서사 자리 · 세 갈래 (pptx 38)
  src/screens/SkillModal.tsx          고유기술 팝업 — 배너 · 효과 · 유래 자리 (pptx 38)
  src/screens/LevelUpScreen.tsx       레벨/스킬 관리 · 재설계 (pptx 39) — 관리 · 고르기 두 걸음
  src/screens/CityScreen.tsx          도시 관리 (pptx 41) — 도시 정보 · 증축 확인 팝업
  src/screens/CityRecordsScreen.tsx   도시 전적 (pptx 41) — 모드 두 줄 + 총합, 필터 3갈래
  src/screens/SquadListScreen.tsx     편성 부대 목록 (pptx 42) — 모드 묶음 · 전투력 · 삭제
  src/screens/SquadNameScreen.tsx     새 편성 만들기 (pptx 43) — 이름 · 참여 인원
  src/screens/SquadEditScreen.tsx     부대 편성 (pptx 42·43) — 구성 · **레벨 눈금** · 전투력
  src/screens/SquadDeployScreen.tsx   배치 프리셋 (pptx 42) — 남군·북군 각각
  src/screens/SortieScreen.tsx        출전 (pptx 45) — 구성 · 부대 고르기 · 최소군량 안내
  src/screens/MatchScreen.tsx         매칭 (pptx 45) — 세 상태 · 다시 찾기 · 전투준비
  src/meta/matchmaking.ts   온라인 탐색 **이음매**. Colyseus가 이 함수 하나를 갈아 끼운다
  src/screens/backdrop.ts      시간대·도시 레벨 → 배경 그림 (순수 함수. 시계는 밖에서 넣는다)
  src/screens/backdropMotion.ts  배경이 짚어 가는 여덟 자세 (순수 함수. 확대는 안전 여백이다)
  src/screens/useBackdropDrift.ts  걸음을 세는 자리 — 시계와 「동작 줄이기」를 읽는 유일한 곳
  src/screens/ScreenChrome.tsx 배경 + 「제목·기어」 헤더 + 환경설정 — 배경 화면 넷의 공용 틀
  src/i18n/             다국어 5종. 한국어만 채워져 있고 없으면 한국어로 물러난다
  src/screens/BattleStage.tsx  전투 UI가 id로 찾는 자리를 그린다 (실전·데모 공용)
  src/meta/storage.ts   계정 저장 — **온라인이 붙으면 이 파일만 서버 API가 된다.**
                        되접기는 여기 없다(meta의 `migrate.ts`). 읽고 쓰는 일만 남는다
  src/battle/boot.ts    Phaser를 켜고 끈다 + 로그에서 처치 수 세기
  src/style.css         전부 rem. 기준값은 useFrameFit이 프레임 폭에 맞춰 정한다
  src/battle/layout.ts      셀 96×120 → 보드가 정확히 2400×2400 정사각형이 된다
  src/battle/transport.ts   ★ **전송 이음매** — 판정 주체와 이야기하는 통로 하나.
                            `LocalTransport`(같은 프로세스의 룰 엔진)가 지금 유일한 구현이고
                            **온라인은 여기에 한 벌 더 붙는다.** 로그를 떼고 붙이는 자리이기도 하다
  src/battle/playback.ts    ★ 이벤트 재생 레이어 — **상태를 만들지 않고 받은 통을 소화한다**
  src/battle/poses.ts       연출 타임라인 — 자세 + **카메라 큐**를 같은 커서 위에 짠다
  src/battle/camera.ts      100%/200% 큐 해석 + 부드러운 추적 (Phaser를 부르지 않는다)
  src/battle/BattleScene.ts 보드·유닛 타일·입력·카메라 (판정은 하지 않는다)
  src/battle/setup.ts       데모 편성 — 전원 Lv9 · HP · 환술 8종 (화면 테스트용, §7 참조)
  src/ui/cardStrip.ts       판 위·아래 캐릭터 카드 — 고유기술 3색 · 차례/퇴각 (pptx 27)
  src/ui/controlModal.ts    커맨드 패널 — 판 안 플로팅, 세로 5줄 · 시전 · 조준 (pptx 29)
  src/ui/panelSlot.ts       커맨드/상태 패널의 사분면 — 가릴 것을 피해 좌우 대칭으로
  src/ui/focusToggle.ts     자동 포커싱 ON/OFF — 판 왼쪽 위. 글자는 「누르면 되는 것」
  src/battle/camera.ts      SCALE_FOCUS 하나가 포커스 배율의 단일 출처 (지금 300%)
  src/ui/hud.ts             상단 HUD 한 줄 — 절대시간 · 차례 · 북군/남군 SP · ⋯
  src/ui/systemLog.ts       판 한가운데 말풍선(최근 1줄) + 전체 기록 + 항복
  src/ui/eventText.ts       BattleEvent[] → 대화 문장 (칸 이름 A~Y / 1~20)
  src/ui/skillFx.ts         고유기술 연출 배너 2초 — 그동안 판이 멈춘다
  src/ui/inspectPanel.ts    상태 팝업 — 커맨드의 대칭 자리. **적 책략은 가린다**
  src/ui/statusChips.ts     버프/디버프 배지 — 상태 팝업이 쓴다
  src/ui/statusPopup.ts     눌러서 보는 설명 — 상태이상 · 고유기술 · 책략 공용
  src/ui/art.ts             그림 경로와 대체 규칙 (수묵화 → 초상화 → 빈자리)

tools/
  extract_data.py   엑셀 → JSON + 검증. 효과 DSL의 단일 출처(TACTIC_EFFECTS/SKILL_EFFECTS)
  build_portraits.py  에셋 → 웹용 그림 3종 (타일 초상화 · 수묵화 · 연출 배너)
  balance.ts        자동 대전 대량 실행 → 승률·결착시간 통계. **Lv1 기본 출력이 기준선이다**
  power_fit.ts      전투력 산출식 도출 → 로지스틱 회귀 · 보정 검사 · 분포 (balance의 형제)
  smoke_ui.ts       화면 좌표 → Intent → 판정 연동 확인 (playwright)
  shot.ts           스크린샷
```

`transport.ts` + `playback.ts`가 이 단계의 핵심이다. **재생기는 상태를 만들지 않는다** —
판정 주체(`BattleTransport`)가 「이렇게 됐다」를 보내면 큐에 쌓았다가 연출 속도로 소화할 뿐이다.
온라인이 붙으면 **`transport.ts`에 구현이 한 벌 더 붙고 그 위는 안 바뀐다** (2026-08-19, H1).

`state.ts`는 `battle.ts`/`effects.ts`를 import하지 않는다 — **순환 참조를 막는 경계**다.
새 기능이 이 경계를 넘으려 하면 원시 연산은 `state.ts`로 내린다.

**UI는 판정하지 않는다.** 버튼 활성 여부를 클라이언트가 계산하지 않고 전부 `validate()`에 묻는다
(`ui/controlModal.ts`). "화면에서는 눌리는데 서버가 거부하는" 어긋남을 원천 차단하는 계약이라,
나중에 React로 옮겨도 이 계약은 유지한다.
대상이 있어야 판정되는 것(이동·공격)은 **엔진이 준 후보를 하나씩 넣어 물어본다** — §7 참조.

### 검증은 5개 층이다

| 층 | 도구 | 무엇을 잡나 |
|---|---|---|
| 데이터 정합성 | `npm run extract` | 엑셀 ↔ 생성물, 이름·등급·위협범위 |
| 순수 로직 | `npm test` (350건) | 스케줄러·판정·책략·고유기술·계정/편성·**보상표 12조합·전적 교차 집계·대전 이력**·성장 스택·**저장 형식 되접기 v1·v2**·**군량 충전·증축**·**부대 하향·배치 프리셋**·연출 시간표·카메라·시각 효과 |
| 밸런스 | `npm run balance -- 5000 [--level N]` | 승률·결착시간 분포. **엔진 버그도 잡는다** |
| 전투력 | `npm run power -- 20000` | 산출식 재도출 + **보정 검사**(새 시드로 예측 대 실제) |
| 전투 화면 | `npm run smoke:ui` | 좌표 변환 → Intent → 판정, 교착, **카드·패널·카메라** |
| 메타 연동 | `npm run smoke:meta` | 계정→편성→배치→정찰→전투→레벨업→저장. **규칙과 전투를 잇는 층** |
| 눈으로 | `npm run shot` | 배치·하이라이트. 스크린샷을 직접 읽어 확인 |
| 그림 파이프라인 | `npm run actions` · `npm run vfx` · `npm run terrain` · `npm run frames` · `npm run backgrounds` | 260/260 · 30/30 · 3/3 · 14/14 생성 여부 + 이상치 이름을 찍어 알린다 |
| 소리 파이프라인 | `npm run audio` | 배경곡 6곡 + **아직 연결 안 된 것**(effects · 고유기술 음성)의 개수 |

밸런스 하네스와 스모크 테스트가 **각각 엔진 버그를 하나씩 잡았다.** 통계·연동 층은 단위 테스트를
보완하는 게 아니라 **다른 종류의 버그**를 잡는다.

### 밟은 지뢰 ★ 다시 밟지 말 것

전체 경위는 [`history/`](history/)의 각 세션 기록 §3에 있다 —
[룰 엔진·고유기술·전투 씬 1차](history/2026-08-02_룰엔진_고유기술_전투씬.md) ·
[전투 씬 2차·시전 UI](history/2026-08-04_전투씬_2차_UI.md) ·
[전투 씬 3차·오프라인 풀 사이클](history/2026-08-04_오프라인_풀사이클.md) ·
[액션 시트·전투 연출](history/2026-08-12_액션시트_5자세_연출.md) ·
[전투 씬 4차·카드·카메라](history/2026-08-12_전투화면_카드_카메라.md) ·
[시각 효과·연출 순서·화면 정리](history/2026-08-14_시각효과_연출순서_화면정리.md) ·
[지형·화면 리디자인·배경음악](history/2026-08-14_지형_화면리디자인_배경음악.md) ·
[간판·도시·자리 화면](history/2026-08-15_간판_도시_자리_화면.md) ·
[배경의 미세한 움직임](history/2026-08-16_배경_미세이동.md) ·
[장수 일람·상세](history/2026-08-17_장수일람_상세.md) ·
[부대 전투력 산출식](history/2026-08-17_부대전투력.md) ·
[레벨/스킬 관리·저장 형식 v2](history/2026-08-17_레벨스킬관리_저장형식v2.md) ·
[전적·보상 개편 v3](history/2026-08-18_전적_보상개편_v3.md) ·
[도시 관리·군량 충전](history/2026-08-18_도시관리_군량충전.md).

- **스케줄러가 멈출 수 있다 — 가장 위험했던 것.** `advanceTime`이 한 번만 진행시키면,
  WT 0에 닿으려던 유닛이 그 구간에서 죽었을 때(도트·지형·곽가 「유언계책」) 제어권을 줄 유닛이
  없어 전투가 그대로 정지한다. **오래 안 보인 이유는 `autoBattle`이 비정상 종료를 조용히
  「무승부」로 보고했기 때문**이다 — 5000판 중 3판이 무승부 통계에 묻혀 있었다.
  지금은 누군가 제어권을 얻을 때까지 반복하고 `autoBattle`은 예외를 던진다.
  → **교훈: 조용한 실패가 지표에 섞이는 게 이 단계에서 제일 위험하다.**
- **UI에 「턴 종료」가 없으면 게임이 멈춘다.** 이동만 하고 공격 대상이 없고 MP도 가득이면
  유효한 의도가 `endTurn` 하나뿐이다. 1차 구현에서 실제로 교착이 났다 —
  제어 모달을 2차로 미룬 탓이었다. `tools/smoke_ui.ts`가 이걸 회귀로 막는다.
- **`BattleState.log`를 deep clone하면 안 된다.** `apply`/`advanceTime`은 사본을 만드는데,
  로그까지 `structuredClone`에 태우면 호출 비용이 로그 길이에 비례해 전투가 **O(턴²)**이 된다.
  실측으로 자동 대전 3판에 152초 → 로그만 얕게 복사하도록 고쳐 **1.7초**(약 90배).
  `battle.ts`의 `cloneState()`와 이를 고정하는 테스트(「로그는 얕게 복사한다」)를 지우지 말 것.
- 이벤트는 만들고 나면 바뀌지 않는 값이라 참조를 나눠 가져도 안전하다. 이 전제가 깨지면 위 최적화도 깨진다.
- **화면에 칠했으면 누를 수 있어야 한다.** 「화면에 칠해진 칸 = 엔진이 통과시킨 칸」이라는
  계약을 화면 쪽에서 두 번 깼다 — 200% 줌으로 후보가 **화면 밖**에 있었고, 플로팅 패널이
  후보 칸의 클릭을 **삼켰다**. 엔진이 통과시켜도 손이 닿지 않으면 없는 것과 같다.
- **연출은 유닛마다 시계를 따로 두면 안 된다.** 이동 뒤 공격이면 때리는 쪽만 밀리고
  맞는 쪽은 안 밀려서, 상대가 아직 걸어오는 동안 대상이 먼저 아파했다. `poses.ts`는
  이벤트를 훑으며 **공용 커서**를 밀고 모든 구간을 그 위에 놓는다.
  → **교훈: 연출은 스크린샷 한 장으로 검증할 수 없다.** 프레임 번호를 20ms 간격으로
  시계열로 찍어야 잡힌다. 이 버그도 그렇게 나왔다.
- **그림 도구의 임계값은 「정상값이 어디까지 올라가는지」를 재고 정한다.** 액션 띠의
  가장자리 띠를 「폭의 50% 이상 찬 줄」로 잡았다가 유엽의 5명이 하나로 붙었다 —
  발치에서는 캐릭터 5명만으로도 폭의 65%가 찬다. 진짜 띠는 100%다(`BAND_FILL = 0.9`).
- **파라미터를 고쳤으면 그 자리에서 전체를 다시 굽는다.** 갇힌 마젠타를 고친 뒤 7장만
  다시 굽고 넘어갔다가, 253장이 옛 규칙으로 남은 채 세션이 끊겼다. 폴더 안에 서로 다른
  규칙으로 만든 파일이 섞이면 나중에 눈으로 못 찾는다.
- **CSS는 틀려도 오류가 없다 — 화면을 봐야 안다.** 세 가지 형태로 반복해 밟았다.
  ① **이름이 겹친다** — `.panel`(전역) · `.ofc-head`(접두사를 붙였는데도 같은 파일 안에서).
  ② **기존 규칙이 선택자가 더 많아 이긴다** — `.scr .foot`이 `.ofc-tally`를 눌러 요약 줄이
  세 줄로 쌓였다. ③ **부모의 일괄 규칙이 자식의 의도를 덮는다** — `.scr-bg > *`의
  `position: relative`가 팝업의 `absolute`를 덮어 팝업이 화면 한가운데가 아니라 **내용 맨
  아래**에 붙었다(환경설정 팝업이 2026-08-15부터 그랬는데 스모크가 「떠 있는가」만 봐서 못 잡았다).
  → **교훈: 「있는가」와 「제자리에 있는가」는 다른 검사다.** 스모크는 좌표를 재야 한다.
  ④ **뜻이 같아도 이름이 겹치면 그냥 겹친다** — 도시 관리 화면(41쪽)에 `.city-info`를
  붙였으면 메인 화면의 `.city`가 그대로 얹혔다. 접두사를 `.cty-`로 갈라 피했다.
  ⑤ **같은 클래스라도 어느 틀 안에 있느냐로 뜻이 달라진다** — 부대 편성(42쪽)의 저장
  단추를 `.foot`으로 뒀더니 안내문이 **세로 한 줄로 짜부라졌다.** `.scr .foot`의
  `margin-top: auto`와 배경 화면 `.place-body`의 것이 서로를 밀어낸다. `.scr-roster`가
  `.foot`을 쓰고 있어 따라 했는데 **그쪽은 `ScreenChrome`을 안 두르는 화면**이었다.
  배경 화면의 단추는 41쪽의 `.cty-acts`처럼 **판때기 안에** 둔다.
- **시간으로 차는 값에는 「자투리」와 「상한」과 「시계가 뒤로」 셋이 함께 온다 ★**
  군량 충전에서 셋 다 조용히 틀릴 수 있었다. 정산 시각을 「지금」으로 찍으면 **30분씩
  들르는 사람이 영원히 한 톨도 못 받고**, 상한에서 시간을 쌓아 두면 **참가비를 내는
  순간 몇 시간이 한꺼번에 돌아오며**, 뒤로 간 시계에 맞춰 정산 시각을 당기면 **앞뒤로
  흔들어 뽑을 수 있다.** 셋 다 화면에는 「군량이 좀 많네」로만 보인다 —
  `city.test.ts`가 각각을 회귀로 박아 뒀다.
- **통계는 「그럴듯한 숫자」를 조용히 내놓는다 — 표준오차를 봐야 안다 ★** 전투력 회귀에서
  두 번 밟았다. ① **기물 지시변수 다섯이 완전 공선**이었다 — 각 진영이 King + (n−1)개를
  가져 **합이 상수**라 차분의 합이 0이 된다. 계수는 그럴듯하게 찍혔지만 **표준오차가
  6435**였다(기준 기물 하나를 빼야 한다). ② **표본에 없는 값으로 외삽했다** — 고유기술의
  SP 코스트가 표본에는 4·5·6뿐인데 헌제만 7이라, 기울기를 외삽해 **실측 최약체인 헌제가
  D급보다 세게** 나왔다. 등급 더미로 바꿔 없앴다.
  → **교훈: 회귀는 관측 범위 밖에서 틀리고, 공선은 값이 아니라 오차에서 드러난다.**
  `npm run power`가 표준오차와 z를 항상 함께 찍는 이유다.
- **「상태를 유지한 채 일부만 되돌린다」는 요구는 기억해야 할 것을 만든다 ★**
  둔갑천서의 원안은 「레벨은 유지하고 선택만 전부 새로」였다. 구현은 됐지만
  **재설계 전용 절차가 한 벌 더** 생겼다 — Lv2부터 순서대로 다시 고르는 마법사,
  걸음 되돌리기, 「고르는 도중」이라는 어디에도 없던 상태. 기획자가
  **「쓴 카드를 전부 돌려주고 Lv1로」**로 바꾸자 그게 전부 사라지고 절차가 레벨업
  하나로 모였다.
  → **교훈: 절차가 둘로 갈라지려 하면, 되돌리는 범위를 넓혀 하나로 만들 수 있는지
  먼저 본다.** 「일부만 되감기」가 「전부 되감기」보다 언제나 비싸다.
  (되돌려주는 양이 **정확히 쓴 만큼**이라는 것이 이 단순화의 값을 치르는 자리다 —
  회귀가 「다시 올리면 카드가 딱 떨어진다」를 고정한다.)
- **되접기·마이그레이션은 화면에 안 뜬다 — 규칙 층에 둬야 테스트가 잡는다.**
  「Lv7의 책략 아홉 개가 레벨별로 옳게 갈렸는가」는 어느 화면에도 나오지 않는다.
  `client`의 저장 층에 두면 브라우저를 띄워야만 확인되므로 `@samchess/meta`에 뒀다
  (정렬을 `officers.ts`로, 시간대를 `backdrop.ts`로 뗀 것과 같은 이유).
  다만 **단위 테스트는 「`loadProfile`이 그 함수를 실제로 부르는가」를 모른다** —
  옛 「버전이 다르면 버린다」 한 줄이 되살아나도 조용히 통과한다. 그래서 스모크가
  **v1 저장분을 실제로 넣고 새로고침**한다. 되돌려 보고 잡히는 것까지 확인했다.
- **조용히 무시하는 경로는 테스트에서 「없는 것」처럼 보인다 ★** 스모크에 「항복으로
  전투를 끊는다」를 더했는데 20초를 기다려도 결과 화면이 안 떴다. 콘솔 오류도 없다 —
  `Playback.submit()`이 **`awaitingInput`이 아닌 의도를 조용히 버린다**(AI가 두는 중,
  연출이 도는 중). 클릭은 성공했고 예외도 없으니 「결과 화면이 안 뜬다」로만 보였다.
  스모크는 내 차례를 기다렸다 누르게 고쳤지만, **화면의 [항복]은 그때도 눌리고 아무 일이
  없다** — 「눌리는데 아무 일도 없으면 「고장인가」가 남는다」에 걸리는 자리라 GDD §12
  미해결에 남겼다.
- **검사가 「기본값과 같은 값」을 확인하면 아무것도 확인하지 않는 것이다 ★**
  스모크에 「저장한 배치 프리셋이 판에 깔리는가」를 넣고 통과를 봤는데, 저장한 것이
  **기본 배치 그대로**였다 — 프리셋이 안 깔려도 엔진이 같은 자리에 세우므로 **무슨
  일이 있어도 통과**한다. 배치 편집기에서 한 칸 옮겨 두고서야 검사가 살았고, 그
  상태에서 적용 코드를 잠깐 지워 실제로 잡히는 것을 확인했다.
  → **교훈: 검사를 넣었으면 그 검사가 실패하는 상태를 한 번 만들어 본다.**
  「방어를 넣었으면 발동하는 입력을 만들어 본다」(B)의 검사 쪽 짝이다.
- **「아직 안 붙은 갈래」에 걸린 검사는 도는 적이 없다 ★** 「다시 찾기」는 온라인에만
  있는데(§5-15) 온라인은 서버가 없어 언제나 빈손이라, 화면이 **늘 AI로 떨어져** 45쪽
  완료 조건의 두 줄이 **한 번도 실행되지 않았다.** 위의 「기본값과 같은 값을 본다」와
  같은 함정이지만, 이번 것은 **검사할 상태 자체에 도달할 수 없는** 형태였다.
  → **교훈: 갈래를 흉내 낼 통로를 검사와 함께 만든다** (`?match=online`).
- **가운데만 재는 회귀는 끝이 어긋난 것을 놓친다 ★** AI 상대를 전투력으로 뽑을 때
  탐욕 배치가 **앞 슬롯의 오차를 마지막 슬롯에 몰아 준다** — 목표 여섯 점만 보는 회귀는
  통과했는데 전 구간을 훑자 바닥 근처에서 **31이 벗어났다**(구간은 ±20). 보정 한 바퀴를
  붙여 최대 오차 11/20이 됐다. D의 「회귀는 관측 범위 **밖**에서 틀린다」와 짝이고,
  이번 것은 **범위 안의 끝**이었다.
  (촘촘히 훑자 `npm test`가 0.8초 → 14초가 됐다 — 후보 표를 부를 때마다 다시 만들고
  2340개를 매번 정렬하고 있었다. 표는 모드마다 한 번, 정렬 대신 **top-k 훑기**로 0.42초.
  **회귀를 촘촘히 하는 것과 규칙을 빠르게 하는 것이 같은 작업이 될 때가 있다.**)
- **규칙 층과 화면 층은 따로 깨 봐야 둘 다 산 것을 안다 ★** 45쪽은 판정을 meta에,
  그리기를 화면에 갈라 뒀다. `canDeclineMatch`만 무력화하면 화면 검사가 살아 있는지
  알 수 없고, `MatchScreen`의 조건만 무력화하면 규칙 검사가 살아 있는지 알 수 없다 —
  **둘을 각각 깨서** 스모크가 서로 다른 줄로 잡는 것을 확인했다.
- **「도는 적이 없는 검사」를 교훈을 알면서 새로 만들었다 ★** H1에서 `pump()`에
  「연출 중에는 큐를 안 꺼낸다」 잠금을 두고 회귀도 넣고 통과를 봤는데, **그 줄을 지우고
  돌려 보니 회귀가 전부 통과했다.** `LocalTransport`는 **청해야 답하므로** 큐가 늘
  비어 있어 잠금이 시험되지 않는다 — 내가 검사한 것은 잠금이 아니라 `update()`의 조기
  반환이었다. F의 §5-52(「온라인이 늘 빈손이라 거절 검사가 실행조차 안 됐다」)와 정확히
  같은 함정을 **그 교훈을 적어 둔 채로 다시 밟았다.** 고친 방법도 같다 —
  **묻지도 않고 한 판을 통째로 밀어 넣는** 가짜 판정 주체(`ScriptedTransport`)를 회귀
  안에 함께 만들었다. 그게 곧 진짜 서버의 모양이다.
  → **교훈: 흉내 통로가 반드시 개발용 URL일 필요는 없다.** 회귀 안의 가짜 구현이면
  화면을 안 더럽히고 낡지도 않는다.
- **상태 기계를 옮겨 적을 때는 「무엇을」만이 아니라 「언제」도 옮겨야 한다 ★**
  새 `consume()`이 통을 받자마자 걸음을 정해 버려, **때리는 연출이 도는 2.6초 동안
  단계가 벌써 `advancing`**이었다 — 화면은 아무것도 안 흐르는데 「시간이 흐르는 중」이라고
  말한다. 옛 `stepOrHold()`는 「연출이 끝난 뒤에 진행한다」였는데, 옮기며 **진행만 미루고
  「걸음 정하기」는 안 미뤘다.** `npm test`는 전부 통과했고 `smoke:ui`만 잡았다
  (`WT 게이지가 시간이 흘러도 줄지 않는다 (151.0 → 151.0)`). 고친 뒤 그 검사를
  단위 회귀로 내렸다 — **`phase`는 화면이 읽는 값이라 거짓말을 하면 안 된다.**
- **타입 스트리핑은 「타입만 지우면 되는」 문법만 받는다 ★** 회귀의 가짜 구현을
  `constructor(readonly x: T)`로 적었다가 `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX`를 봤다 —
  파라미터 프로퍼티는 대입문을 **만들어 내야** 해서다. **데코레이터 · `enum` ·
  `namespace`도 같은 부류**이고, 그래서 `@colyseus/schema`(`@type` 데코레이터)를 안 쓰기로
  한 것은 설계 취향이 아니라 **이 저장소의 물리적 제약**이다.
- **검사가 안 잡는 것은 주장하지 말고 적어 둔다 ★** `pump()`의 재진입 잠금을 지워도
  회귀가 전부 통과한다 — 잠금이 없으면 재귀로 파고들 뿐 **꺼내는 차례는 같기** 때문이다.
  잠금이 막는 것은 순서가 아니라 **깊이**(스택이 턴 수만큼 쌓인다)다. 주석이
  「재진입 잠금이 이 함수의 요점이다」라고 과장하고 있어 사실대로 고쳤다.
  → **교훈: 도는 적 없는 검사를 하나 더 만드는 것보다, 무엇이 검사로 안 잡히는지를
  적어 두는 편이 낫다.**
- **단추의 이름을 줄바꿈하면 이름이 쪼개진다.** `.btn .lbl`에 `nowrap`이 없어
  「튜토리얼 시나리 / 오」가 됐다 — **접히는 쪽은 언제나 사연(`.sub`)이어야 한다.**
  700px 눈검사에서만 보였다. 같은 자리에서 안내문 둘이 화면 맨 아래에 나란히 붙어
  **어느 단추 이야기인지 알 수 없는** 것도 잡았다(제 단추 밑으로 옮겼다).
- **초기 상태를 바꾸는 기능은 그 상태에 기대던 검사를 전부 흔든다.** 배치 프리셋이
  기물을 구역 맨 윗줄로 옮기자 옛 스모크의 「배치 이동」이 깨졌다 — 옮길 자리를
  `y − 2`로 잡고 있었고 그게 **구역 밖**이라 엔진이 거부했다. 옛 검사는 「기물이 늘
  구역 한가운데에서 시작한다」를 **말없이** 전제하고 있었다.
- **되돌릴 수 있는 것은 조용히, 되돌릴 수 없는 것은 말한다.** 부대의 장수 레벨이
  보유 레벨보다 크면(재설계 뒤) **조용히 눌러 담고**, 장수가 계정에서 빠졌으면
  **목록이 말한다.** 앞은 약해지는 방향이라 안전하고 뒤는 메울 방법이 없다.
- **규칙이 아니라 표본이 틀릴 수 있다.** 「승리 군량 +1」 회귀가 열두 조합 중 넷에서
  깨져 규칙을 세 번 다시 읽었는데, **새 계정은 군량이 상한에 붙어 시작**한다(20/20).
  받을 자리가 없으니 0이 맞았다. 고친 것은 코드가 아니라 테스트의 시작 상태다.
- **읽는 자리를 하나로 모으지 않으면 「하향했는데 책략은 그대로」가 난다.**
  성장 스택을 두 군데서 직접 펴면 한쪽이 `cap`을 잊는다. 화면에는 아무 표시도 안 나고
  **전투에 들어가야** 드러나는 종류라, `statPicksOf`/`tacticsOf` 둘만 부르게 막았다.
- **문서만 낡는 어긋남은 실행해도 안 잡힌다.** GDD §4.3의 레벨업 카드 표가 엑셀과 달랐고
  (누적 116 vs **100**), 화면의 `AT +1`이 확정치 `+0.5`와 달랐다. 코드는 둘 다 데이터를
  읽고 있어서 **동작은 멀쩡했다.** 초상화·연출 이미지는 추출기가 대조해 빌드를 막지만
  **GDD 본문과 손으로 적은 글자에는 그 그물이 없다.**
- 그 밖에 한 번씩 밟은 것 — `consumeCharge`가 `charges` 없는 지속형까지 지움 /
  `legalTargetsFor`가 「유인」(이동만) 상태를 무시해 UI가 못 할 공격을 켬 /
  「초선」이 「삼고초려」의 영구 조종을 덮어씀 / Phaser 카메라 `setBounds`가 축소 시 중앙 정렬을 깨뜨림 /
  제어권 획득은 상태 변경이 아니라 **시간 경과**라 하이라이트를 다시 그릴 계기가 없었음.

---

## 7. 다음 할 일

> **지금 무엇을 할 차례인가는 §2 표와 이 절의 로드맵이 정한다.**
> [`docs/작업계획.md`](docs/작업계획.md)에는 **왜 그렇게 정했는가**(§5 결정 로그)와
> 끝난 트랙(PPT 37~45)의 이력이 있다.
>
> **PPT 37~45 트랙이 끝났고** (`A → D → B → C1 → C2 → E → F`, 2026-08-18),
> 그 뒤의 **Colyseus 트랙이 셋으로 갈렸다** — **H1** 전송 층(2026-08-19 ✅) ·
> **H2** 서버와 방 · **H3** 계정·로그인과 계정 권위.
> F가 「서버가 검증할 것(부대·전투력·매칭 기준·거절 군량)을 오프라인에서 먼저 굳힌다」로
> 자리를 남겨 뒀고, **H1이 같은 이유로 한 겹 더 굳혔다** — 전선 계약이 회귀로 서 있어
> 서버를 붙인 뒤 어긋나도 「전송인가 방인가 시계인가」를 가릴 필요가 없다.

```
1. CTB 스케줄러             ✅ advanceTime() — WT 최소값까지 진행, SP 충전, DoT/지속시간 만료 정산
2. 행동 판정                ✅ 이동/공격/명상/턴종료 + 시드 PRNG (rngCursor로 재현성 확보)
3. Effect DSL              ✅ 책략 18종 + A/B/E급 10종
4. S급 30종                 ✅ 28종은 DSL, 스크립트는 소패왕전·차동풍 둘뿐
5. 밸런스 하네스             ✅ 기준 AI + 자동 대전 5000판 통계, 무승부 규칙 time 6000
─────────────────────────── 여기까지가 밸런스 검증 ───────────────────────────
6. Phaser 전투 씬 + AI 대전  ✅ 1차(보드·타일·입력) + 2차(바·HUD·모달·시전·배지) + 3차(디자인 가이드)
7. 오프라인 풀 사이클        ✅ 계정·편성·배치·정찰·전투·결과·성장
7-1. 액션 시트 · 전투 연출   ✅ 260명 5자세 + 이동/공격/책략/퇴각 타임라인 (2026-08-12)
7-2. 전투 씬 4차 (PPT 27~29) ✅ 카드 스트립 · 플로팅 커맨드 패널 · 카메라 100%/200% (2026-08-12)
7-3. 시각 효과 30종          ✅ 지속형 링 23 (2초 스왑) · 일회성 애니 7 (2026-08-13)
7-4. 지형 그림 3종           ✅ 화계·수계·성지를 칸에 (2026-08-14)
7-5. 전투 화면 리디자인       ✅ 판 지도 · 카드 벽보 액자 · 산수 배경 (2026-08-14)
7-6. 배경음악                ✅ 화면마다 한 곡 · 첫 조작에 풀린다 (2026-08-14)
7-7. 간판·도시·자리 (PPT 33~36) ✅ 시간대 배경 · 궁궐/병영/장터 · 환경설정 · 다국어 틀 (2026-08-15)
7-8. 배경의 미세한 움직임      ✅ 2.6초마다 한 자세 (2026-08-16)
7-9. PPT 37~45 (궁궐·병영)   ✅ **끝.** 아홉 장 전부 → docs/작업계획.md §2
     ├ A. 장수 일람·상세      ✅ 표·검색·정렬·기술 팝업 (2026-08-17)
     ├ D. 부대 전투력 산출식   ✅ 모드당 2만 판 회귀 · 보정 검사 (2026-08-17)
     ├ B. 레벨/스킬 관리·재설계 ✅ 저장 형식 v2 · 되접기 · 증분 미리보기 (2026-08-17)
     ├ C1. 전적·보상 개편      ✅ 저장 형식 v3 · 대전 이력 (2026-08-18)
     ├ C2. 도시 관리          ✅ 군량 시간 충전 · 증축 (2026-08-18)
     ├ E. 부대 편성·배치 프리셋 ✅ 필드 둘 · 레벨 하향 · 프리셋 (2026-08-18)
     └ F. 출전·매칭 + 문 통합  ✅ 저장 형식 그대로 · AI를 전투력으로 · 거절 군량 (2026-08-18)
8. Colyseus 온라인 대전      ⬜ ← **지금 차례.** F가 남긴 searchOnline() 하나가 입구다
9. 메타 나머지 (상점/가챠/랭킹/결제) ⬜
G. 별도 트랙                 ⬜ 인물 서사 · 시설(병원·황궁) · 튜토리얼 시나리오
```

> **순서를 바꿨다 (2026-08-04, 기획자 판단).** 원래는 7이 Colyseus였는데,
> **오프라인 한 바퀴를 먼저 완성하고 그 뒤에 온라인으로 잇기로** 했다.
> 근거: 넷코드가 감싸야 할 단계(배치·대기·정찰)의 화면이 아직 없어서, 그 상태로 서버를
> 만들면 나중에 편성·배치 화면을 붙이며 프로토콜을 다시 손대게 된다. 편성 규칙도
> 서버가 반드시 검증해야 하는 대상이라 오프라인에서 굳혀 두는 편이 낫다.

**계정에서 전투까지 한 바퀴가 온전히 돈다** — GDD §3.9의 흐름이 화면에 다 있다.
남은 것은 넷코드다. 서버는 **타이머와 전송만** 맡으면 된다.

### 밸런스 5000판 실측 (기준 AI, 3:3, 전원 Lv1, 책략 없음)

```
선공 49.8%  후공 50.2%  판정승 0%  무승부 0%
결착 중앙값 time 1259(13일)  최대 2681(27일)   ← 상한 6000까지 2.2배 여유

등급별  S 66.5% / A 59.4% / B 47.3% / C 46.6% / D 40.7%   (E 45.8%, 표본 120회)
기물별  King 50.0 / Rock 51.2 / Bishop 47.6 / Knight 50.5 / Queen 52.4 / Pawn 48.2
```

기물은 47~52%로 고르다 — GDD §11에서 우려했던 Queen·Knight는 문제없이 나왔다.
편성은 시드에서 결정적으로 뽑으므로 같은 판수·같은 옵션이면 결과가 완전히 재현된다.

**A급↔B급 12%p는 의도된 것이다** (2026-08-12 기획자 확정). 과금 설계상 A급이 확실히
세야 한다. 아래의 성장 실측은 이 격차가 **레벨을 올려도 유지되는지**를 본 것이다.

### 성장을 넣고 다시 재기 (2026-08-12)

`npm run balance -- 5000 --level 9 --stat mix` — 레벨업 성장을 넣어 봤다.
**기본값은 Lv1 그대로 둔다.** 위 실측치와 비교가 끊기기 때문이다.

| 성장 정책 | 결착 중앙값 | S | A | B | C | D | **A↔B** | S↔D |
|---|---|---|---|---|---|---|---|---|
| Lv1 (기준) | 13일 | 66.4 | 59.4 | 47.3 | 46.6 | 40.7 | **12.1** | 25.7 |
| Lv9 `mix` | 14일 | 65.8 | 60.4 | 47.4 | 47.0 | 39.4 | **13.0** | 26.4 |
| Lv9 `hp` | 26일 | 66.5 | 59.2 | 47.7 | 46.7 | 40.2 | **11.5** | 26.3 |
| Lv9 `mp` | 13일 | 66.4 | 59.4 | 47.3 | 46.6 | 40.7 | **12.1** | 25.7 |
| Lv9 `at` | **9일** | 56.2 | 53.2 | 48.6 | 49.2 | 46.2 | **4.6** | **10.0** |

읽어야 할 것 셋.

- **등급 격차는 성장으로 희석되지 않는다.** 레벨이 올려 주는 것은 HP/MP/AT뿐이고
  **모두에게 똑같이** 올라간다. 등급 우위가 사는 곳은 **무력(크리티컬)·통솔(WT)·고유기술**이라
  레벨이 건드리지 않는다 → 고등급 카드의 값어치가 시간이 지나도 유지된다.
- **`at` 몰빵이 등급 격차를 반토막 냈다** (A↔B 12.1 → 4.6%p). AT 2 → 10이면 크리티컬
  ×2의 상대적 값어치가 줄고, 판이 9일로 짧아져 통솔 우위가 쌓일 시간도 없다.
  → **`AT` 성장을 1에서 0.5로 내렸다** (2026-08-12 확정, 아래).
- **`mp` 몰빵이 Lv1과 소수점까지 같다.** MP는 책략에만 쓰이는데 **기준 AI가 책략을 쓰지 않아서**다
  (아래). 도구가 지력을 못 재고 있다는 직접적인 증거이기도 하다.

### AI에 책략을 붙이고 다시 재기 (2026-08-12) ★

원래 `rules/ai.ts`에는 `castTactic`이 **하나도 없었다.** 지력이 쓰이는 곳은 환술 판정뿐이라
**지력이 승률에 전혀 반영되지 않았고**, 그게 특히 B급을 낮게 잡았다.

```
등급  인원  능력합   무력   지력   통솔   고유기술
S      30   237.2   78.2   75.3   83.7   30/30
A      40   216.1   72.5   66.4   77.2   40/40
B      55   180.7   57.6   64.4   58.7   55/55   ← 지력형
C      90   166.8   61.4   49.4   56.0    0/90   ← 무력이 B보다 높다
D      44   129.5   45.5   45.0   39.0    0/44
```

책략 시전을 붙여(§「기준 AI」 아래) Lv9 `mix`로 다시 쟀다.

| Lv9 `mix` | S | A | B | C | D | E | A↔B | **B↔C** |
|---|---|---|---|---|---|---|---|---|
| AI가 책략을 **안 씀** | 65.8 | 60.4 | 47.4 | 47.0 | 39.4 | 44.2 | 13.0 | **0.4** |
| AI가 책략을 **씀** | 67.0 | 58.9 | 48.4 | 46.7 | 39.2 | 35.8 | 10.5 | **1.7** |

- **B가 처음으로 C 위로 올라왔다** (0.4 → 1.7%p). 지력이 승률에 반영되기 시작했다는 뜻이다.
- **그래도 1.7%p뿐이다.** B는 C보다 지력이 15 높고 **고유기술까지 있는데** 그 정도다.
  → **B↔C가 붙어 있는 것은 측정 탓이 아니라 설계다.** 지력의 값어치가 무력에 비해 낮다.
- **E급(헌제)이 44.2 → 35.8로 폭락했다.** 능력치 1/1/1이라 환술을 전부 맞고 하나도 못 건다.
  책략이 도는 판에서는 「무적 990」만으로 버티지 못한다.
- A↔B가 13.0 → 10.5%p로 좁아졌다 — 책략이 B를 끌어올린 만큼이다.

> **Lv1 기준선은 그대로다** (S 66.4 / A 59.4 / B 47.3 / C 46.6 / D 40.7). Lv1은 책략이
> 하나도 없어서 AI가 바뀌어도 행동이 같다 — 5000판 실측치와의 비교가 끊기지 않는다.

### 부대 전투력 산출식 (2026-08-17) — 트랙의 D

pptx 44쪽 「전투 통계 기반으로 산출식을 도출하는 게 좋겠음」. 사양은 **GDD §7.1**,
경위는 [`history/2026-08-17_부대전투력.md`](history/2026-08-17_부대전투력.md)에 있다.

```
npm run power -- 20000 --calib 5000        모드당 2만 판 · 총 488초
```

**전투력은 점수가 아니라 승률로 환산되는 눈금이다.**

```
power(팀) = Σ 유닛별 기여          winChance = σ((격차)/100)      MATCH_BAND = ±20 (45~55%)
```

| 특징 | 3v3 | 5v5 | | 특징 | 3v3 | 5v5 |
|---|---:|---:|---|---|---:|---:|
| 무력 | 1.47 | 0.889 | | S급 고유기술 | 34.125 | 13.539 |
| 지력 | 0.315 | 0.283 | | A급 고유기술 | 16.52 | 4.173 |
| 통솔 | 0.756 | 0.382 | | B급 고유기술 | 5.669 | **0** |
| HP | 6.344 | 3.444 | | MP | 0.779 | 1.122 |
| AT | 38.3 | 22.042 | | 유닛 바닥값 | −126.476 | −65.688 |

**모형이 맞는지 확인한 방법** — 적합에 쓰지 않은 **새 시드 5천 판**으로 보정 검사.
예측 승률 11개 구간이 전부 실제와 **±3%p 이내**였다. 여기에 Δ등급점수 × Δ평균레벨 표가
두 방향 모두 어긋난 칸 없이 단조다(3v3, 왼쪽 위 0.0% → 오른쪽 아래 97.1%).

```
             Δ레벨 ≤−3   −3~−1   −1~1    1~3    ≥3
  Δ등급 ≤−12      0.0%    3.7%   12.4%  23.6%  47.5%
  Δ등급  −2~2    12.1%   26.1%   45.2%  64.5%  79.8%
  Δ등급  ≥12     51.9%   70.1%   81.3%  91.7%  97.1%
```

**읽어야 할 것 넷.**

- **모드마다 계수가 다르다** — 3v3가 5v5의 **1.65~1.75배**다(차이의 z 4.6~17.7).
  `5/3 ≈ 1.67` — 한 명이 셋 중 하나일 때가 다섯 중 하나일 때보다 판을 더 크게 좌우한다.
  44쪽의 「3 vs 3, 5 vs 5 각각 필요」가 실측으로 맞았다.
- **그래서 두 모드의 값 범위가 겹친다** (3v3 209~1304 · 5v5 253~1302). 계수가 작아진 만큼
  인원이 늘어 상쇄된다. **나란히 놓으면 비교하고 싶어지는데 비교하면 안 되는 값**이라
  `battlePower()`가 인원 불일치에 예외를 던진다.
- **지력이 낮다** (무력 1.47 대 0.315). 기준 AI가 책략을 「칠 수 없는 턴에만」 쓰는 한계가
  그대로 나온 것이다 — 임의로 올리면 실측 기반이라는 근거가 사라지므로 그대로 두고
  테스트로 고정했다. 다시 재는 것은 **의도된 변경**이어야 한다.
- **기물은 일부러 뺐다** (기획자 확정). 회귀에서는 유의했다 — 아래.

### 기물 효과를 따로 재 봤다 (전투력에는 넣지 않는다)

Pawn을 기준으로 놓고 능력치를 통제한 뒤 남은 기물 효과다. **GDD §11의 우려 둘을 뒤집었다.**

```
3v3   Queen +20.8 · Knight +18.0 · Rock +16.4 · Pawn 0 · Bishop −11.3
5v5   Queen +11.4 · Knight  +9.5 · Rock  +8.7 · Pawn 0 · Bishop  +4.1
```

- **§11-3의 Queen 우려는 기우였다** — 공격이 상하 1칸뿐인데 가장 높다.
- **§11-4의 Knight 우려도 기우였다** — 위협 범위 최하(25)인데 상위권이다. 경로 무시가 값을 한다.
- **Bishop만 음수다.** 이동은 대각, 공격은 직교라 **방향이 어긋난다** — 다가간 자리에서
  못 때리는 일이 잦다. 새 관찰 대상이라 GDD §11-9에 적었다.
- 전투력이 같아도 기물 조합만으로 **3v3 승률 8.0%p**가 갈린다. 구간 폭(±20 = ±5%p)과
  같은 크기라 **매칭이 기물 때문에 한 구간만큼 어긋날 수 있다**는 뜻이다.

> **「기물별 승률 47~52%」(아래 5000판 실측)와 다른 것을 잰 값이다.** 그쪽은 편성 전체의
> 승률이라 장수 능력치에 묻히고, 이쪽은 **능력치를 통제한 뒤 남은 몫**이다.

### `AT` 성장을 0.5로 내렸다 (2026-08-12 확정)

`AT+1`이던 시절 **몰빵 하나가 등급 격차를 지웠다.** 등급으로 값을 매기는 설계에서
성장 선택 하나가 그 근거를 무너뜨리는 셈이라 0.5로 내렸다 (GDD §4.2 · §12).

| AT 성장 | 결착 | `at` 몰빵 A↔B | 균형(`mix`) A↔B |
|---|---|---|---|
| 1.0 | 9일 | **3.0%p** | 10.5%p |
| **0.5** | 10일 | **6.3%p** | **10.5%p** |

**균형 성장은 소수점까지 그대로이고 몰빵만 눌린다** — 정상적으로 크는 사람은 영향이 없다.

> **데미지는 매 타격마다 따로 내림한다**(`floor(AT × 배수)`). 그래서 `AT 2.5`는
> 평타 2 · 크리티컬 5이고, **홀수 번째 AT 선택은 크리티컬일 때만 값을 한다.**
> 화면이 감추면 「찍었는데 왜 그대로지」가 되므로 상태 팝업의 AT를 **`2-4` 범위 표기**로
> 바꿨다 (pptx 28쪽 원안이기도 하다). 계산은 엔진의 `attackRange()`가 한다.

> **AT 스텝의 단일 출처는 `tools/extract_data.py`의 `statChoices`다.**
> `rules/types.ts`가 그 값을 **리터럴 타입**으로 못 박고 있어서, 한쪽만 고치면
> 타입체크가 먼저 막는다 — 데이터와 타입이 갈리지 않는다.

### 전투 씬 2차 — 완료 (2026-08-03~04)

다섯 항목을 전부 붙였다. **왜 그렇게 만들었고 어디서 넘어졌는지는**
[`history/2026-08-04_전투씬_2차_UI.md`](history/2026-08-04_전투씬_2차_UI.md)에 있다.

| | 붙은 것 | 파일 |
|---|---|---|
| 1 | 타일 배지 4종 — 좌상 고유기술 / 우상 버프 점 / 좌하 `S9` / 우하 디버프 점 | `battle/BattleScene.ts` |
| 2 | 타일 상단 바 3줄 — HP · MP · WT(회색→흰색, 제어권 획득 시 금색) | `battle/BattleScene.ts` |
| 3 | 상단 HUD — 시계 · SP 칸 · 고유기술 보유 현황 | `ui/hud.ts` |
| 4 | 제어 모달 3상태 + 턴 3단계 | `ui/controlModal.ts` |
| 5 | 책략·고유기술 시전(목록 → 조준 → 시전) · 공격 범위 · 장수 정보 팝업 | `ui/controlModal.ts` · `ui/inspectPanel.ts` |

**턴 3단계가 화면 구조 그대로다** (GDD §3.4) — `[1] 고유기술?` → `[2] 이동` → `[3] 행동`.
고유기술은 조건이 되면 `[고유기술 발동] [보류]`로 **먼저 묻는다**(이동하면 못 쓰므로).
제자리를 누르면 이동 없이 행동 단계로 넘어가는데, 이건 **엔진에 보낼 의도가 없는 화면만의 상태**다 —
"이동 안 함"을 선언하는 `Intent`가 룰에 없다.

**이 세션에서 굳은 계약 셋** (넷코드로 넘어가도 그대로 간다)

- **대상이 필요한 행동도 활성 여부를 엔진에 묻는다.** 「이동」·「공격」·「책략」은 의도 하나로
  `validate()`를 물을 수 없어서, **엔진이 준 후보를 하나씩 넣어 물어보고 하나라도 통과하면 켠다.**
  조준 하이라이트도 같은 후보 목록이라 **화면에 칠해진 칸 = 엔진이 통과시킨 칸**이다.
  「백의도강」 무적이 조준 후보에서 자동으로 빠진 것이 이 설계가 값어치를 증명한 순간이었다.
- **조준 규약은 엔진이 알려 준다.** 그래서 `effects.ts`를 패키지 밖으로 내보냈다
  (`aimingSpec` / `tacticMpCost`). 클라이언트가 "무엇을 조준해야 하는가"를 다시 구현하지 않는다.
- **버프/디버프 구분의 단일 출처는 `types.ts`의 `STATUS_META`다.** `Record<StatusId, …>`라
  상태를 새로 추가하면 표가 비어 **컴파일이 깨진다.** 화면에 조용히 빠지는 일이 없다.

**하이라이트는 두 층이다** — 채우기는 유닛 **아래**(depth 5), 테두리는 유닛 **위**(depth 15).
공격 대상과 조준 후보는 정확히 유닛이 서 있는 칸이라, 채우기만 쓰면 초상화(depth 10)가 덮어
**화면에서는 아무것도 안 보인다.** 실제로 그렇게 났고 `smoke:ui`가 회귀로 막는다.

### 전투 씬 3차 — 디자인 가이드 반영 (2026-08-04)

기획자가 pptx 20~25쪽에 디자인 지침을 넣었고 그대로 붙였다. **룰 엔진은 안 건드렸다** —
바뀐 것은 화면 골격과, 엔진 이벤트를 사람 말로 푸는 층뿐이다. 사양은 GDD §3.10에 옮겨 적었다.

| | 붙은 것 | 파일 |
|---|---|---|
| 1 | **모바일 세로 `1:2` 프레임** — 판은 가로 폭에 맞춘 정사각, 가로·세로 가운데 | `index.html` · `style.css` |
| 2 | **시스템 대화창** — 최근 줄이 판에 가장 가깝게 가운데, 지난 줄은 왼쪽으로 밀려 올라감 | `ui/systemLog.ts` · `ui/eventText.ts` |
| 3 | **하단 패널 개편** — 수묵화 + 4줄(등급·이름·기물·레벨 / HP·MP·AT / 무력·지력·통솔 / 상태 배지) | `ui/controlModal.ts` |
| 3-1 | 수묵화는 커맨드 위 남은 높이를 채우는 정사각, 가로 상한은 판 폭의 절반 | `style.css` `.ctl-art` |
| 3-2 | **보드 좌표 눈금** — 첫 행 `A`~`Y` · 첫 열 `1`~`20`, 배율이 바뀌어도 같은 크기 | `battle/BattleScene.ts` |
| 4 | **고유기술 물음 → 연출** — 판 한가운데에서 `[아니오][예]`, 발동하면 배너 2초 | `ui/skillFx.ts` |
| 5 | **기물 정보 팝업을 판 영역 안으로** | `ui/inspectPanel.ts` |
| + | 상태 배지를 눌러 뜻 보기 · HUD 「항복」 | `ui/statusPopup.ts` · `ui/hud.ts` |

**이 세 가지가 이 작업에서 굳은 계약이다.**

- **크기는 전부 `rem`이고 기준값은 프레임 폭이 정한다** (`main.ts`의 `fitFrame`). 프레임이
  320px(작은 폰)에서 700px(세로 모니터)까지 변하는데 글자만 고정이면 좁은 화면에서
  커맨드가 줄바꿈되고 넓은 화면에서는 허전해진다. **px를 새로 적지 말 것.**
- **대화 문장은 엔진이 준 `BattleEvent[]`만 읽어서 만든다.** 화면이 상태를 뒤져 무슨 일이
  있었는지 추측하지 않는다 — 온라인 대전에서 서버가 보내 주는 것이 바로 이 배열이라,
  판정 주체가 서버로 바뀌어도 이 층은 그대로 쓰인다.
- **상태이상의 이름·분류·설명은 `types.ts`의 `STATUS_META`가 단일 출처다.** 이번에 `desc`를
  같은 표에 넣었다. 화면이 설명문을 따로 적어 두면 상태가 늘었을 때 조용히 어긋난다.

**밟은 지뢰 둘**

- **캔버스가 더 이상 화면 전체가 아니다.** 스모크의 격자→픽셀 변환이 캔버스 좌상단을
  더하지 않아 클릭이 엉뚱한 칸으로 갔다. `getBoundingClientRect()`를 더해야 한다.
- **연출 중에는 갱신이 멈추므로 물음창이 뒤에 남는다.** 고유기술을 쏘는 순간 물음을
  직접 걷어야 한다(`dismissPrompt`). "다음 갱신 때 사라지겠지"가 통하지 않는 구간이다.

### 오프라인 풀 사이클 (2026-08-04)

```
[새 계정] → [메인·도시] ─┬─ [장수 관리] → 레벨업 ─┐
                          └─ [편성] → [전투] → [결과] ┘
```

| | 붙은 것 | 자리 |
|---|---|---|
| A | 보유 장수 모델 + 저장 (초기 지급 S·A·B·C·D 각 1명) | `packages/meta` · `client/src/meta/storage.ts` |
| B | 기물 편성 — 기물 고르기 + 장수 매치 | ~~`screens/RosterScreen.tsx`~~ → **F가 지웠다**. 구성은 `SquadEditScreen`(42쪽) 하나 |
| D | 결과·보상 — AI는 군량만 | `screens/ResultScreen.tsx` · `meta/rewards.ts` |
| E | 장수 관리·레벨업 — 능력 택1 + 책략 택1 | `screens/OfficerScreen.tsx` |
| F | AI 상대 편성 — ~~내 팀 등급 점수에 맞춰~~ → **전투력에 맞춰** (F가 갈아 끼웠다) | ~~`meta/roster.ts` `makeAiPicks`~~ → `meta/match.ts` `makeAiOpponent` |
| C | 배치·정찰 화면 — 진영 내 자유 배치 30초, 정찰 30초 | `ui/prepPanel.ts` · `battle/playback.ts` |

**여기서 굳은 계약 셋**

- **계정 규칙은 `@samchess/meta`에 있다.** 화면이 "보유하지 않은 장수를 편성했다"를 스스로
  판단하지 않고 `validateRoster()`에 묻는다 — 전투 UI가 `validate()`에 묻는 것과 같은 이유다.
  서버가 붙어도 판정이 갈리지 않는다.
- **엔진이 최종 권위다.** `validateRoster`를 통과한 편성은 `createBattle`도 받아들여야 하고,
  그걸 테스트로 고정했다(「편성 검증을 통과하면 룰 엔진도 받아들인다」).
- **저장은 `client/src/meta/storage.ts` 한 곳뿐이다.** 온라인이 붙으면 **이 파일만** 서버 API가
  된다 — 화면은 그대로다. `playback.ts`를 둔 것과 같은 결이다.

**배치·정찰에서 굳은 것** (넷코드가 그대로 물려받는다)

- **엔진은 시계를 갖지 않는다.** 절대시간은 전투가 시작해야 흐르고, **배치 30초·정찰 30초**는
  실시간이라 재는 주체가 따로 있어야 한다. 지금은 `Playback.deadlineMs` 하나가 그 자리이고
  **온라인에서는 서버가 이 값을 내려 준다** — 읽는 쪽(패널의 남은 시간)은 그때도 그대로다.
  `BattleState.controlStartedAtMs`를 서버가 주입하기로 한 것과 같은 결이다.
- **`deploy` 의도는 전원의 자리를 한 번에 받는다.** 한 명만 옮겨도 나머지의 현재 위치까지
  함께 보내야 검증을 통과한다 — 부분 배치라는 개념이 룰에 없다.
- **`waiting` 단계는 화면에 나타나지 않는다.** AI는 곧바로 준비를 마치므로 「매칭 대기 최대
  1분」(GDD §3.9)이 지나갈 일이 없다. 온라인에서 상대의 `ready`를 기다리는 자리가 여기다.
- **배치 중에는 정보 팝업을 띄우지 않는다.** 판 한가운데를 덮어 놓을 자리가 안 보인다.
  장수를 살펴보는 것은 다음 단계인 「정찰」이 맡는다.
- **배치 중에도 상대 기물이 보인다** (2026-08-04 확정). 가릴 수도 있었지만 배치 구역은
  어차피 정해져 있어 숨겨서 얻는 정보 우위가 크지 않다. 「정찰」은 정보를 처음 여는
  단계가 아니라 **살펴볼 시간을 주는 단계**로 둔다 — 온라인에서도 같다.

**개발용 카드 지급** — 장수 관리 화면 아래에 있다. AI 대전이 카드를 주지 않기로 확정되어
오프라인만으로는 성장을 시험할 수 없어서 뚫은 문이다. **게임 규칙이 아니다** —
온라인·상점이 붙으면 지운다.

### 화면 확인용 통로

| | 용도 |
|---|---|
| `?sp=15` | 시작 SP를 채운다. 고유기술은 SP 4~7이 모여야 열려 4~7일을 기다려야 한다 |
| `?auto=1` | 양쪽 AI 관전 |
| `?terrain=1` | 판 한가운데에 화계·수계·성지를 하나씩 — **지형 그림 확인용**. 데모 편성에는 지형을 만드는 책략이 없다 (`npm run shot -- --terrain`도 같은 길) |
| `M` 키 | 배경음악 음소거 (새로고침해도 기억한다) |
| `npm run shot -- --zoom 2.4` | 제어권 유닛에 카메라를 붙인다. **기본 줌에서는 타일 위의 것이 안 보인다** |
| `F` 키 | 확대해서 헤맬 때 판 전체로 되돌린다 |
| 씬의 `debug*` | 조준 후보 · 잔여 WT · 배지 개수 · 그린 명령 수 |

**데모 편성은 Lv1이 아니다** (기획자 지정) — `battle/setup.ts`는 **전원 Lv9 · 능력 전부 HP(50) ·
책략은 전 레벨 환술 8종**이다. Lv1은 책략이 없어 버프·디버프가 아예 생기지 않고 MP도 쓸 데가 없다.

> **`tools/balance.ts`는 여전히 Lv1이다.** 저기까지 바꾸면 5000판 실측치와 비교가 안 된다.

### 전투 연출 층 — 액션 자세 (2026-08-12)

기물 타일이 정지 그림에서 **5자세 스프라이트**가 됐다. 시간표는 GDD §3.10에 확정돼 있다.

| 상황 | 연출 |
|---|---|
| 이동 | 경로의 칸마다 **0.3초씩**. 미끄러지지 않고 한 칸씩 뛴다 |
| 이동 (Knight) | **긴 축 두 칸 → 짧은 축 한 칸**, 모두 세 칸 |
| 공격 | 공격 0.3초 → 대기 0.3초 → **공격 2.3초**. 마지막 2.3초에 대상이 피격 |
| 책략·명상 | 1.3초. **적에게 성공하면 1.3초 더**, 그때 대상이 피격 |
| 퇴각 | 피격을 **0.5초 간격 3번 점멸** 후 소멸 |
| 부활 | 점멸이 **다 끝난 뒤** 부활 자리로 옮겨 0.9초 (조조 「화용도」) |
| 그 외 (고유기술 포함) | 평상 |

> **모든 구간 앞에 카메라 도착 시간 0.6초가 붙는다** (2026-08-13). 큐를 놓고 기다린
> 뒤에 자세가 시작한다 — 같은 자리를 계속 보고 있으면 기다리지 않는다.
>
> **2026-08-13에 전체를 한 박자(≈0.3초) 늦췄다.** 대화창이 판을 못 따라간다는 지적이
> 있었다(실측 밀린 줄 평균 2.7 · 최대 8 → 고친 뒤 1.51). 대화창도 함께 고쳤다 —
> 줄 간격을 **연출 길이 ÷ 줄 수**(0.45~1초)로 잡고, 그래도 모자라면 판이 더 기다린다.

고유기술이 평상인 것은 **의도된 것이다** — 전용 배너(`skillFx`)가 판 전체를 덮으므로
타일 자세까지 바꿀 필요가 없다는 기획자 판단.

**층이 이렇게 나뉜다. 넷코드가 얹혀도 이 경계는 그대로다.**

```
engine        BattleEvent[]           판정 — 언제 무엇이 일어났나
   ↓
poses.ts      PoseDirector.plan()     누가 몇 ms 동안 어떤 칸을 → 필요한 총 시간(ms)
   ↓ (ms)
playback.ts   Playback.hold(ms)       그동안 절대시간·입력·AI 정지
   ↓
BattleScene   sprite.setFrame(n)      그린다
```

- **`PoseDirector`는 Phaser를 부르지 않는다.** 이벤트와 시간만 다룬다. 그 경계 덕에
  헤드리스로 시간표를 잴 수 있고, `packages/client/test/poses.test.ts` 15건이 거기서 나왔다.
  **눈으로는 0.2초와 0.3초를 구분할 수 없으니 이게 유일한 검증 수단이다.**
- **`Playback`은 「몇 ms 기다려라」만 안다.** 무엇을 그리는지는 모른다. 온라인이 되면
  서버는 기다려 주지 않고 재생기가 뒤처질 뿐이라, 이 층이 그대로 쓰인다.
- 공격의 2초와 책략의 두 번째 1초는 **시스템 대화창을 읽을 시간**이다.

> 시간표를 바꾸려면 `poses.ts` 위쪽 상수만 고친다. 테스트가 먼저 깨지므로 의도한
> 변경인지 사고인지가 바로 드러난다.

### 전투 씬 4차 — 카드 스트립 · 커맨드 패널 · 카메라 (2026-08-12)

기획자가 pptx **27~29쪽**에 디자인 지침을 넣었고 그대로 붙였다. **룰 엔진은 안 건드렸다** —
화면 골격과 카메라 층뿐이다. 사양은 GDD §3.10에 옮겨 적었다.
경위는 [`history/2026-08-12_전투화면_카드_카메라.md`](history/2026-08-12_전투화면_카드_카메라.md).

| | 붙은 것 | 파일 |
|---|---|---|
| 1 | **캐릭터 카드 스트립** — 판 위·아래. 6줄 카드 + 고유기술 3색 · 차례/퇴각 | `ui/cardStrip.ts` |
| 2 | **HUD 한 줄** — 시계 · 차례 · 북군/남군 SP 숫자 · `⋯`(기록+항복) | `ui/hud.ts` |
| 3 | **말풍선** — 최근 1줄만 (2026-08-13에 판 **좌하단** 고정으로) | `ui/systemLog.ts` |
| 4 | **커맨드 패널을 판 안 플로팅으로** — 세로 5줄 · 최소화 · 「대기」 | `ui/controlModal.ts` |
| 5 | **패널 사분면** — 가릴 것을 피해 좌우 대칭 | `ui/panelSlot.ts` |
| 5-1 | **자동 포커싱 토글** — 판 왼쪽 위 반투명 버튼 | `ui/focusToggle.ts` |
| 6 | **상태 팝업 개편** — 2×2 · 클릭 설명 · **적 책략 가림** | `ui/inspectPanel.ts` |
| 7 | **카메라 100%/200%** — 부드러운 전환 · 다중 피격 순회 | `battle/camera.ts` · `poses.ts` |
| 8 | **턴 2구간 · 카드 WT 게이지 · 패널 글자 −10%** (플레이 피드백) | 아래 |

### 플레이 피드백 반영 (2026-08-12, 같은 날 2차)

기획자가 직접 돌려 보고 다섯 가지를 짚었다. 전부 반영했다.

| 지적 | 고친 것 |
|---|---|
| 공격할 때 확대가 안 된다 | **드래그 문턱 8px** — 클릭 중 손떨림 1~2px에 `manual`이 서서 자동 카메라가 통째로 죽어 있었다 |
| 이동 후에 커맨드 패널이 떠야 한다 | **턴을 두 구간으로** — 이동 단계(판만) → 행동 단계(패널) |
| 패널 글자가 넘치거나 줄바꿈된다 | 패널·팝업 글자 **일괄 −10%** |
| 판 위 WT 게이지가 안 보인다 | **카드에 대기 게이지**를 붙였다. 판 바깥이라 기본 배율에서도 읽힌다 |
| 공격 범위가 이동 범위와 같이 뜬다 | **`공격`을 눌러야** 범위가 뜬다. 한 번에 한 가지만 칠한다 |
| 공격 직후 패널이 한 번 더 뜬다 | **연출 중에는 패널이 물러난다** — 턴은 연출이 끝나야 넘어간다 |
| 화면을 건드리면 그 뒤로 줌인이 안 된다 | 판 왼쪽 위에 **자동 포커싱 토글** — 수동/자동을 눈으로 보고 되돌린다 |

### 플레이 피드백 4차 (2026-08-12)

| 지적 | 고친 것 |
|---|---|
| 장수 정보 패널 배치 | **등급을 맨 위로 + 6색**(S보라 A빨강 B파랑 C초록 D회색 E황금) · 사진을 **패널 절반 정사각**으로 · `(SP 6)` → `(6)` |
| 공격 범위에 적이 없는데 「공격」이 눌린 것처럼 보인다 | 그때는 **「뒤로」 하나만** 남긴다 |
| 「공격」을 누르면 포커스가 판 전체로 물러난다 | **공격·유닛 조준 중에는 포커스를 유지**한다 (공격 대상은 언제나 인접 칸) |
| 책략 대상을 고르기 어렵다 | **카드로 조준** → 대상으로 포커스 이동 → **확인창**(이름·효과·발동 확률) → [확정]/[취소] |
| 포커스 배율을 300%로 | `camera.ts`의 `SCALE_FOCUS` 하나만 고치면 테스트·스모크가 따라온다 |

> **등급 색의 단일 출처는 `style.css`의 `--grade-*`다.** 카드 스트립·커맨드 패널·메타
> 화면이 전부 이 값을 쓴다 — 같은 등급이 화면마다 다른 색이면 색으로 구분하는 의미가 없다.
>
> **발동 확률의 출처는 엔진의 `illusionChance()`다** (`rules/effects.ts` 신규).
> 화면이 `20 + 지력차`를 다시 적으면 공식이 바뀌었을 때 조용히 어긋난다 —
> 「결계」 0% · 「좌도방술」 100%라는 예외까지 실제 판정과 같은 자리에서 본다.

> **「제자리 대기」가 되살아났다.** 29쪽만 보고 없앴는데, 「이동 후(제자리에 있는 것 포함)」가
> 사양이라 다시 필요해졌다. 엔진에 보내는 의도는 여전히 없다 — 이동 범위를 걷고 패널을
> 띄우는 화면만의 상태다. **화면만의 상태라 무를 수도 있어야 한다** — 행동 단계의 `이동`이
> 그 통로이고, **실제로 움직인 뒤에는 사라진다**(엔진이 거부하는 수라서다).
>
> **`공격`은 대상이 없어도 눌린다.** 의도가 아니라 보기 전환이라서다. 대상이 있을 때만
> 열면 사거리를 볼 방법이 아예 없어진다. 실제로 스모크가 「범위를 볼 수 없다」로 막혔다.

**여기서 굳은 계약 넷**

- **진영 이름은 판이 정한다.** `P2 = 북군(위)`, `P1 = 남군(아래)` — `deployZone`이 P2를
  위쪽 5행에 두기 때문이다(§3.1). 화면에서 이걸 뒤집으면 카드와 기물이 서로 다른 말을 한다.
- **카메라 큐는 자세와 같은 커서 위에 놓는다.** `PoseDirector.plan()`이 둘을 함께 짠다.
  카메라만 따로 0에서 세면 때리는 그림이 뜬 뒤에야 화면이 따라간다 — 액션 자세 때
  「유닛마다 시계를 따로 두면 안 된다」로 밟았던 것과 **같은 지뢰**다.
- **고르는 중에는 카메라가 100%로 물러난다.** 28쪽에 없는 규칙인데 없으면 게임이 막힌다
  (아래 지뢰 참조). 「화면에 칠해진 칸 = 엔진이 통과시킨 칸」이라는 계약은 **누를 수 있어야**
  성립한다 — 화면 밖도, 패널 아래도 누를 수 없다.
- **화면이 같은 값을 두 곳에서 그리지 않는다.** 능력치·상태이상은 카드와 상태 팝업이
  나눠 갖고 커맨드 패널에서 뺐다. 고유기술 현황 줄도 HUD에서 카드로 옮겼다.

**밟은 지뢰 넷** ★

- **200% 줌에서는 먼 칸을 누를 수 없다.** 「내 차례 = 200%」를 곧이곧대로 넣었더니
  「공포」의 대상 두 칸이 **전부 화면 밖**이었다 — 칠해져 있는데 클릭이 닿지 않는다.
  Rock의 이동 후보도 마찬가지다. **고르는 동안에는 100%**로 물러나게 고쳤다.
  화면 밖 기물을 살펴보는 통로는 28쪽이 이미 준 **카드 클릭**이다.
- **플로팅 패널이 판 클릭을 삼킨다.** 조준 대상이 커맨드 패널 아래에 깔려 눌리지 않았다.
  배경은 통과시키고(`pointer-events`) 조작하는 것만 받게 했고, 더해서 **패널 자리를
  후보 칸들의 무게중심을 피해** 잡는다.
- **`#control`에 남은 `position: relative`가 `.panel`의 `absolute`를 덮었다.**
  `data-x`/`data-y`는 제대로 붙는데 패널이 계속 좌상단에 눌러앉았다 — `dataset`만 보고
  「자리는 맞다」고 판단해 한참 헤맸다. **DOM 속성이 아니라 `getBoundingClientRect()`를 재야 한다.**
  스모크가 지금 그렇게 확인한다.
- **Node 타입 스트리핑은 생성자 파라미터 프로퍼티를 지원하지 않는다.**
  `camera.ts`는 `poses.test.ts`가 그대로 실행하므로 `constructor(private x)`를 쓸 수 없다.
  Vite(esbuild)는 통과시켜서 **타입체크와 브라우저는 멀쩡한데 `npm test`만 깨진다.**
- **클릭 한 번에도 포인터는 1~2px 움직인다.** 드래그 문턱 없이 `pointermove`에서
  「직접 조작」을 세웠더니, 첫 클릭에 자동 카메라가 꺼지고 그 뒤로 **연출 큐가 전부 무시**됐다.
  겉으로는 「공격할 때 확대가 안 된다」로만 보인다 — 큐는 멀쩡히 만들어지고 있었다.
  터치에서는 더 심하다. 손가락은 반드시 움직인다.

### 시각 효과 30종 (2026-08-13)

기획자가 엑셀에 「오라매핑」 시트를 넣고 `assets/SpecialStatus/` 30장을 줬다.
경위는 [`history/2026-08-14_시각효과_연출순서_화면정리.md`](history/2026-08-14_시각효과_연출순서_화면정리.md).

사양은 GDD §3.10 「시각 효과」, 파이프라인은 §5, 회귀는 `client/test/visualEffect.test.ts` 20건.

| | 붙은 것 | 파일 |
|---|---|---|
| 1 | **매핑표** — 상태·오라·조종·지형·WT 보정 → 링 / 기술·책략 → 애니 | `tools/extract_data.py` `STATUS_FX_*` |
| 2 | **그림 파이프라인** — 경계상자로 크기 맞추기 · 2×2 → 가로 4칸 | `tools/build_status_fx.py` (`npm run vfx`) |
| 3 | **지속형 링** — 캐릭터 뒤, 겹치면 2초 스왑 | `battle/visualEffect.ts` · `BattleScene.syncRing` |
| 4 | **일회성 애니** — 판 영역 한가운데 4프레임 1초 | `ui/burstFx.ts` |
| 5 | **배너 → 애니 이음** — 고유기술은 2초 배너 뒤에 1초 | `ui/skillFx.ts` |

**여기서 굳은 계약 넷**

- **「오라」와 「시각 효과」는 다른 것이다.** 위 §5의 경고 참조. 엔진에서 `aura`는
  언제나 반경 판정이고, 화면 그림은 `visualEffect`다. 이걸 섞으면 「여포의 링」이
  시전자 표식(`10`)인지 반경 안 적이 보는 것(`9`)인지 말할 수 없게 된다.
- **키가 갈래마다 다르다.** 지속형은 **상태**, 일회성은 **기술·책략 id**.
  지속형을 기술 단위로 잡으면 「증폭」과 「일당백」이 같은 크리티컬인데 다른 그림이 되고,
  일회성을 상태로 잡으면 방덕 「세한지송백」처럼 **이벤트조차 없는** 것을 놓친다.
- **`STATUS_META`가 늘면 테스트가 먼저 깨진다.** 상태이상 22종이 전부 표에 있는지
  `unmappedStatuses()`가 센다 — `STATUS_META`는 `Record<StatusId, …>`라 컴파일이
  이름·설명은 지켜 주지만, 그림 표는 JSON이라 그 보호가 없다.
- **화면이 엔진 상태를 기억하는 자리는 「선공」 하나뿐이다**(`PendingRings`).
  즉시 WT를 당기고 흔적을 안 남기는 것이 그것뿐이라서다. 어떤 것이 그런지는
  추출기가 DSL을 훑어 뽑으므로(`hastenWt`) 목록을 손으로 적지 않는다.

**밟은 지뢰 둘**

- **일회성을 판의 월드 좌표에 놓으면 안 된다.** 기획자 지시는 「지금 화면에서 체스판
  영역의 중심」인데, 월드 좌표에 놓으면 카메라가 200%·300%일 때 화면 밖으로 나간다.
  줌아웃 → 연출 → 줌인으로 되돌리는 길도 있었지만, **DOM(`#burst`)이면 그게 없다** —
  `skillFx` 배너가 이미 같은 층이다. 「200% 줌에서는 먼 칸을 누를 수 없다」와 같은 지뢰다.
- **링은 유닛 컨테이너의 첫째 자식이어야 한다.** 별도 depth로 빼면 이동 연출에서
  캐릭터만 움직이고 링이 제자리에 남는다. 컨테이너 안에 두면 깊이(캐릭터 뒤)와
  좌표(같이 움직임)가 한 번에 해결된다.

### 플레이 피드백 5차 (2026-08-13) — 시각 효과를 붙이고 나서

| 지적 | 고친 것 |
|---|---|
| 조조가 **맞자마자 부활 자리로 순간이동**해 거기서 피격 점멸을 한다 | `unitRevived`가 **쓰러진 자리(`from`)를 함께 싣는다.** 화면은 점멸이 끝날 때까지 그 자리에 붙들어 두고, 다 끝난 뒤 부활 자리에서 되살아난다 (0.9초) |
| 상대 차례에 커맨드 패널이 판을 가린다 | **상대 차례에는 저절로 접힌다.** 「누구를 제어 중」 한 줄만 남는다. 「턴 넘기기」가 열리면 저절로 펴진다 |
| 시스템 대화창이 게임 속도를 못 따라간다 | **연출을 한 박자(≈0.3초) 늦추고**, 대화 간격을 **연출 길이 ÷ 줄 수**로 잡았다. 그래도 모자라면 판이 더 기다린다 |

**여기서 굳은 계약 셋**

- **판은 자기가 설명하는 것을 기다린다.** `hold = max(연출, 대화)`. 예전에는 연출만
  세어서, 한 행동이 4~5줄을 만드는 구간(책략 = 시전 + 성공 + 효과들)에서 말만 뒤로
  밀렸다 — 화면은 이미 두세 수 지난 일을 말하고 있었다.
- **엔진이 좌표를 갈아 끼우는 순간과 화면이 그것을 보여줄 순간은 다르다.**
  부활이 그 첫 사례다. 「연출이 끝난 뒤에 옮긴다」가 필요하면 **엔진이 옛 값을 실어 줘야**
  한다 — 화면이 이전 상태를 기억하게 만들면 온라인에서 재접속할 때 무너진다.
- **자동으로 접은 것과 사용자가 접은 것을 구분한다**(`autoMin` ↔ `minimized`).
  섞으면 내 차례가 왔을 때 원래 상태로 못 돌아가고, 「펼치기」를 눌렀는데 아무 일도
  일어나지 않는 것처럼 보인다.

**밟은 지뢰 하나** — **스모크에 고정 대기 시간(`waitForTimeout(300)`)을 쓰면 안 된다.**
연출을 0.3초 늦추자 「이동을 마쳤는데 커맨드 패널이 뜨지 않는다」로 막혔다. 실제로는
아직 연출 중이었다. `settle()`이 자세·대화·카메라가 모두 멎기를 기다리므로 그쪽을 쓴다
(`debugCameraSettled`에 `playback.busy`를 더했다).

### 플레이 피드백 6차 (2026-08-13) — 시각 효과를 붙이고 나서 2차

| 지적 | 고친 것 |
|---|---|
| HP 게이지가 **가장 먼저** 줄고 때리는 그림이 나중에 뜬다 | 연출 층이 「언제 얼마가 변하는가」를 들고 있다가 **피격 그림과 같은 순간**에 반영한다 (`PoseDirector.shownHp`) |
| 카드의 「대기」가 뭔지 모르겠다 · `0.0일`에 머문다 | **`WT`**로 고치고 **소수 둘째 자리**까지 |
| 공격도 책략처럼 미리 보고 확정하고 싶다 | **공격 확인창** — 예상 데미지 · 남을 HP · 크리티컬 확률 · `[확정]/[취소]` |
| 고유기술을 **누가** 쐈는지 안 보인다 | 배너 앞에 **시전자 얼굴 1초**를 넣어 3단으로 (얼굴 → 배너 → 일회성) |
| 어두워진 배경에서 시스템 메시지가 안 보인다 | **연출 중에는 대화도 멈춘다** — 걷히고 나서 읽힌다 |
| 말풍선이 진영에 따라 위아래로 옮겨 다닌다 | **판 영역 한가운데 고정** |
| 화용도 — 포커스가 **부활 자리에 미리** 가 있고 공격·피격은 화면 밖에서 벌어진다 | 공격·피격 큐는 **쓰러진 자리**를, 부활 큐만 새 자리를 비춘다 |

**여기서 굳은 계약 셋**

- **미리보기의 출처는 엔진이다.** 공격 확인창의 숫자는 전부 `forecastAttack()`이 낸다 —
  책략이 `illusionChance()`에 묻는 것과 같다. 화면이 `floor(AT × 2)`를 다시 적으면
  공식이 바뀌었을 때 **표시만** 조용히 어긋난다. 그리고 미리보기는 **난수를 쓰지 않는다** —
  여기서 굴리면 `rngCursor`가 밀려 리플레이가 깨진다.
- **`state`는 이미 적용이 끝난 상태다.** 화면이 「맞기 전」을 보여 주려면 연출 층이
  그 차이를 들고 있어야 한다. HP 게이지(`hpPending`)와 부활 좌표(`holdAt`)가 같은
  구조다 — 화면이 이전 상태를 **기억**하게 만들지 않고, 이벤트가 실어 준 것과
  「아직 안 온 변화분」만으로 되돌린다. 온라인에서 재접속해도 무너지지 않는다.
- **의도를 내지 않는 조작은 스스로 다시 그려야 한다.** 확인창을 띄우는 것은 엔진에
  아무것도 보내지 않으므로 상태가 안 바뀌고, 따라서 갱신 계기가 없다.
  「제어권 획득은 상태 변경이 아니라 시간 경과」와 같은 자리다.

**밟은 지뢰 둘**

- **`look()`이 권위 좌표를 읽는 것이 화용도에서 함정이 됐다.** `plan()`이 받는 `state`는
  적용이 끝난 뒤라 조조의 `pos`가 이미 부활 자리다 — 그래서 **맞기도 전에** 카메라가
  새 자리를 비추고 있었다. 이벤트가 실어 준 좌표(`from`/`at`)를 쓰는 쪽이 맞다.
  카메라 테스트의 픽스처는 좌표가 고정이라 이 어긋남을 **테스트가 먼저 잡아 줬다.**
- **확인창을 띄우고 `syncUnits()`를 안 불러 아무것도 안 떴다.** 클릭은 먹었고 상태도
  섰는데 화면만 그대로였다 — 「눌러도 반응이 없다」로 보인다. 취소 버튼도 같았다.

### 플레이 피드백 7차 (2026-08-13) — 화면 정리 12건

| 지적 | 고친 것 |
|---|---|
| 줌인·줌아웃이 **액션과 동시에** 돌아 움직임이 안 보인다 | 카메라 큐를 놓고 **0.6초 기다린 뒤** 자세를 시작한다(`CAM_LEAD_MS`). 같은 자리면 안 기다린다 |
| 말풍선이 한가운데라 캐릭터를 가린다 | **판 좌하단**으로. 왼쪽 위는 자동 포커싱 토글, 왼쪽 아래는 말풍선 — 가운데는 비운다 |
| 공격 확인창의 [확정]이 안 걷히고 판을 가린다 | **확인창을 없앴다.** 대신 조준 중 **대상 칸 위에 크리티컬 확률**을 반투명 숫자로 |
| Knight가 한 번에 순간이동한다 | **긴 축 두 칸 → 짧은 축 한 칸**, 세 칸으로 걷는다 |
| 상태 팝업의 「아군/적군」이 자리를 먹는다 | 뺐다 — 사진 테두리 색과 판의 위아래가 이미 말해 준다 |
| WT 게이지 색·그라데이션 | 차오르는 동안 회색 · 다 차면 파랑. HP·WT 모두 그라데이션. 「다음 차례」 노란색은 없앴다 |
| 고유기술 버튼이 단색 | 나무결 그라데이션 |
| 타일의 `S1 조조·K` 글자와 바 3종 | **글자 전부 삭제 · 바는 HP 하나만** |
| 상태 팝업 아래가 텅 빈다 | 「걸린 상태 없음」을 없애고, 높이 0.6배를 **상한**으로 바꿨다 |
| 커맨드가 세로 5줄이라 길다 | **2×2** (공격·책략 / 명상·대기). 「이동」은 무를 때만 한 줄 더 |
| 고유기술 초상화가 원형이라 밋밋하다 | **판 전체를 덮는 사각**으로 꽝 → 흐려지며 판이 검게 → 배너 이름 하이라이트 |
| 테두리가 전원에게 있어 차례가 안 보인다 | **차례인 기물만** — 아군 초록 · 적군 빨강 |

**여기서 굳은 계약 셋**

- **카메라가 먼저 도착하고 나서 자세가 시작한다.** `look()`이 커서를 미는 구조라
  **반드시 `show()`보다 먼저** 불러야 한다 — 순서가 뒤집히면 자세가 이미 시작된
  자리에 큐가 놓인다. 각 `case`의 첫 줄이 `look()`인 것은 그래서다.
- **판 위에는 한눈에 봐야 하는 것만 남긴다.** 이름·급·MP·WT는 전부 카드 스트립과
  상태 팝업이 더 크게 보여준다. 기본 배율에서 셀은 30px대라 글자가 어차피 뭉갠다.
- **판 가운데는 비워 둔다.** 포커스가 잡히는 자리라 무엇을 놓든 캐릭터를 가린다.
  고정으로 둘 것은 **모서리**로 — 왼쪽 위 토글, 왼쪽 아래 말풍선.

**밟은 지뢰 셋** ★

- **연출을 늦추면 「그 전」에 무엇을 그릴지도 함께 정해야 한다.** 카메라 리드를 넣자
  그 0.6초 동안 `cellOf()`가 비어 화면이 **권위 좌표(도착지)**를 그렸다 — 기물이
  도착지에 한 번 떴다가 사라지고 출발점부터 다시 걸었다. 이동도 부활과 같은
  `holdAt`/`holdUntil`로 **출발점에 붙들어** 고쳤다.
  → **교훈: `state`는 언제나 결과다. 연출이 시작되기 전 구간은 전부 「붙들어 둘 자리」가 필요하다.**
- **모달로 되돌릴 기회를 주는 것이 늘 좋은 것은 아니다.** 공격 확인창을 붙였더니
  판 한가운데를 덮어 정작 연출을 가렸다 — 공격은 인접 칸에 한 번 치는 것이라
  책략만큼 무를 값어치가 없었다. **붙였다 뺐고, 알아야 할 값(확률)만 칸 위로 옮겼다.**
- **화면에서 글자를 지웠더니 스모크 검사 하나가 조용히 무력화됐다.**
  「적 책략은 가린다」를 `inspect.side === '적군'`으로 골라내고 있었는데, 그 글자를
  빼자 조건이 늘 거짓이 되어 **무엇을 노출해도 통과**하게 됐다. `data-side`로 옮겨 고쳤다.
  → **교훈: 검사는 사람이 읽는 글자가 아니라 기계가 읽는 속성으로 건다.**

### 전투 화면 리디자인 — 판 지도 · 카드 벽보 (2026-08-14)

경위는 [`history/2026-08-14_지형_화면리디자인_배경음악.md`](history/2026-08-14_지형_화면리디자인_배경음악.md).

기획자가 목업을 주고 `assets/map/`의 두 장(`chessmap.png` · `person.png`)을 붙여 달라고
했다. **룰 엔진은 안 건드렸다** — 판을 그리는 층과 카드 스트립의 DOM·CSS뿐이다.
사양은 GDD §3.10 「화면 바탕 — 지도와 벽보」, 굽는 것은 §5.

| | 붙은 것 | 파일 |
|---|---|---|
| 1 | **판 지도** — 2400² 판 전체를 덮고 그 위에 색조·격자·눈금만 | `battle/BattleScene.ts` `drawBoard` |
| 2 | **카드 벽보 액자** — 9분할. 카드는 안쪽 흰 종이 위에 | `ui/cardStrip.ts` · `style.css` `.uc-frame` |
| 3 | **산수 배경** — 스트립 뒤 | `style.css` `#top, #bottom` |
| 4 | 그림 파이프라인 | `tools/build_frames.py` (`npm run frames`) |

**여기서 굳은 계약 셋**

- **밝은 바탕이 되면 「검은 외곽선」이 전부 뒤집힌다.** 좌표 눈금은 먹으로 쓰고
  종이색으로 두른다 — 그대로 두면 글자는 날아가고 외곽선만 남아 판이 얼룩진다.
  진영 색도 옅게, 체스판 무늬는 아예 뺐다. **지도가 없으면 예전 값 그대로다.**
- **배경은 굽는 단계에서 누르고, 앞의 것은 화면에서 세운다** (기획자 지적 2차).
  지도는 채도를 빼고 종이색으로 밀어 **미리 눌러** 굽고(`--map-desat`·`--map-fade`),
  격자선은 **화면 기준 굵기**(1.1px)로 배율마다 다시 긋는다. 반투명 막으로 덮는 길도
  있었지만 그러면 그 위의 하이라이트·기물까지 같이 뿌예진다.
- **액자는 9분할이어야 한다.** 통째로 늘이면 3:3(카드 3장)에서 지붕이 옆으로 퍼지고
  5:5에서는 홀쭉해진다. 자르는 자리는 `tools/build_frames.py`의 `SLICE`와 `style.css`
  두 곳에 있고 **같은 값이어야 한다** — 도구가 돌 때마다 CSS 한 줄을 찍어 준다.
- **카드 한 장 = 벽보 한 장.** 카드 줄과 고유기술 줄이 따로 있던 두 그리드를 한 칸으로
  합쳤다. 액자가 둘을 함께 감싸야 하기 때문이고, 버튼이 카드 아래 한 줄로 늘어서는
  모습은 그대로다(칸마다 맨 아래이므로).

**밟은 지뢰 둘** ★

- **그리드 줄을 `auto`로 두면 카드가 사진 크기만큼 자란다.** `.strip-slots`에
  `grid-template-rows`를 안 적었더니 암시적 줄이 내용 높이로 늘어, **3:3에서만**
  벽보가 스트립을 넘고 HP·WT·고유기술 줄이 잘렸다(5:5는 우연히 높이가 맞아 멀쩡했다).
  `minmax(0, 1fr)`로 못 박았고, **스모크가 「액자 높이 > 칸 높이」로 잡는다** —
  고친 것을 잠깐 되돌려 실제로 잡히는지 확인했다(248px > 185px로 걸렸다).
  → **교훈: 「한쪽 판에서만 깨지는 레이아웃」이 있다. 3:3과 5:5를 둘 다 본다.**
- **흰 종이와 크림색 배경의 차이가 12뿐이다.** 나무 바깥을 색으로 지우다가 종이까지
  뚫을 뻔했다. 종이가 없는 **줄**에서만 지우도록 막았다. 처음에는 「가장자리에서
  이어진 것만」 지웠는데, 처마 아래 그늘 틈이 까치발에 막혀 갇혀 있어 카드마다
  흰 줄이 하나씩 그어졌다.

### 배경음악 — 화면마다 한 곡 (2026-08-14)

경위는 [`history/2026-08-14_지형_화면리디자인_배경음악.md`](history/2026-08-14_지형_화면리디자인_배경음악.md) §7~§8.

기획자가 `assets/Audio/bgm/` 6곡을 주고 화면별로 지정했다. 붙은 것은 §5의 표 그대로다.

| | 붙은 것 | 파일 |
|---|---|---|
| 1 | 곡 고르기·페이드·음소거 | `audio/bgm.ts` |
| 2 | 메타 화면 → 곡 | `screens/App.tsx` (`trackForScreen`) |
| 3 | 전투 안 단계 → 곡 | `battle/BattleScene.ts` (`trackForPhase`) |
| 4 | 옮기는 도구 | `tools/build_audio.py` (`npm run audio`) |

**여기서 굳은 계약 셋**

- **모르는 화면은 메인곡이다.** 지정이 「넷이 아닌 화면」이라 상점·랭킹이 생겨도
  `bgm.ts`를 손대지 않아야 그 말이 지켜진다 — 테스트가 그걸 고정한다.
- **전투 안의 곡은 화면이 아니라 단계가 정한다.** 배치·정찰과 전투의 경계를 아는 것은
  `Playback.phase`뿐이다. 화면 이름으로 고르면 배치 단계에 전투곡이 먼저 나온다.
- **곡을 다시 틀지 않는다.** 같은 곡이면 `playBgm`이 아무 일도 하지 않는다 —
  화면이 다시 그려질 때마다 처음부터 되감기면 안 된다.

**밟은 지뢰 둘** ★

- **「이미 최신」이라며 조용히 건너뛰는 것이 소리에서는 특히 위험하다.** 기획자가 3번과
  4번 곡의 순서를 바로잡느라 **내용만 맞바꿔** 저장했는데 파일 시각은 예전 그대로였다.
  시각만 보던 도구는 아무 일도 하지 않았고, 화면에서는 옛 곡이 계속 나왔다 —
  그림이면 눈에 띄지만 소리는 그렇지 않다. **내용을 비교하도록** 고쳤다.
  → **교훈: 확인이 어려운 산출물일수록 「안 해도 되는지」를 더 엄격하게 판단한다.**
- **브라우저는 첫 조작 전에는 소리를 내주지 않는다.** 첫 화면에서 `play()`가 거부되므로
  곡을 물고 있다가 첫 클릭·첫 키에 다시 튼다. 실측으로 확인했다(조작 전
  `blocked: 'main'` → 클릭 뒤 `null`). **오류가 아니라 규칙이라 콘솔에도 안 남는다** —
  모르면 「첫 화면만 소리가 안 난다」로 보이고 원인이 코드에 없다.

> 검증은 **「지금 어느 곡을 틀기로 했는가」**로 한다. 헤드리스에는 출력 장치가 없어
> 소리 자체는 볼 수 없다. `smoke:meta`가 메인→편성→배치→전투 네 자리에서 곡을 확인하고,
> 보상 화면은 전투가 끝나야 열려 스모크가 못 가므로 `test/bgm.test.ts`가 규칙을 고정한다.

### 간판 · 도시 · 자리 화면 (2026-08-15)

기획자가 pptx **33~36쪽**에 화면 구성을 넣고 `assets/Backgrounds/` 4장을 줬다.
**룰 엔진은 안 건드렸다** — 메타 화면(React)과 그림 파이프라인뿐이다.
사양은 GDD §8.1, 파이프라인은 §5.

| | 붙은 것 | 파일 |
|---|---|---|
| 1 | **간판·로그인 화면** — 게임 URL의 첫 화면. `ID` · 입장 · 계정 생성 | `screens/TitleScreen.tsx` |
| 2 | **시간대 배경** — 접속 시각으로 낮·황혼·밤을 고른다 | `screens/backdrop.ts` |
| 3 | **도시 화면 개편** — 배경 위에 궁궐·병영·장터 간판 | `screens/MainScreen.tsx` |
| 4 | **궁궐·병영·장터** — 도시 레벨로 그림이 갈린다 | `screens/PlaceScreen.tsx` |
| 5 | **환경설정 팝업** — 기어. ID · ID 기억 · Language · 화면 모드 · 배경음악 | `screens/SettingsModal.tsx` |
| 6 | **다국어 틀** — 한국어 기준 + 영어·포르투갈어·일본어·중국어 | `i18n/index.ts` |
| 7 | 그림 파이프라인 | `tools/build_backgrounds.py` (`npm run backgrounds`) |

**게임 이름이 「만민의 삼국지」로 정해졌다** (기획자 지정). 화면에 뜨는 이름은
`i18n`의 `game.title` 한 곳이다 — 「삼국 약식 체스」는 프로젝트 이름으로만 남는다.

**여기서 굳은 계약 넷**

- **시각을 읽는 것과 규칙은 다른 층이다.** `bandForHour(h)`가 규칙이고 지금 시각을
  넣는 것은 부르는 쪽이다. 전투 재현성과는 상관없지만, **경계(7·16·20시)를 테스트로
  고정하려면** 시계를 밖에서 넣을 수 있어야 한다 — 밤 그림은 밤에 접속해야 보인다.
- **배경은 화면이 뜰 때 한 번 정한다.** 매 렌더마다 `new Date()`를 만들면 자정에
  걸친 판에서 그림이 깜빡이고, 어차피 한 화면에 머무는 동안 바뀔 일이 아니다.
- **번역이 없으면 한국어로 물러난다.** 문구는 별도 세션에서 오므로 `en`~`zh`가
  비어 있는데, 그동안 화면에 **키 이름이 뜨면 안 된다**. 키는 한국어 표에서 뽑으므로
  (`Record<StringKey, …>`) 없는 키를 쓰면 타입 검사가 먼저 막는다.
- **자리만 잡아 둔 스위치는 잠근다.** `ID 기억`·`화면 모드`는 눌리지 않게 두고 왜인지
  적었다 — 눌리는데 아무 일도 없으면 「고장인가」가 남는다.

**밟은 지뢰 넷** ★

- **띠를 등분해 자르면 옆 칸이 딸려 온다.** `openBackground`는 804px인데 진짜
  경계가 **266·535**(등분하면 268·536)라, 낮 그림 오른쪽에 황혼의 주황 띠가 2px
  남았다. 스크린샷을 눈으로 보고서야 알았다 — 도구는 아무 말도 하지 않았다.
  지금은 등분한 자리 언저리에서 **색이 가장 크게 튀는 열**을 찾아 자르고 옮긴 값을 찍는다.
  → **교훈: 「그림 넉 장을 넉 등분」처럼 당연해 보이는 것이 실제로는 안 맞을 수 있다.**
- **저해상도 원본을 그대로 썼다.** 처음 받은 PNG는 칸 하나가 225~300px이라 프레임
  (320~700px)에 늘려 깔면 깨진다. **480px 스크린샷으로만 확인해서 못 알아챘고**,
  기획자가 화면을 보고 지적했다 — 그 크기에서는 늘려도 눈에 잘 안 띈다.
  → **교훈: 화면을 꽉 채우는 그림은 프레임이 가장 클 때(700px)로 확인한다.**
- **`.panel`은 이미 전투 화면이 쓰고 있었다.** 자리 화면의 판때기에 같은 이름을 붙였더니
  `position: absolute` + 폭 `--board × 3/8`이 그대로 먹어 **오른쪽 아래에 좁게 붙었다.**
  CSS는 이름이 겹쳐도 오류가 없어 화면을 봐야만 안다. `.place-panel`로 바꿨다.
  → **교훈: 전역 CSS에 흔한 이름(`.panel`·`.card`·`.bar`)을 새로 쓰지 않는다.**
- **스모크가 옛 길을 그대로 걷고 있었다.** 첫 화면이 간판으로 바뀌고 `3v3`이 병영으로
  들어가자 `smoke:meta`가 통째로 막혔다. 화면 골격을 바꿀 때는 **스모크의 경로를 함께
  고친다** — 지금은 간판 → 도시 → 병영/궁궐을 실제로 눌러 지나간다.

> **배경 확인은 「띄우기로 한 그림」과 「실제로 뜬 그림」을 둘 다 본다.**
> `background-image`가 붙어 있어도 에셋이 없으면 404만 나고 화면은 바탕색으로
> 물러난다 — 스모크가 그 URL을 `fetch`해 200인지까지 확인한다.

### 장수 일람 · 상세 (2026-08-17) — 트랙의 A

pptx **37·38쪽**. **룰 엔진은 안 건드렸다** — 메타 조회 함수와 화면뿐이다.
사양은 GDD **§8.2**, 진행 통제와 밟은 지뢰는 [`docs/작업계획.md`](docs/작업계획.md) §3-A.

| | 붙은 것 | 파일 |
|---|---|---|
| 1 | 궁궐 두 갈래 — **[장수 일람] [도시 관리]**(잠김) | `screens/PlaceScreen.tsx` |
| 2 | **일람** — 표 6열 · 검색 · 정렬 4종 · 레벨업 Flag · 요약 줄 | `screens/OfficerListScreen.tsx` |
| 3 | **상세** — 정보 · 서사 자리 · [일람으로][레벨/스킬 관리][전적 보기] | `screens/OfficerDetailScreen.tsx` |
| 4 | **고유기술 팝업** — 배너 · 효과 · 유래 자리 | `screens/SkillModal.tsx` |
| 5 | **레벨/스킬 관리** — 옛 화면을 그대로 옮김 (B가 갈아 끼운다) | `screens/LevelUpScreen.tsx` |
| 6 | 줄 만들기·검색·정렬·요약·**AT 범위** (순수 함수 · 회귀 13건) | `meta/src/officers.ts` |

**여기서 굳은 계약 넷**

- **화면은 고르기만 하고 정렬도 판정도 하지 않는다.** 「레벨업 Flag」는 `canLevelUp()`이,
  「가나다순」은 `sortRows()`가 낸다. 화면이 `카드 >= 필요분`을 다시 적으면 규칙이 바뀌었을 때
  **배지만** 조용히 어긋나고, 정렬이 진짜 사전 순인지는 스크린샷으로 확인할 수 없다.
  전투 UI가 `validate()`에 묻는 것과 같은 결이다.
- **`AT`는 언제나 범위다.** 데미지가 매 타격 내림이라 `AT 2.5`는 평타 2 · 크리티컬 5다.
  `atRange()`가 `FORMULA.damage`를 지난다 — 전투의 `attackRange()`는 `BattleState`를
  요구해 편성 밖에서 못 쓴다.
- **자리만 만든 것은 두 갈래로 나뉜다.** **글**(인물 서사·기술 유래)은 없으면 **줄째로
  사라지고**, **단추**([전적 보기]·[도시 관리])는 **눌리지 않게 두고 왜인지 적는다.**
  빈 글은 「데이터가 빠졌나」, 죽은 단추는 「고장인가」가 남는 것이 각각의 이유다.
- **`OfficerData.story?` · `UniqueSkillData.origin?`는 타입에만 있다.** 추출기는 아직
  안 채운다 — 엑셀에 열이 더해지면 값이 오고 **화면은 손대지 않는다.**

**밟은 지뢰 셋** ★ — 전부 **CSS라 오류가 안 나고 화면을 봐야 안다**

- **접두사를 붙여도 같은 파일 안에서 겹친다.** `.ofc-head`를 표 머리(grid)와 상세
  머리(flex) **양쪽**에 붙여 한쪽이 조용히 무너졌다. `.ofc-thead` / `.ofc-bio`로 갈랐다.
  → **교훈: 「흔한 이름을 쓰지 않는다」는 전역만의 규칙이 아니다.**
- **`.scr .foot`(선택자 둘)이 `.ofc-tally`(하나)를 이겼다.** 요약 줄이 한 줄이 아니라
  세 줄로 쌓였다 — `flex-direction: column`이 그대로 살아 있었다.
- **팝업이 화면 한가운데가 아니라 내용 맨 아래에 붙었다.** `.scr-bg > *`·`.scr-dim > *`가
  「어둠 위로 올리려고」 자식 **전부**에 `position: relative`를 걸어 `.modal-back`의
  `absolute`를 덮고 있었다. **환경설정 팝업도 원래 그랬다** — 스모크가 「떠 있는가」만
  보고 있어서 못 잡았다. 함께 고쳤다.
  → **교훈: 「있는가」와 「제자리에 있는가」는 다른 검사다.** 커맨드 패널에서 이미 밟았다.

**GDD §4.3의 레벨업 카드 표가 엑셀과 달랐다** — Lv6~9가 GDD `15/20/25/30`(누적 116),
엑셀·`growth.json`은 `12/15/22/25`(누적 **100**). **엑셀이 맞다**는 기획자 확인을 받아
GDD를 고쳤다. 코드는 처음부터 JSON을 읽고 있었으므로 동작은 바뀌지 않았다.

### 레벨/스킬 관리 · 재설계 (2026-08-17) — 트랙의 B · 저장 형식 v2

pptx **39쪽**. **룰 엔진은 안 건드렸다** — 저장 형식과 메타 규칙, 그리고 화면뿐이다.
사양은 GDD **§4.2 · §4.3 · §8.3**, 경위는
[`history/2026-08-17_레벨스킬관리_저장형식v2.md`](history/2026-08-17_레벨스킬관리_저장형식v2.md).

```
v1   statPicks ['hp','hp','at',…]              길이 = level − 1
     tactics   ['공포','화계','진화',…]         ← Lv6·7이 둘씩 들어와 어긋난다
v2   growth[]  [{stat, tactics:[…]}, …]        길이 = level − 1, 언제나
```

| | 붙은 것 | 파일 |
|---|---|---|
| 1 | **저장 형식 v2** — `GrowthStep` · `growth[]` | `meta/src/types.ts` |
| 2 | **파생 함수 둘** — `statPicksOf(inst, cap?)` · `tacticsOf(inst, cap?)` | `meta/src/profile.ts` |
| 3 | **재설계(되감기)** — `canRespec` · `applyRespec` · `cardsSpentOn` · `checkGrowth` | 〃 |
| 4 | **증분 미리보기** — `growthPreview()` (39쪽의 `+5 → 15`) | 〃 |
| 5 | **되접기** — v1 → v2 · 손상 복구 · 멱등 | `meta/src/migrate.ts` (신규) |
| 6 | **화면** — 관리 · 고르기 · 재설계 확인 팝업 | `screens/LevelUpScreen.tsx` |

**여기서 굳은 계약 다섯** ★

- **`growth.length === level − 1`은 언제나 참이다.** 이 하나로 **레벨 하향이
  `slice` 한 줄**이 된다(E의 전제). 마이그레이션도 **레벨을 스택에서 정한다** —
  저장된 `level`을 믿고 스택을 맞추는 쪽이면 손상된 입력에서 불변식이 깨진다.
- **재설계(둔갑천서)는 「되감기」다** (2026-08-17 기획자 확정) — 레벨업에 쓴 카드를
  **전부 돌려주고 Lv1로.** 그 뒤는 정상 레벨업 절차 그대로라 **재설계 전용 규칙이
  없다.** 「레벨 유지 + 다시 고르기」였다면 마법사·걸음 되돌리기·「고르는 도중」이라는
  없던 상태가 전부 따라왔다. **돌려주는 양이 정확히 누적 필요분**이라 다시 올리면
  카드가 딱 떨어진다 — 어긋나면 재설계가 카드를 찍어 내거나 삼킨다. `record`는 안 건드린다.
- **성장 스택을 읽는 자리는 파생 함수 둘뿐이다.** 화면도 편성도 엔진 변환도 전투력도
  거기만 부른다. 룰 엔진의 `RosterEntry`는 **평면 형식 그대로**이고 펴는 곳은
  `toRosterEntries()` 하나라, `packages/rules`와 D의 실측 계수가 한 글자도 안 바뀌었다.
- **버전이 달라도 계정을 버리지 않는다 — 되접는다.** 되접었으면 **그 자리에서
  저장한다**(안 그러면 옛 형식이 남아 접속마다 다시 되접히고 복구도 안 굳는다).
  살릴 수 있는 만큼 살리되 **레벨을 올려 주지는 않는다**. 모르는 미래 형식(`version`이
  더 큼)만 버린다.
- **버전은 뜻이 바뀔 때만 올린다.** 필드가 더해지기만 하는 변경(C2의 도시, E의 부대)은
  마이그레이션이 기본값으로 채우므로 **버전을 올리지 않는다** — 올리면 되접을 것이
  없는데도 옛 계정이 한 번씩 그 길을 지난다. `migrate.ts`에 그 자리를 주석으로 표시해 뒀다.

> **재설계에는 금화 10냥이 실제로 나간다**(`ECONOMY.respecItemGold`). 상점이 없어
> 오프라인에서는 못 눌러 보므로 개발용 통로에 **「금화 +10」**을 함께 뒀다 —
> 「카드 +5」와 같은 자리이고 상점이 붙으면 함께 지운다.

> **`power.test.ts`는 안 깨졌다** — 인수인계 노트의 예상과 달랐다. 그 테스트는
> `RosterEntry` 모양을 직접 만들어 `OfficerInstance`를 지나지 않는다. 그래서
> 「v1과 v2가 같은 전투력을 낸다」를 회귀로 따로 박았다 — 안 깨진 것이 우연이면 안 된다.

### 전적 · 보상 개편 (2026-08-18) — 트랙의 C1 · 저장 형식 v3

pptx **40쪽 + 45쪽 보상표**. 룰 엔진은 또 안 건드렸다. 사양은 GDD
**§6.1 · §6.4 · §7 · §8.4**, 경위는
[`history/2026-08-18_전적_보상개편_v3.md`](history/2026-08-18_전적_보상개편_v3.md).

```
v2   record { wins, losses, kills }                  ← 평평하다. 기물도 모드도 모른다
v3   record['{상대}/{모드}/{기물}'] = { plays, wins, draws, losses, kills }
     matches[]  한 줄 = DB 한 행 (seq가 PK) · matchSeq
```

| | 붙은 것 | 파일 |
|---|---|---|
| 1 | **저장 형식 v3** — `RecordTally` · `MatchRow` · `result: 'win'\|'draw'\|'lose'` | `meta/src/types.ts` |
| 2 | **읽는 자리** — `sumTally` · `pieceRows` · `modeRows` · `recentMatches` | `meta/src/records.ts` (신규) |
| 3 | **보상표** — 세 결말 · 무승부 택1 · 상한 B급 · 이력 한 줄 | `meta/src/rewards.ts` |
| 4 | **되접기** — v2 → v3 (옛 평평한 전적은 버린다) | `meta/src/migrate.ts` |
| 5 | **화면** — 40쪽 전적 관리 · 필터 · 이력 목록 | `screens/RecordsScreen.tsx` (신규) |
| 6 | **결과 화면** — 세 결말 · 무승부 택1 · 예상 승률 줄 | `screens/ResultScreen.tsx` |

**여기서 굳은 계약 다섯** ★

- **AI와 온라인이 갈리지 않는다** (2026-08-04와 45쪽을 **뒤집는다**). 보상도 전적도 같고,
  `opponent`는 **이력의 라벨과 전적 필터로만** 쓴다. 근거는 F가 문을 [출정하기] 하나로
  합치기 때문이다 — 상대가 사람인지 AI인지는 고르는 것이 아니라 **그때의 운**이라
  보상이 갈리면 접속 시간대가 곧 보상이 된다.
- **전적은 교차로 한 번만 세고 표의 모든 줄을 합으로 낸다.** 40쪽이 같은 숫자를
  기물별·모드별·총합 셋으로 요구하는데, 따로 세면 어긋나고 **어긋나도 화면은 조용하다.**
  읽는 자리는 `sumTally()` 하나다. **계정 전적만은 따로 센다** — 장수 전적을 더하면
  한 판에 3~5명이 뛰므로 출전 수가 사람 수만큼 부풀려진다.
- **무승부는 고른 뒤에만 반영된다.** 「고르는 도중」을 저장하지 않는다(재설계와 같은 결).
  `applyBattleResult()`는 택1 없는 무승부를 **던져서** 막는다 — 조용히 넘기면 전적만
  오르고 보상이 사라진 계정이 남는다.
- **이력은 DB로 옮길 것을 전제로 둔다** (기획자). 한 줄이 평평하고 참조가 없으며
  `seq`가 PK다. 읽는 자리가 `recentMatches()` 하나뿐이라 옮길 때 이 함수만 쿼리로 갈면
  화면은 안 바뀐다. **집계와 이력을 따로 쌓는다** — 이력에서 집계를 파생시키면
  꼬리를 덜어 낼 때(`MATCH_LOG_CAP` 200) **통산이 조용히 준다.**
- **예상 승률은 승률과 양쪽 전투력 원본을 함께** 남긴다(눈금이 바뀌어도 그때 판단이
  보존되게). 계산은 `winChance()`를 부르고 **랭킹 정렬 기준으로는 쓰지 않는다** —
  정렬에 넣으면 담합이 1위를 만든다.

**알면서 안 고치고 남긴 것 다섯** — 목록과 담당 세션은 작업 계획 **§4-5**에 있다.
요지는 ① **[항복]이 내 차례가 아니면 조용히 무시된다**(화면에 표시 없음) ·
② 이력의 빈 열 둘(부대 이름 = E, 상대 id = F) · ③ 한산한 시간대의 AI 파밍(관찰 대상,
막을 자리는 `CARD_GRADES` 하나) · ④ 랭킹 화면 부재 · ⑤ 무승부 화면은 실측으로 못 봤다.

> **`applyRespec`은 여전히 전적을 안 건드린다.** 되감는 것은 성장이지 싸운 역사가 아니다 —
> v3에서도 그 경계를 그대로 뒀다.

> **v2의 평평한 전적은 버렸다**(기획자 확정). 기물·모드를 몰라 넣을 칸이 없고, 그 값은
> 전부 「AI는 안 센다」던 시절의 AI 전적이다. **계정과 장수는 그대로 살린다** —
> 「살릴 수 있는 만큼 살린다」의 유일한 예외라 `migrate.ts`에 이유를 적어 뒀다.

### 도시 관리 · 군량 시간 충전 (2026-08-18) — 트랙의 C2 · 저장 형식은 필드 하나

pptx **41쪽**. 룰 엔진은 또 안 건드렸다. 사양은 GDD **§5 · §8.5**, 경위는
[`history/2026-08-18_도시관리_군량충전.md`](history/2026-08-18_도시관리_군량충전.md).

```
v3 그대로 + PlayerProfile.grainAt   군량을 마지막으로 정산한 시각(epoch ms). 0 = 아직
```

| | 붙은 것 | 파일 |
|---|---|---|
| 1 | **군량 시간 충전** — `syncGrain(profile, nowMs)` 순수 함수 | `meta/src/city.ts` (신규) |
| 2 | **증축** — `upgradeCost` · `canUpgradeCity` · `applyCityUpgrade(p, nowMs)` | 〃 |
| 3 | **필드 하나 + 되접기 한 줄** — `grainAt` | `meta/src/types.ts` · `migrate.ts` |
| 4 | **계정 모드별 두 줄** — `accountModeRows` | `meta/src/records.ts` |
| 5 | **화면** — 41쪽 도시 정보 · 증축 확인 팝업 | `screens/CityScreen.tsx` (신규) |
| 6 | **화면** — 41쪽 도시 전적 (필터 3갈래) | `screens/CityRecordsScreen.tsx` (신규) |
| 7 | **시각을 넣는 자리 하나** — 열릴 때 + 1분마다 | `screens/App.tsx` |

**여기서 굳은 계약 다섯** ★

- **버전을 올리지 않았다 — 처음으로 그 규칙을 실제로 지킨 자리다.** `grainAt` 하나가
  더해질 뿐 기존 필드의 뜻이 안 바뀌었다. 되접기는 **없으면 0**이고, **0은 「아직
  정산한 적 없다」**라 첫 정산이 도장만 찍고 지나간다 — meta는 시계를 못 읽어
  마이그레이션이 「지금」을 찍어 줄 수 없고, 0을 1970년으로 읽으면 옛 계정이
  접속하자마자 상한까지 찬다.
- **시계는 `App.tsx` 하나가 넣는다.** `syncGrain(profile, nowMs)`는 순수 함수이고
  (`bandForHour(h)`·`MatchRow.at`과 같은 규약), 지금 시각을 넣는 자리는 앱이 열릴 때
  한 번 + 1분마다. **도시 화면에서만 채우면 안 된다** — 군량을 읽는 자리가 넷
  (메인의 통계·병영의 잠금·편성의 참가비·도시)이다.
- **충전의 세 경계.** ① 상한에 붙어 있던 시간은 **사라진다**(쌓아 두면 참가비를 내는
  순간 몇 시간이 한꺼번에 돌아온다) ② 자투리는 **다음 접속으로 이어진다**(정산 시각을
  「지금」으로 찍으면 30분씩 들르는 사람은 영원히 한 톨도 못 받는다) ③ **시계가 뒤로
  가도 줄지 않고 정산 시각을 당기지도 않는다**(당기면 앞뒤로 흔들어 뽑을 수 있다 —
  앞선 시각은 진짜 시간이 따라잡을 때까지 둔다).
- **`applyCityUpgrade`만 `nowMs`를 받는다.** 증축은 **생산 요율이 바뀌는 자리**라
  올리기 전에 먼저 정산한다 — 안 하면 Lv1에서 논 세 시간이 Lv2 요율로 6이 된다.
  화면이 「증축 전에 `syncGrain`」을 잊지 않기를 기대하는 대신 규칙 층에 묶었다.
- **도시 전적은 판수로 센다 — 장수 전적의 합이 아니고 그것이 맞다.** 한 판에 3~5명이
  뛰므로 더하면 사람 수만큼 부푼다. C1이 계정 칸을 따로 둔 이유이고, 스모크가
  **「도시 6전 ≠ 그 장수 4전」**을 회귀로 박았다.

**밟은 지뢰 셋** ★

- **`.city`는 이미 메인 화면이 쓰고 있었다.** 도시 관리 화면에 `.city-info`를 붙였으면
  35쪽의 도시 이름 블록 규칙이 그대로 얹혔을 것이다 — `.panel`·`.ofc-head` 때와 같은
  자리라 **시작부터 `.cty-`로 갈랐다.** 「도시」라는 뜻이 같아도 CSS에는 뜻이 없다.
- **GDD §5의 증축 자재 표가 엑셀과 달랐다** — GDD `30/40/50…`(누적 370), 엑셀·`city.json`
  `10/15/20/25/30/35/40/50`(누적 **225**). §4.3 레벨업 카드 표와 **똑같은 종류**이고,
  코드는 처음부터 `city.json`을 읽고 있어 **동작은 멀쩡했다.** 엑셀이 정본이다.
- **작업 계획의 완료 조건 한 줄이 틀려 있었다** — 「도시 전적 합 **=** 장수 전적 합」.
  C1이 계정 칸을 따로 둔 이유가 **같을 수 없다**는 것인데, 계획 문서에만 반대로 적혀
  있었다. 그대로 믿고 구현했으면 계정 칸을 장수 합으로 만들었을 자리다.
  → **교훈: 계획 문서의 완료 조건도 낡는다.** 코드와 회귀가 정본이고, 어긋나면
  회귀(`records.test.ts`)를 먼저 본다.

### 부대 편성 · 배치 프리셋 (2026-08-18) — 트랙의 E · 저장 형식은 필드 둘

pptx **42·43쪽**. 룰 엔진은 **내보내기 한 줄만** 건드렸다(`defaultDeployPos`).
사양은 GDD **§5 · §8.6**, 경위는
[`history/2026-08-18_부대편성_배치프리셋.md`](history/2026-08-18_부대편성_배치프리셋.md).

```
v3 그대로 + PlayerProfile.squads[]    저장된 부대 (상한 = 도시 레벨)
         + PlayerProfile.squadSeq     다음 부대 id. 지운 번호를 다시 쓰지 않는다
         + RosterPick.level?          하향 「상한」. 없으면 보유 레벨 그대로
```

| | 붙은 것 | 파일 |
|---|---|---|
| 1 | **판정과 CRUD** — `validateSquad`(=`validateRoster`를 감싼다) · `squadRow` · `squadPower` | `meta/src/squads.ts` (신규) |
| 2 | **레벨 하향이 걸리는 자리 하나** — `pickLevel` · `toRosterEntries` | `meta/src/roster.ts` |
| 3 | **부대 개수 상한** — `squadCap()` + `city.json`의 새 열 | `meta/src/city.ts` · `tools/extract_data.py` |
| 4 | **배치 프리셋** — `squadDeployment` · `defaultSquadCells` · `isDeployable` | `meta/src/squads.ts` |
| 5 | **화면 넷** — 목록 · 이름/모드 · 편성(수정=신규) · 배치 | `screens/Squad*.tsx` (신규) |
| 6 | **이력의 `mySquad`** — 「저장된 부대」 줄을 지나 실린다 (F가 [출정하기]로 옮겼다) | `screens/BattleScreen.tsx` |

**여기서 굳은 계약 다섯** ★

- **부대의 레벨은 「상한」이지 「스냅샷」이 아니다** (2026-08-18 기획자 확정).
  전투력을 낮춰 약한 상대와 붙기 위한 눈금이고, **그 레벨의 능력치·책략은 성장
  스택에서 그대로 꺼낸다**(`growthUpTo`의 `slice`). 그래서 저장할 것이 숫자 하나뿐이고
  하향 Lv5가 진짜 Lv5와 **완전히 같다**.
- **보유 레벨보다 크면 조용히 눌러 담는다.** 재설계로 Lv9가 Lv1이 되어도 부대가
  죽지 않고, **「저장된 부대는 언제나 `createBattle`을 통과한다」**가 계약으로 선다
  (2026-08-04의 「편성 검증을 통과하면 룰 엔진도 받아들인다」의 확장). 반대로
  **장수가 계정에서 빠지면 말한다** — 눌러 담을 수 없는 종류다.
- **배치 프리셋은 위치 배열이 아니라 기물을 키로 든다.** 구성을 고치면 위치 배열은
  조용히 어긋나고, 엔진의 `UnitId`도 `{진영}-{기물}`이라 그대로 옮겨진다.
  **어긋나면 던지지 않고 `null`** — 기본 배치가 깔리고 사람이 30초 동안 고친다.
- **3v3과 5v5를 목록에서 묶음으로 가른다.** 계수가 달라 값 범위가 겹치는데
  (3v3 209~1304 · 5v5 253~1302) 나란히 놓으면 견주게 된다. `battlePower()`가
  계산이 섞이는 것은 막지만 **눈이 섞이는 것은 화면만 막을 수 있다.**
- **「기본 배치로」는 엔진이 세우는 그 함수다** — `defaultPlacement`를 `state.ts`로
  옮겨 `defaultDeployPos`로 내보냈다. 화면이 「5×5의 중앙」을 다시 적으면
  배치 구역 규칙이 바뀌었을 때 **미리보기만** 조용히 어긋난다.

**밟은 지뢰 넷** — §6에 넷 다 적었다. 요지는
① **`.foot`은 배경 화면에서 쓰면 안 된다**(`margin-top: auto`가 서로를 밀어낸다) ·
② **검사가 「기본값과 같은 값」을 봐서 아무것도 확인하지 않고 있었다** ·
③ 새 기능이 옛 스모크의 **말없는 전제**를 깼다 ·
④ 기물 단추는 더블클릭이 「빼기」라 `dblclick`으로는 안 들어간다.

> ~~**`RosterScreen`의 「저장된 부대」 줄은 임시 통로다.**~~ → **F가 흡수했다**
> (2026-08-18). [출정하기]로 옮겨 가며 **화면째 사라졌다** — 즉석 편성으로 출전하는
> 길을 두지 않기로 정해서다. **북군(P2) 프리셋은 저장만 되고 아직 안 쓰인다**(오프라인은
> 언제나 `humanSide: 'P1'`) — `squadDeployment`가 이미 진영을 받으므로 온라인에서
> 손댈 것이 없다.

### 출전 · 매칭 + 문 통합 (2026-08-18) — 트랙의 F · 저장 형식은 그대로

pptx **45쪽**. 룰 엔진은 **안 건드렸다.** 사양은 GDD **§6.1 · §7.1 · §8.7**, 경위는
[`history/2026-08-18_출전매칭_문통합.md`](history/2026-08-18_출전매칭_문통합.md).

```
저장 형식은 v3 그대로이고 **필드도 늘지 않았다** — 이 트랙에서 유일하다
```

| | 붙은 것 | 파일 |
|---|---|---|
| 1 | **거절 판정** — `MATCH_DECLINE_GRAIN` · `canStartMatch` · `canDeclineMatch` · `declineMatch` | `meta/src/match.ts` (신규) |
| 2 | **AI 상대를 전투력으로** — `makeAiOpponent`. 옛 `makeAiPicks`(등급 점수)를 버렸다 | 〃 |
| 3 | **온라인 탐색 이음매** — `searchOnline`. Colyseus가 이 함수만 갈아 끼운다 | `client/src/meta/matchmaking.ts` (신규) |
| 4 | **화면** — 구성 · 부대 고르기 · 최소군량 안내 | `screens/SortieScreen.tsx` (신규) |
| 5 | **화면** — 매칭 세 상태 · 다시 찾기 · 전투준비 | `screens/MatchScreen.tsx` (신규) |
| 6 | **이력의 빈 칸 둘** — `opponentId` · `theirSquad` | `screens/BattleScreen.tsx` |
| 7 | **`RosterScreen.tsx` 삭제** (257줄) | — |

**여기서 굳은 계약 여섯** ★

- **병영의 문은 셋이다** — `[부대 편성]` · `[출정하기]` · `[튜토리얼(잠김)]`.
  **출전지 화면은 두지 않았다** — 문 통합으로 살아 있는 단추가 하나뿐이 되면
  「자리만 만든 것」이 된다. 참여 인원은 [출정하기] 안의 첫 걸음이다.
- **저장하지 않은 편성으로 출전하지 않는다** (기획자 확정). 「직접 편성 루트」는
  **[새 편성 만들기]로 보내는 것**이고, 구성 화면은 42쪽의 `SquadEditScreen` 하나다.
  둘이면 판정이 두 벌이 되고 나중 기능이 한쪽에만 붙는다. 대신 **막다른 길을 두지
  않는다** — 부대가 없으면 목록 자리가 그렇게 말한다.
- **AI 상대도 `battlePower`가 눈금이다.** 등급 점수는 **레벨을 못 봤다**(Lv9 S급 셋
  1211이 Lv1 S급 셋 797을 만났다). 온라인이 `MATCH_BAND`로 고르는 것과 같은 자라야
  「바뀌는 것은 상대가 사람인지뿐」이 성립한다. **도달 범위 밖이면 던지지 않고 가장
  가까운 값**으로 물러나며 잰 값을 그대로 싣는다 — 막으면 「전투력을 올려서 출전을
  못 한다」가 된다. 실측 최대 오차 3v3 **11** · 5v5 **20**(구간 20).
- **참가비는 [전투준비]에서만 나간다.** 거절 −1이 참가비를 내기 **전의** 잔여 군량으로
  갈리므로(§5-16), 매칭만 하고 뒤로 가면 한 톨도 안 나간다.
- **거절 판정도 안내문도 규칙이 낸다** — `canDeclineMatch()`. 화면이 `grain > 3`을
  다시 적으면 참가비가 바뀌었을 때 조용히 어긋난다. 판정은 `grain > cost`가 아니라
  **`grain − MATCH_DECLINE_GRAIN >= cost`**로 적어 패널티가 바뀌어도 경계가 따라온다.
  **거절은 온라인에만 있다** — AI는 상대를 「생성」하는 것이라 거절당할 상대가 없고,
  그때는 [다시 찾기]가 없는 **이유를 적는다.**
- **`BattleScreen`은 상대를 만들지 않는다** — 매칭이 정한 상대를 **값으로** 받는다.
  예전에는 전투 화면이 AI를 다시 뽑아, 「찾았습니다!」가 보여 준 상대와 실제로 싸우는
  상대가 **같은 시드로 두 번 만든다는 데 기대고** 있었다. 온라인이 붙어 서버가 상대를
  줄 때도 이 화면은 안 바뀐다.

**밟은 지뢰 다섯** — §6에 넷을 적었다. 요지는
① **「아직 안 붙은 갈래」에 걸린 검사는 도는 적이 없다**(온라인이 늘 빈손이라 거절
검사가 실행조차 안 됐다 — 흉내 통로를 함께 만들었다) ·
② **가운데만 재는 회귀는 끝을 놓친다**(전 구간을 훑자 바닥에서 31이 벗어났다) ·
③ 촘촘한 회귀가 `npm test`를 14초로 만들었다(후보 표 캐시 + top-k 훑기로 0.42초) ·
④ 옛 스모크가 **「군량은 20에서 시작한다」**를 말없이 전제하고 있었다 ·
⑤ `.btn .lbl`에 `nowrap`이 없어 **단추의 이름이 두 줄로 쪼개졌다**(700px에서만 보였다).

> **검사가 실패하는 상태를 두 번 만들어 봤다** — 규칙(`canDeclineMatch`)과
> 화면(`MatchScreen`의 조건)을 **각각** 무력화해 스모크가 서로 다른 줄로 잡는 것을
> 확인했다. 판정과 그리기를 갈라 뒀으면 검사도 둘 다 서야 한다.

> **개발용 통로가 다섯이 됐다** — 「카드 +5」·「금화 +10」·「재료 +10」에
> **`?match=fast`·`?match=online`**이 더해졌다. 상점·온라인이 붙을 때 함께 지운다.

### 전송 층 분리 (2026-08-19) — Colyseus 트랙의 **H1** · 서버 코드 0줄

경위는 [`history/2026-08-19_전송층_분리.md`](history/2026-08-19_전송층_분리.md),
사양은 GDD **§3.9 · §10 · §12**. **동작은 한 글자도 안 바뀌었다** — 바뀐 것은
「누가 상태를 만드는가」다. 스모크 둘 다 **갱신 없이** 통과한 것이 그 증거다.

```
예전   [Playback] ──apply()/advanceTime()/takeTurn()──▶ [룰 엔진]
지금   [Playback] ──의도──▶ [BattleTransport] ──▶ 판정 주체
            ◀──「이렇게 됐다」(ServerMsg)──      (로컬: 룰 엔진 · 온라인: 서버)
```

| | 붙은 것 | 파일 |
|---|---|---|
| 1 | **전송 이음매** — `BattleTransport` · `ServerMsg` · `toWire`/`applyWire` · `LocalTransport` | `client/src/battle/transport.ts` (신규) |
| 2 | **재생기 개편** — 상태를 만들지 않고 **받은 통을 큐에서 꺼내 소화**한다 | `client/src/battle/playback.ts` |
| 3 | **시간 상수의 단일 출처** | `rules/src/timing.ts` (신규) |
| 4 | 판정 주체를 밖에서 받는다 | `battle/boot.ts` · `screens/BattleScreen.tsx` · `screens/DemoBattle.tsx` |
| 5 | **회귀 17건** | `client/test/transport.test.ts` (신규) |

**여기서 굳은 계약 여섯** ★

- **전선에는 「로그를 뺀 스냅샷 + 이벤트」를 싣는다.** 실측 — 스냅샷 3v3 **2,408 B** ·
  5v5 **3,705 B**인데 로그를 실으면 27,743 · 55,089 B이고 **판 길이에 비례**한다
  (전선의 `O(턴²)`). 뗀 로그는 받는 쪽이 이벤트를 이어 붙여 되만들고, 그게 정확히 같은
  것은 엔진의 `commit()`이 보장한다(`이전 로그 ++ events === 다음 로그`).
  **이벤트만 보내는 길은 안 골랐다** — 리듀서가 없고 만들면 `apply()`의 두 번째 구현이다.
  **재접속도 이 결정에 딸려 풀린다.**
- **`Playback`은 상태를 만들지 않는다.** 내 의도·상대의 수·시간 진행·마감 **넷이 전부**
  온라인에서 「도착한다」로 바뀌므로 이음매 하나로 모았다. 계약은 `send(intent)`와
  `ready()` 둘이고, **`ready()`는 로컬만 듣는다** — 뜻이 「진행시켜 달라」가 아니라
  **「나는 밀리지 않았다」**이고 서버는 어차피 기다려 주지 않는다.
- **마감은 「남은 ms」로 오고 단계가 바뀔 때만 다시 잰다.** 절대시각을 실으면 시계
  어긋남이 카운트다운에 그대로 나오고, 통마다 새로 재면 배치 중에 기물을 옮길 때마다
  30초가 되살아난다.
- **전투 단계의 실시간 제한은 `rules/timing.ts` 하나가 정한다.** 재는 주체가 클라와
  서버 **둘**이 되면 상수가 두 벌일 때 **어긋나도 화면이 아무 말도 안 한다.**
  **값만 두고 시계는 안 들인다.**
- **연출이 도는 동안에는 단계가 굳어 있다.** `phase`는 화면이 읽는 값이라 거짓말을
  하면 안 된다 — 「연출이 도는 2.6초 동안 벌써 `advancing`」을 스모크가 잡았다(§6).
- **Colyseus는 방·전송·재접속으로만 쓴다.** `@colyseus/schema`를 쓰면 상태가 두 벌이
  되고, **Node 타입 스트리핑이 데코레이터를 못 넘긴다**(§6).

**밟은 지뢰 다섯** — §6에 넷을 적었다. 요지는 ① **교훈을 알면서 「도는 적이 없는
검사」를 새로 만들었다**(회귀 안의 가짜 판정 주체로 고쳤다) · ② **상태 기계를 옮길 때
「언제」를 안 옮겼다**(스모크만 잡았고 단위 회귀로 내렸다) · ③ **타입 스트리핑은 타입만
지우면 되는 문법만 받는다** · ④ 임계값은 재고 정했다(스냅샷 1.54배 대 로그 21배) ·
⑤ **재진입 잠금은 깨도 안 잡힌다** — 주장하지 않고 사실을 적었다.

> **검사가 실패하는 상태를 다섯 번 만들어 봤다.** 넷은 잡혔고 하나(연출 중 큐)는
> **처음에 안 잡혀서** 통로를 함께 만든 뒤에야 살았다.

### 다음 작업 — Colyseus **H2**: 대전 서버와 방

**`playback.ts`도 `transport.ts`의 계약도 다시 만들지 않는다.** H1이 끝나 채울 자리는
**`BattleTransport` 구현 한 벌**과 그것이 이야기할 서버다.

시작점으로 볼 것:

0. **`client/src/battle/transport.ts`의 `OnlineTransport` 자리** — `LocalTransport`와
   같은 계약을 채운다. `ready()`는 아무것도 하지 않고, 통은 서버가 밀어 준다.
   그 모양은 이미 **회귀 안의 `ScriptedTransport`**가 흉내 내고 있다.
1. **`client/src/meta/matchmaking.ts`의 `searchOnline()`** — F가 남긴 이음매.
   지금은 30초 뒤 `null`을 준다. 방 참가로 바꾸면 `MatchScreen`은 한 글자도 안 바뀐다 —
   **상대를 값(`MatchOpponent`)으로 넘기는 계약**이 이미 서 있다.
2. **`packages/server/`** (신규) — `room-logic.ts`(Colyseus를 import하지 않는 순수 층,
   시각을 인자로 받는다) + `BattleRoom.ts`(얇은 껍데기) + `queue.ts` + `clock.ts`.
   제어 20초·배치 30초·재접속 유예·거절 재대기는 **브라우저 둘을 띄워야만 보이는 종류**라
   순수 층에 있어야 `npm test`가 잡는다 (되접기를 meta에 둔 것과 같은 이유).
3. **`battle.ts`의 `engine`** — `RulesEngine` 계약 구현체. 서버가 이걸 그대로 쓴다.
4. **`BattleState.controlStartedAtMs`** — 서버가 주입한다. 서버는 20초를 **재되 먼저
   움직이지 않는다** — 상대에게 `[차례 넘기기]`가 뜨고, `forceSkipTurn`이 왔을 때
   정말 지났는지 검증한다.
5. **`rngCursor`** — 서버가 유일한 소비자라 갈릴 수 없다. 스냅샷마다 단조 증가를
   확인하고, 전투 끝에 `seed`+최종 `cursor`를 남겨 리플레이 근거로 둔다(GDD §10).

H2에서 처음 도는 것들: **`waiting` 단계**(AI에게는 기다릴 것이 없어 한 번도 안 돌았다) ·
**제어 20초 마감** · **북군(P2) 배치 프리셋**(`squadDeployment`가 이미 진영을 받는다) ·
**「다시 찾기」가 진짜 다른 사람을 물어 오는 것**.

H2에서 정해 둔 것(작업 계획 §5-58~64): 로컬 개발만 · 로그인은 뒤 · 매칭 구간은 안 넓힌다 ·
거절은 두 사람의 사건 · **참가비는 방이 열린 순간에 확정**([전투준비] 뒤에
「상대를 기다리는 중」이 한 걸음 붙는다) · **이탈의 갈림길은 「배치를 마쳤는가」**.

**이탈 처리** (2026-08-19 확정 · GDD §3.9 · §6.1 · §8.7) — 「사라졌다」는 **재접속 유예를
넘겨 안 돌아왔다**는 뜻이다.

| | 사라진 쪽 | 남은 쪽 |
|---|---|---|
| **한쪽만 · 배치 중** (`deploy` · `waiting`) | 참가비를 **잃는다** | 참가비를 **돌려받는다.** 전적도 보상도 없다 |
| **한쪽만 · 정찰 이후** (`scout` 뒤) | **항복과 같다** — 패배 보상·전적 | **승리** 보상·전적 |
| **양쪽 다** (단계와 무관) | 양쪽 다 **돌려받는다.** 전적도 보상도 없다 | |

**끊는 것이 거절보다 싸지면 안 된다 ★** — 사라진 쪽에게 환불하지 않는 **진짜 이유**다.
상대를 물리는 정식 통로가 「다시 찾기」(군량 −1)인데, 참가비를 낸 뒤 **끊어서 돌려받을
수 있으면 같은 회피를 군량 0으로** 할 수 있어 **거절 패널티가 통째로 무력해진다.**
지금은 참가비(3v3 3 · 5v5 5)를 잃으므로 **언제나 거절이 더 싸다.** 이 문장을 잃으면
언젠가 「사라진 쪽도 돌려주는 게 인정 아닌가」로 되돌려진다.

**갈림길이 엔진에 이미 있는 값이다** — `state.ready[side]`이고 `deploy → scout` 전이가
정확히 그 순간이라, 「어디까지가 본 게임인가」를 두 군데서 적을 일이 없다. **정찰 이후
갈래에는 새 규칙이 없다** — 서버가 사라진 쪽 대신 `{ t: 'surrender' }`를 적용하면
`outcome: 'surrender'`가 그대로 나오고 보상표(GDD §6.4)가 그대로 걸린다.
**배치 중 이탈에 승리는 주지 않는다** — 주면 「상대를 끊게 만드는 것」이 이득이 되고,
성립한 적 없는 전투의 전적이 남는다.

**양쪽이 다 사라졌으면 지킬 사람이 없다** — 대개 시스템 장애이고, 아니라면 싸우지 않기로
합의한 것으로 볼 수 있다. **기다리느라 불편을 겪은 사람이 없으므로** 양쪽 다 돌려받고,
짜고 둘이 끊어도 **얻는 것이 없다**(대전을 안 한 것과 같아진다).
**판정 시점은 「기다린 사람이 있었는가」로 갈린다** — 먼저 끊긴 쪽의 유예가 끝나는 순간
상대가 붙어 있으면 **곧바로** 한쪽-이탈로 끝내고, 그때 상대도 끊겨 있으면 둘의 유예가
모두 끝날 때까지 둔다.

**「끊긴 것」과 「가만히 있는 것」은 다르다** (2026-08-19 확정 · GDD §3.3 · §3.9) —
붙어 있는데 아무것도 안 두는 경우다. 배치·정찰은 마감이 있어 서버가 넘기므로 **멈추는 곳은
제어 단계 하나**이고, 거기서는 절대시간이 멈춰 **`time 6000` 무승부 상한에 영원히 못 닿는다.**

| | 무엇이 끝내나 | 결말 |
|---|---|---|
| **한쪽만 손을 놓았다** | 상대가 **`[차례 넘기기]`를 3번** 누른다 | 누른 쪽 **승리** — 보상·전적은 항복과 같다 |
| **양쪽 다 손을 놓았다** | **방 유휴 상한 2분** | **양쪽 환불 · 전적도 보상도 없다** |

**겹치지 않는다** — 누르는 사람이 있으면 방은 유휴가 아니고, 아무도 안 누르면 3번에
닿을 일이 없다. **횟수는 누적**이고 한 번 누르려면 상대가 20초를 통째로 흘려보내야 하므로
**3번은 최소 60초의 무응답**이다. **화면에 「(2/3)」로 보여야 한다** — 누르면 승리에
가까워지는 단추가 아무 말도 안 하면 안 된다. 결말은 `outcome: 'surrender'`로 적어
**엔진에 새 결말이 늘지 않는다**(이탈 처리와 같은 장치를 세 번째로 쓴다).

> **재접속 유예 = 1분** (2026-08-19 확정) — **3번 규칙이 이 값을 정한다.** 3번은
> **내 제어 창 세 번**이라 `20초 × 3 = 60초`가 **하한**이고 사이에 상대의 턴들이 끼어
> 실제로는 더 길다. 유예를 1분으로 두면 **돌아온 사람이 언제나 3번에 닿기 전에 도착한다.**
> 매칭 대기 상한(`waiting` 60초)과 **같은 수인 것이 우연이 아니고**(같은 기다림을 잰다),
> 유휴 상한 120초보다 짧다는 조건도 만족한다.
>
> ⚠ **끊긴 사람에게는 자동 `[준비완료]`를 걸지 않는다** (파생 규칙). 배치 마감 30초의
> 자동 준비는 **자리에 있는 사람**을 위한 편의다 — 끊긴 사람에게 걸면 `+30초`에 `ready`가
> 서고 `+60초`에 유예가 끝날 때는 이미 정찰 이후라, **배치 중 이탈이 언제나 항복으로
> 판정되어 이탈 표의 첫 줄이 영원히 안 돈다.** 「도달할 수 없는 갈래」가 규칙 쪽에
> 생기는 자리다.
>
> **이 규칙은 룰 엔진을 건드린다 — 이 트랙에서 처음이다.** `BattleState`에 진영별
> 「차례 넘기기 횟수」가 늘고 `validate`/승패 판정이 그것을 본다. **저장 형식은 그대로다**
> (`BattleState`는 저장되지 않는 런타임 상태다). **H1에서 구현하지 않은 이유**는 발동
> 조건(제어 20초 초과)이 `controlStartedAtMs`를 요구하고 그 값은 서버가 주입하기 때문이다 —
> 서버 없이 만들면 「도달할 수 없는 갈래」가 하나 더 생긴다.

미리 보이는 지뢰 둘: ·
**같은 브라우저의 두 탭은 `localStorage`가 같아 계정이 하나다**(개발용 접미사가 필요하다) ·
**`?match=online` 흉내는 진짜와 갈릴 수 있다**(서버가 붙으면 지우고 스모크가 진짜 서버를 띄운다).

UI 쪽은 이미 서버 권위를 전제한다 — 버튼 활성 여부도 조준 후보도 전부 `validate()`에 물어서
만들었기 때문에 **판정 주체가 서버로 바뀌어도 화면 로직은 그대로다.**

### 기획자에게 물어야 할 것

> **지금 트랙(PPT 37~45)의 열린 질문은 [`docs/작업계획.md`](docs/작업계획.md) §4에 있다.**
> 아래 표는 그 트랙 **밖의** 것들이다 — 에셋·음성·밸런스처럼 언젠가 물어야 할 것들.

| 항목 | 언제 |
|---|---|
| ~~A급↔B급 승률 격차 12%p가 의도인지~~ | ✅ **의도됨** (2026-08-12) |
| ~~B↔C가 붙어 있는 것이 진짜인지~~ | ✅ **진짜다** — 책략을 붙여도 1.7%p (2026-08-12) |
| ~~성장의 `AT` 몰빵이 등급 격차를 반토막 낸다~~ | ✅ **`AT` 성장을 0.5로 내렸다** (2026-08-12) |
| **`assets/map/지형_돌.png`을 어떻게 할지** — 엔진의 지형은 `fire`/`water`/`holy` 셋뿐이라 놓을 자리가 없다. 새 지형(장애물?)인지, 다른 용도인지 | 지형을 더 붙일 때 |
| ~~`assets/Audio/`의 배경음악~~ | ✅ **화면마다 한 곡으로 붙였다** (2026-08-14) |
| **`effects/`가 비어 있다** — 효과음을 넣을 것인지, 어떤 것부터인지 | 효과음을 붙일 때 |
| **고유기술 음성이 5개 언어 × 3곡뿐이다** — 40종을 다 채울 것인지, 언어는 어떻게 고를지 (`KR/가후지잭`은 오타로 보인다) | 음성을 붙일 때 |
| ~~음소거 단추를 화면 어디에 둘지~~ | ✅ **환경설정 팝업에 넣었다** (2026-08-15). `M` 키도 그대로 |
| **다국어 문구** — 영어·포르투갈어·일본어·중국어. 틀은 있고 표만 비어 있다 | 별도 세션(기획자 예고) |
| **계정 생성·로그인** — SSO 포함. 지금 「입장」은 곧바로 들어간다 | 별도 세션(기획자 예고) |
| **`eachBackground` 넷째 칸(게시판)을 어디에 이을지** | 기획자가 알려주기로 함 |
| **장터에 무엇을 둘지** — 상점·가챠(GDD §6)가 그대로 들어오는가 | 메타/경제 단계 |
| **지력의 값어치가 무력에 비해 낮다** — B급을 올릴지, 지력 쓰임새를 늘릴지 | 밸런스 조정할 때 |
| **E급(헌제)이 책략전에서 35.8%로 무너진다** — 그대로 둘지 | 밸런스 조정할 때 |
| 1:1 대전의 기물 구성 | 1:1 모드 만들 때 |
| 레벨업 실패 시 카드 처리 | 메타/경제 단계 |
| ~~AI 대전 보상 (온라인 대비)~~ | ✅ **온라인과 완전히 같다** (2026-08-18) |

애매한 판정은 **모아서 한 번에** 묻는 편이 효율적이었다 (S급 30종을 12개 질문으로 압축).

### 고유기술을 붙이며 생긴 구조 ★

**대부분의 스킬은 "표식만 걸고" 실제 동작은 엔진 훅이 한다.** 스킬 하나가 코드 한 덩어리가
아니라, 데이터가 상태를 걸고 파이프라인이 그 상태를 읽는 식이다. 새 스킬을 붙일 때도 이 결을 따른다.

| 훅 위치 | 무엇을 보는가 |
|---|---|
| `state.ts` `resolveAttack` | 크리티컬 확정, 오라, 대신받기, 즉사, AT 누적, 반격, 전향 |
| `state.ts` `damageUnit` | 부활(화용도), 사후 예약(유언계책) |
| `state.ts` `legalTargetsFor` | 사거리 무시(백보천양), 무적, 지정 강제(소패왕전), 조종 |
| `battle.ts` `applyTick` | 도트·지형·SP·**예약 효과**(pending) |
| `battle.ts` `endTurn` | 지속형 WT 보정(병귀신속·신속) |

주의할 것 셋:

- **`UnitState.side`는 readonly가 아니다.** 「삼고초려」로 적이 아군이 된다.
  id 접두사(`P2-Queen`)는 생성 시점의 진영이라 전향 후에는 일치하지 않는다 — 진영은 반드시 `side`로 본다.
- **오라는 시전 시점에 고정하지 않는다.** 시전자에게만 표식을 두고 피해 계산 때 거리를 다시 잰다.
  이걸 "대상들에게 상태를 뿌리는" 방식으로 바꾸면 흩어져도 효과가 남아 오라가 아니게 된다.
  → **그래서 영향받는 쪽에는 흔적이 없다.** "내 공격력이 왜 절반이지?"에 답할 방법이 없어서
  엔진에 `aurasOn(state, unitId)`를 두고 화면이 그걸 물어 「◎ 여포 2칸」 배지를 띄운다.
  판정(`auraApplies`)도 같은 함수를 쓴다 — 둘이 갈리면 "표시에 없는데 걸리는" 어긋남이 난다.
- **`charges`가 붙은 상태는 그 행동이 실제로 소모해야 한다.** 여포 「인중여포」의 자유이동은
  데이터에 `charges: 1`로 적혀 있었는데 **이동이 횟수를 깎지 않아** 사실상 지속형으로 돌았다
  (기획자가 플레이 중 발견, 2026-08-04). 지금은 **기물 마스크 밖으로 갔을 때만** 깎는다 —
  원래도 갈 수 있던 한 칸을 움직였다고 「어디든 한 번」이 날아가면 함정이다.
- **스크립트만 있고 `effects`가 빈 스킬은 조준 대상을 선언할 수 없다.**
  `resolveTacticTarget`이 `effects`에서 조준 규약을 읽기 때문이다.
  소패왕전이 대상 쪽 표식을 굳이 DSL로 거는 건 그래서다.

### 밸런스 관찰 대상 (GDD §11에 전체)

구현하면서 확인할 것. **원본을 바꾸지 않았고, 검토 의견이다.**

1. **부저추신(B, SP4)** — SP 4를 써서 적 SP를 1 깎는다. 교환비가 불리하다.
2. **크리티컬 공식의 스윙** — `30 + 무력차`. 관우(98) vs 조식(15)이면 113% → 항상 발동.
3. **Queen** — 이동 최광역(24칸)인데 공격이 상하 1칸뿐.
4. **Knight** — 위협 범위 25로 King과 동률 최하. 유일한 강점이 경로 무시(도약).
5. **A급 「용맹전진」 14명** — A급 40명 중 35%가 같은 스킬.
6. **헌제(E급)** — 능력치 1/1/1에 SP 7로 `time 990` 무적. **King 지정은 금지로 확정**(GDD §4.1).
   5000판 실측 45.8%(표본 120회)로 D급(40.7%)보다 높다 — 능력치가 1/1/1인데도 그렇다.
   표본이 작으니 헌제만 고정 편성해 따로 볼 것.
7. **탈진·질병 영구 지속** — 해제 수단이 「결계」 하나뿐. 질병은 HP 10 유닛을 10턴에 죽인다.

---

## 8. 주의사항

- **`packages/data/generated/`를 직접 수정하지 말 것.** 엑셀을 고치고 `npm run extract`.
- **룰 엔진에서 `Date.now()` / `Math.random()` 금지.** 전투 재현성이 깨진다. 난수는 `BattleState.rng` 경유.
- **클라이언트는 판정 결과를 보내지 않는다.** `Intent`(의도)만 보내고 서버가 검증→적용→`BattleEvent[]` 브로드캐스트.
- **원래 있던 DOS 게임 폴더는 건드리지 말 것.**
  `c:\Users\user\Documents\Hackers_IELTS_Listening_Basic_MP3_free\DGGL\Games\SamHero\` 에
  삼국지 영걸전(KOEI, 1993) 원본이 있고 **DGGL 런처가 직접 참조**한다.
  `autoexec.conf`가 `HERO.COM`을 경로 없이 실행하므로 파일을 옮기면 실행이 깨진다.
  2026-07-31에 이 프로젝트를 그 폴더에서 분리했고, **이제 아무 의존 관계가 없다.**

---

## 9. 파일 지도

| 경로 | 내용 |
|---|---|
| `HANDOFF.md` | 이 문서 — **현재 상태의 정본** |
| `README.md` | 폴더 구조, 명령, 파이프라인 설명 |
| `Design/GDD.md` | **구현 기준 문서** — 13장. 전투/캐릭터/메타/데이터모델/밸런스/미결 |
| `history/*.md` | 세션별 기록 — **왜 그렇게 정했고 어디서 넘어졌는가**. 현재 상태는 안 적는다 |
| `docs/삼국지 약식체스.xlsx` | 원천 데이터 6시트 (장수·스킬·책략·기물·환경·**오라매핑**). 읽기 전용 |
| `docs/삼국 약식 체스.pptx` | 원천 기획 (책략·경제·UI의 출처). 읽기 전용 |
| `docs/PROMPT.md` | 캐릭터 카드 생성용 Gemini 프롬프트 (2단계 티칭 방식) |
| `assets/Chars/*.png` | 초상화 260장 (440×540 RGBA, **배경 없음**). 보드 타일의 원본 |
| `assets/CharsInBattle/*.jpg` | 수묵화 흉상 260장 (~808²). 하단 패널·정보 팝업의 원본 |
| `assets/CharsAction/*` | **액션 띠 260장** (1535×310, 가로 5칸). 보드 타일의 원본 |
| `assets/SpecialStatus/*.png` | **시각 효과 30장** — 지속형 링 `1`~`23` · 일회성 `A`~`G`(2×2) |
| `assets/map/지형_*.png` | **지형 그림** 256² — `불`·`물`·`성채`가 화계·수계·성지. `돌`은 아직 엔진에 없다 |
| `assets/map/chessmap.png` | **판 지도** 750². 체스판 아래에 깔린다 |
| `assets/map/person.png` | **벽보** 1440×2912. 카드 액자(9분할)와 산수 배경의 원본 |
| `assets/Backgrounds/*_big.jpg` | **화면 배경 4장** — 간판·도시(시간대 3칸) · 자리(궁궐/병영/장터/미정 4칸). `_big`이 없으면 같은 이름의 PNG로 물러난다 |
| `assets/Audio/bgm/*.mp3` | **배경음악 6곡**. 화면마다 한 곡 (§5) |
| `assets/Audio/effects/` · `Specialskills/` | 효과음(비어 있다) · 고유기술 음성(5개 언어 × 3곡). **아직 연결 안 함** |
| `tools/extract_data.py` | 엑셀→JSON 추출 + 검증. 의존성 0. **효과 DSL의 단일 출처**. 에셋 대조도 여기서 |
| `tools/crop_chars.py` | 카드→개별 초상화 절단. **입력이 백업에 있다** — 기본값으로 돌리지 말 것 |
| `tools/remove_char_background.py` | 초상화 배경 제거. **적용 완료** — 결과가 지금의 `assets/Chars/`다 |
| `tools/build_portraits.py` | 초상화 → 웹용 96×120 (`npm run portraits`) |
| `tools/build_action_sheets.py` | 액션 띠 → 스프라이트시트 110²×5 (`npm run actions`) |
| `tools/build_status_fx.py` | 시각 효과 → 링 256² · 일회성 4칸 시트 (`npm run vfx`) |
| `tools/build_terrain.py` | 지형 그림 → 128² (`npm run terrain`). **한글 이름 → 지형 id를 잇는 유일한 자리** |
| `tools/build_frames.py` | 판 지도 · 카드 벽보 액자(9분할) · 산수 배경 (`npm run frames`) |
| `tools/build_audio.py` | 배경음악 → `public/bgm/` (`npm run audio`). **한글 이름 → 화면 id를 잇는 유일한 자리** |
| `tools/build_backgrounds.py` | 배경 띠 → 칸별 그림 (`npm run backgrounds`). **칸 순서 → id를 잇는 유일한 자리** |
| `tools/balance.ts` | 자동 대전 대량 실행 → 승률·결착시간 통계 |
| `tools/smoke_ui.ts` | 화면 연동 스모크 (좌표→Intent→판정, 교착 회귀) |
| `tools/shot.ts` | 스크린샷 |
| `packages/data/generated/` | 자동 생성 JSON 10종 (커밋 대상) |
| `packages/data/src/index.ts` | 로더 + 조회 인덱스(`officerById`, `skillById`, …) |
| `packages/rules/src/types.ts` | **타입 계약** — Effect DSL, BattleState, Intent, FORMULA |
| `packages/rules/src/rng.ts` | 시드 PRNG (카운터 기반, 순수 해시) |
| `packages/rules/src/pieces.ts` | 기물 기하 — `legalMoves`, `attackCells`, `threatRange` |
| `packages/rules/src/state.ts` | 상태 원시 연산 — 조회·체력·사망·승패·공격 판정·**공격 미리보기** |
| `packages/rules/src/effects.ts` | Effect DSL 실행기 + 환술 판정 |
| `packages/rules/src/scripts.ts` | 서사형 고유기술 스크립트 (소패왕전·차동풍) |
| `packages/rules/src/battle.ts` | CTB 스케줄러 · 검증 · 의도 적용 · `engine` |
| `packages/rules/src/ai.ts` | 기준 AI + 자동 대전. 우선순위 6단계 — 책략은 5번 |
| `packages/rules/test/data.test.ts` | 데이터 정합성 회귀 12건 |
| `packages/rules/test/battle.test.ts` | 스케줄러·행동·재현성·**공격 미리보기** 회귀 39건 |
| `packages/rules/test/tactics.test.ts` | 책략 18종 회귀 23건 |
| `packages/rules/test/skills.test.ts` | 고유기술 시전 틀 + A/B/E급 10종 회귀 19건 |
| `packages/rules/test/skills-s.test.ts` | S급 30종 회귀 24건 (엔진 훅 집중) |
| `packages/rules/test/fixtures.ts` | 테스트 공용 픽스처 (러너가 직접 실행하지 않음) |
| `packages/client/src/main.tsx` | 진입점. URL 쿼리로 시드·모드·진영 지정 |
| `packages/client/src/battle/playback.ts` | ★ 이벤트 재생 레이어 — 온라인 대전에서 재사용 |
| `packages/client/src/battle/BattleScene.ts` | 보드·유닛 타일·입력·카메라 (판정은 하지 않는다) |
| `packages/client/src/battle/poses.ts` | 연출 타임라인 — 자세와 **카메라 큐**를 같은 커서 위에 |
| `packages/client/src/battle/camera.ts` | 100%/200% 큐 해석 + 부드러운 추적 (Phaser 없음) |
| `packages/client/src/battle/layout.ts` | 셀 96×120 좌표 변환 |
| `packages/client/src/battle/setup.ts` | 데모 편성 (편성 화면이 생기면 대체된다) |
| `packages/meta/src/` | **계정의 권위** — 프로필·편성·레벨업·보상 (순수 함수) |
| `packages/meta/test/meta.test.ts` | 메타 회귀 17건 — 경제 확정 셋을 여기서 지킨다 |
| `packages/client/src/screens/` | 메타 화면 (React) — 새 계정·메인·편성·결과 |
| `packages/meta/src/officers.ts` | **장수 일람의 규칙** — 검색·정렬·등급 요약·**AT 범위**. 화면은 부르기만 한다 |
| `packages/meta/test/officers.test.ts` | 장수 일람 회귀 13건 — 눈으로 못 보는 정렬·경계를 고정한다 |
| `packages/client/src/screens/OfficerListScreen.tsx` | 장수 일람 (pptx 37·38) — 표 6열 · 검색 · 정렬 4종 · 레벨업 Flag |
| `packages/client/src/screens/OfficerDetailScreen.tsx` | 장수 상세 (pptx 38) — 서사·전적은 **자리만** |
| `packages/client/src/screens/SkillModal.tsx` | 고유기술 팝업 (pptx 38) — 배너·효과·유래 자리 |
| `packages/client/src/screens/LevelUpScreen.tsx` | 레벨/스킬 관리 · 재설계 (pptx 39) — 관리 · 고르기 두 걸음. **숫자는 meta가 낸다** |
| `packages/meta/src/squads.ts` | **부대 편성 · 배치 프리셋** (42·43쪽) — `validateRoster`를 감싼다. 어긋난 배치는 `null`로 물러난다 |
| `packages/meta/test/squads.test.ts` | 부대 회귀 27건 — 상한 · 하향 = 진짜 · **저장된 부대가 `createBattle`을 통과하는가** |
| `packages/client/src/screens/SquadListScreen.tsx` · `SquadNameScreen.tsx` · `SquadEditScreen.tsx` · `SquadDeployScreen.tsx` | 부대 목록 · 이름/모드 · 편성(수정=신규) · 배치 프리셋 (42·43쪽) |
| `packages/meta/src/migrate.ts` | **저장된 계정을 지금 형식으로 되접는다** — 버리지 않는다. 멱등. 필드 추가는 여기 한 줄 |
| `packages/meta/test/growth.test.ts` | 성장 스택 회귀 29건 — 되접기 · 불변식 · **재설계(되감기)** · **레벨 하향**(E의 전제) |
| `packages/client/src/meta/storage.ts` | 계정 저장. **온라인이 붙으면 이 파일만 서버 API가 된다.** 되접기는 meta에 있다 |
| `packages/client/src/battle/boot.ts` | Phaser 켜고 끄기 + 로그에서 처치 수 세기 |
| `tools/smoke_meta.ts` | 메타 연동 스모크 — 계정→편성→전투→레벨업→저장 |
| `packages/client/index.html` | `#root` 하나. 프레임(1:2)은 React가 그린다 |
| `packages/client/src/style.css` | 화면 스타일. 전부 `rem`, 기준값은 `useFrameFit`이 준다 |
| `packages/client/src/ui/cardStrip.ts` | 판 위·아래 캐릭터 카드 — **카드 한 장 = 벽보 한 장**. 고유기술 3색 · 차례/퇴각 |
| `packages/client/src/ui/panelSlot.ts` | 커맨드/상태 패널의 사분면 — 가릴 것을 피해 대칭으로 |
| `packages/client/src/ui/focusToggle.ts` | 자동 포커싱 ON/OFF 토글 — 판 왼쪽 위 반투명 버튼 |
| `packages/client/src/ui/controlModal.ts` | 커맨드 패널 — 판 안 플로팅, 세로 5줄 · 시전 · 조준 |
| `packages/client/src/ui/hud.ts` | 상단 HUD 한 줄 — 절대시간 · 차례 · 북군/남군 SP · ⋯ |
| `packages/client/src/ui/systemLog.ts` | 판 **좌하단** 말풍선(최근 1줄) + 전체 기록 + 항복. 간격은 연출 길이가 정한다 |
| `packages/client/src/ui/prepPanel.ts` | 배치·정찰 패널 — 남은 시간 · 준비완료 · 전투 시작 |
| `packages/client/src/ui/eventText.ts` | `BattleEvent[]` → 대화 문장 (칸 이름 A~Y / 1~20) |
| `packages/client/src/ui/skillFx.ts` | 고유기술 연출 배너 2초 — 그동안 판이 멈춘다. 뒤에 일회성 애니를 잇는다 |
| `packages/client/src/ui/burstFx.ts` | 일회성 시각 효과 — 판 영역 한가운데 4프레임 (화면 좌표) |
| `packages/client/src/battle/visualEffect.ts` | **지속형 링 판정** — 상태→그림, 2초 스왑, 장수별 예외 셋 |
| `packages/client/src/battle/terrain.ts` | **지형 그림 표** — 지형→그림·크기. 링과 달리 **칸**에 붙는다 |
| `packages/client/src/audio/bgm.ts` | **배경음악** — 화면·단계 → 곡, 페이드, 첫 조작에 풀기, `M` 음소거 |
| `packages/client/src/screens/backdrop.ts` | **시간대·도시 레벨 → 배경 그림**. 경계(7·16·20시 / Lv5)의 단일 출처 |
| `packages/client/src/screens/TitleScreen.tsx` | 간판·로그인 (pptx 33·34) — 「입장」은 아직 곧바로 들어간다 |
| `packages/client/src/screens/PlaceScreen.tsx` | 궁궐·병영·장터 (pptx 35·36). 병영이 전투 길의 입구 |
| `packages/client/src/screens/ScreenChrome.tsx` | 배경 + 「제목·기어」 헤더 + 환경설정. 배경 화면 넷이 함께 쓴다 |
| `packages/client/src/screens/SettingsModal.tsx` | 환경설정 — 언어·음소거는 동작, ID·화면 모드는 자리만 |
| `packages/client/src/i18n/index.ts` | **다국어** — 5개 언어. 한국어만 채워져 있고 없으면 한국어로 물러난다 |
| `packages/client/test/visualEffect.test.ts` | 시각 효과 회귀 20건 — 눈으로 못 보는 규칙을 여기서 고정한다 |
| `packages/client/test/terrain.test.ts` | 지형 그림 회귀 4건 — 표에 빠진 지형이 없는가 |
| `packages/client/test/bgm.test.ts` | 배경음악 회귀 5건 — 기획자 지정 다섯 줄을 그대로 고정한다 |
| `packages/client/test/backdrop.test.ts` | 화면 배경·다국어 회귀 9건 — 눈으로 못 보는 경계(밤·Lv5)를 고정한다 |
| `packages/client/src/ui/statusChips.ts` · `statusPopup.ts` | 버프/디버프 배지와, 눌러서 보는 설명 |
| `packages/client/src/ui/art.ts` | 그림 경로와 대체 규칙 (수묵화 → 초상화 → 빈자리) |
| `packages/client/src/ui/inspectPanel.ts` | 상태 팝업 — 커맨드의 대칭 자리. **적 책략은 가린다** |
| `packages/client/test/poses.test.ts` · `camera.test.ts` | 연출 시간표 20건 · 카메라 15건 |
| `packages/rules/src/types.ts` `STATUS_META` | 상태이상 22종의 **버프/디버프 구분과 표시 이름** |
