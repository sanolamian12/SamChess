# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 지침이다.

## 시작하기 전에

**[`HANDOFF.md`](HANDOFF.md)를 먼저 읽는다.** 프로젝트 개요, 확정된 규칙, 진행 상황, 다음 할 일이
전부 거기에 있다 — **진행 상황은 HANDOFF §2 표와 §7 로드맵이 정한다**(작업계획 §2는
끝난 트랙 PPT 37~45의 표다). 그다음 [`Design/GDD.md`](Design/GDD.md)(구현 기준 문서)를 참조한다.

## 무엇인가

**삼국 약식 체스** — 체스 6기물의 이동 규칙을 빌린 삼국지 장수 260명의 ATB 전술 대전 게임.
TypeScript 모노레포(npm workspaces). PC(웹) · Android · iOS 단일 코드베이스 목표.

"턴제"라고 부르지만 실제로는 **CTB 스케줄러**다 — 절대시간이 흐르고, 유닛별 `waiting time`이 0이
되면 제어권을 얻으며, 제어 중에는 시간이 멈춘다. 서버가 시계의 유일한 기준점이다.

## 명령

```bash
npm install
npm run extract      # docs/*.xlsx → packages/data/generated/*.json (검증 실패 시 exit 1)
npm run backgrounds  # assets/Backgrounds/ → 화면 배경 14장
npm run typecheck    # tsc --build
npm test             # node --test
npm run server       # 대전 서버 (ws://localhost:2567). 꺼져 있어도 AI 대전은 돈다
npm run smoke:online # 온라인 스모크 — 제 안에서 서버를 띄운다 (dev 서버 불필요)
```

온라인을 혼자 확인하려면 **탭 둘**이다 — `?seat=1` · `?seat=2`, 병영 [출정하기]로 각각
검색하면 진짜 대기열에서 서로 매칭된다(H2b). `?seat=N`이 **계정과 「사람」(재접속용
`deviceId`)을 둘 다** 가른다.

Node 22.6+ 필요. `.ts`를 타입 스트리핑으로 그대로 실행하므로 번들 단계가 없다.
TypeScript는 타입 검사와 `.d.ts` 생성에만 쓴다(`emitDeclarationOnly`). 소스에서 import 확장자는 `.ts`로 명시한다.

## 지켜야 할 규칙

- **`docs/*.xlsx` / `docs/*.pptx`는 읽기 전용.** 정규화는 전부 `tools/extract_data.py`에서 한다.
- **`packages/data/generated/`를 직접 수정하지 않는다.** 엑셀을 고치고 `npm run extract`.
  생성물은 커밋 대상이다(엑셀 없이도 빌드가 되어야 하므로).
- **룰 엔진에서 `Date.now()` / `Math.random()` 금지.** 전투 재현성이 깨진다.
  난수는 `BattleState`의 시드 PRNG를 경유하고 `rngCursor`로 소비 순서를 기록한다.
- **클라이언트는 판정 결과를 보내지 않는다.** `Intent`(의도)만 보내고, 서버가 검증 → 적용 →
  `BattleEvent[]`를 브로드캐스트한다. 현금 가챠와 랭킹이 있어 치팅 유인이 크다.
- 기물 마스크의 단일 출처는 `packages/data/generated/pieces.json`이다.
  Python 추출기와 TS 테스트가 각각 독립적으로 위협 범위를 재계산해 검증한다.
- **책략·고유기술의 효과는 데이터(Effect DSL)로 적는다.** 사양의 출처는
  `tools/extract_data.py`의 `TACTIC_EFFECTS`이고, `packages/rules/src/effects.ts`는 해석기일 뿐이다.
  엔진에 개별 스킬을 하드코딩하는 것은 데이터로 접히지 않는 S급 `scriptId` 핸들러뿐이다.
- **`BattleState.log`를 deep clone하지 않는다.** `battle.ts`의 `cloneState()` 참조 —
  로그까지 복사하면 긴 전투가 O(턴²)이 된다 (실측 90배 차이).
- **전투력 계수는 실측이고 상수다.** `packages/meta/src/power.ts`의 `POWER_MODELS`는
  `npm run power`가 찍어 준 값을 **손으로 옮겨 적은 것**이다 — 실행할 때마다 다시
  적합시키면 전투력이 조용히 흔들리고 **매칭 상대까지 흔들린다.** 테스트가 고정하므로
  다시 재는 것은 언제나 **의도된 변경**이어야 한다. 3v3와 5v5는 **계수가 다르고 값 범위가
  겹쳐서** 나란히 놓으면 비교하고 싶어지는데 비교하면 안 된다 —
  `battlePower()`가 인원 불일치에 예외를 던지는 것이 그 자리를 지킨다.
- **통계 도구는 그럴듯한 숫자를 조용히 내놓는다 — 표준오차를 함께 봐야 안다.**
  기물 지시변수 다섯의 합이 상수라 완전 공선이었는데 계수는 멀쩡해 보였고 **표준오차만
  6435**였다. 고유기술 SP 코스트는 표본에 없는 값(헌제 7)으로 **외삽**되어 실측 최약체를
  D급보다 세게 만들었다. **회귀는 관측 범위 밖에서 틀리고, 공선은 값이 아니라 오차에 나온다.**
- **조용히 무시하는 경로는 「없는 것」처럼 보인다.** `Playback.submit()`은
  `awaitingInput`이 아닌 의도를 **아무 말 없이 버린다**(AI 차례·연출 중). 화면의
  [항복]은 그때도 눌리고 아무 일이 없어, 스모크가 「결과 화면이 안 뜬다」로 20초를
  기다렸다. 전투에 의도를 내는 검사는 **내 차례를 기다린 뒤에** 누른다.
- **화면이 미리 보여 주는 숫자는 엔진이 낸다.** 공격 확인창은 `forecastAttack()`,
  책략 확인창은 `illusionChance()`, 레벨업의 증분(`HP 10 → +5 15`)은 `growthPreview()`.
  화면이 공식을 다시 적으면 **표시만** 조용히 어긋난다 — 실제로 `AT +1`이 화면 글자에만
  남아 있었다(데이터·엔진은 0.5였다). 미리보기는 난수를 쓰지 않는다 —
  굴리면 `rngCursor`가 밀려 리플레이가 깨진다.
- **보유 장수의 성장은 레벨별 스택이고 `growth.length === level − 1`은 언제나 참이다.**
  읽을 때는 반드시 `statPicksOf(inst, cap?)` · `tacticsOf(inst, cap?)` 둘만 부른다
  (`packages/meta/src/profile.ts`). 두 군데서 직접 펴면 한쪽이 `cap`을 잊어
  **「하향했는데 책략은 그대로」**가 나는데, 화면에는 표시가 없고 전투에 들어가야 드러난다.
  룰 엔진의 `RosterEntry`는 평면 형식 그대로이고 펴는 자리는 `toRosterEntries()` 하나다.
- **부대의 레벨은 「상한」이지 「스냅샷」이 아니다.** `Squad.picks[].level`은 전투력을
  낮춰 약한 상대와 붙기 위한 눈금이고, 그 레벨의 능력치·책략은 **성장 스택에서 그대로
  꺼낸다**(`growthUpTo`의 `slice`). **보유 레벨보다 크면 조용히 눌러 담는다** —
  재설계로 Lv9가 Lv1이 되어도 부대가 죽지 않아야 한다. 누르는 자리는
  `toRosterEntries()` **하나**이고, 그래서 **「저장된 부대는 언제나 `createBattle`을
  통과한다」**가 계약으로 선다. 막는 쪽을 고르면 화면에 표시도 없이 전투 직전에 던진다.
  다만 **장수가 계정에서 빠진 것은 말한다** — 되돌릴 수 있는 것은 조용히, 없는 것은 말한다.
- **배치 프리셋은 위치가 아니라 기물에 붙는다.** `{piece, x, y}`이고 엔진의 `UnitId`도
  `{진영}-{기물}`이다 — 위치 배열로 두면 구성을 고쳤을 때 **조용히 어긋난다.**
  남군(P1)·북군(P2)은 좌표계가 달라 따로 저장하고, **어긋나면 던지지 않고 `null`**로
  물러난다(기본 배치가 깔리고 사람이 30초 동안 고친다). 「기본 배치로」는 엔진의
  `defaultDeployPos`를 그대로 부른다.
- **3v3과 5v5의 전투력은 눈으로도 섞지 않는다.** `battlePower()`가 인원 불일치에
  예외를 던져 **계산이 섞이는 것**은 막지만, 목록에 나란히 놓아 **눈이 섞이는 것**은
  화면만 막을 수 있다 — 부대 목록을 모드 묶음으로 가른 이유다.
- **검사가 「기본값과 같은 값」을 확인하면 아무것도 확인하지 않는 것이다.** 스모크의
  「저장한 배치가 판에 깔리는가」가 기본 배치를 저장해 놓고 봐서 **안 깔려도 통과**했다.
  방어를 넣었으면 발동하는 입력을, **검사를 넣었으면 실패하는 상태**를 만들어 본다.
- **재설계(둔갑천서)는 「되감기」다** — 레벨업에 쓴 카드를 **전부 돌려주고 Lv1로**.
  그 뒤는 정상 레벨업 절차 그대로라 재설계 전용 규칙이 없다. 원안이던 「레벨 유지 +
  다시 고르기」는 **이전 레벨에서 찍은 것을 기억해야 해서** 절차가 한 벌 더 필요했다.
  **돌려주는 양이 정확히 누적 필요분**이라야 다시 올릴 때 카드가 딱 떨어진다 —
  어긋나면 카드를 찍어 내거나 삼킨다(`cardsSpentOn()`이 `cardsToLevelUp()`을 더한다).
- **전적은 「한 번만 세고 표는 합으로 낸다」.** 칸은 `{상대}/{모드}/{기물}` **교차**
  하나뿐이고(`OfficerInstance.record`), 40쪽의 기물 여섯 줄·모드 두 줄·총합은 전부
  `records.ts`의 `sumTally()`가 낸다 — 따로 세면 언젠가 어긋나는데 **어긋나도 화면은
  아무 말도 하지 않는다.** **계정 전적(`PlayerProfile.record`)만은 따로 센다** —
  장수 전적을 더하면 한 판에 3~5명이 뛰어 출전 수가 사람 수만큼 부풀려진다.
  **AI와 온라인은 보상도 전적도 같다**(2026-08-18, 2026-08-04를 뒤집음) — `opponent`는
  이력의 라벨과 필터로만 쓴다. 문이 [출정하기] 하나라 상대를 고를 수 없기 때문이다.
- **대전 이력은 DB 한 행처럼 적는다.** `matches[]`의 한 줄은 평평하고 참조가 없으며
  `seq`가 PK다. 읽는 자리는 `recentMatches()` 하나 — 서버로 옮길 때 이 함수만 쿼리로
  갈면 화면이 안 바뀐다. **집계와 이력은 따로 쌓는다**(이력에서 집계를 파생시키면
  꼬리를 덜 때 통산이 조용히 준다). 시각(`at`)은 meta가 아니라 **화면이 넣는다.**
- **저장 형식이 달라도 계정을 버리지 않는다 — `migrateProfile()`이 되접는다.**
  판정은 `client`의 저장 층이 아니라 **`@samchess/meta`**에 둔다. 되접기는 화면에
  안 떠서 meta에 있어야 `npm test`가 잡는다. **버전은 뜻이 바뀔 때만 올린다** —
  필드가 더해지기만 하면 마이그레이션이 기본값으로 채운다. 다만 단위 테스트는
  「`loadProfile`이 그걸 실제로 부르는가」를 모르므로 **스모크가 옛 형식을 넣고 새로고침**한다.
- **시간으로 차는 값에는 자투리·상한·되감긴 시계 셋이 함께 온다.** 군량 충전
  (`packages/meta/src/city.ts`의 `syncGrain`)에서 셋 다 조용히 틀릴 수 있었다.
  정산 시각을 **「지금」으로 찍으면** 30분씩 들르는 사람이 **영원히 못 받고**
  (번 만큼만 밀어 자투리를 남긴다), 상한에서 시간을 **쌓아 두면** 참가비를 내는
  순간 몇 시간이 되돌아오며, 뒤로 간 시계에 맞춰 정산 시각을 **당기면** 앞뒤로
  흔들어 뽑을 수 있다. 화면에는 전부 「군량이 좀 많네」로만 보인다.
  **요율이 바뀌는 자리(증축)에서는 반드시 먼저 정산한다** — 그래서
  `applyCityUpgrade(profile, nowMs)`가 시각을 받는다.
- **meta에 시계를 들이지 않는다 — 지금 시각을 넣는 자리는 `App.tsx` 하나다.**
  `syncGrain(profile, nowMs)`는 순수 함수다(`bandForHour(h)`·`MatchRow.at`과 같은
  규약). **도시 화면에서만 채우면 안 된다** — 군량을 읽는 자리가 넷(메인의 통계·
  병영의 잠금·편성의 참가비·도시)이라 도시를 안 들르면 나머지가 낡은 값을 본다.
- **전선에는 「로그를 뺀 스냅샷 + 이벤트」를 싣는다 — 로그를 실으면 `O(턴²)`다.**
  실측: 스냅샷 3v3 **2,408 B** · 5v5 **3,705 B**인데 로그를 실으면 27,743 · 55,089 B이고
  **판 길이에 비례**한다. `cloneState()`가 로그만 얕게 복사하는 그 지뢰가 네트워크에
  다시 나타나는 것이고 화면에는 「후반이 좀 버벅이네」로만 보인다. 뗀 로그는 받는 쪽이
  **이벤트를 이어 붙여** 되만들고, 그게 정확히 같은 것은 엔진의 `commit()`이 보장한다
  (`이전 로그 ++ events === 다음 로그`). 떼는 자리(`toWire`)와 붙이는 자리(`applyWire`)는
  각각 **하나**다. **이벤트만 보내지 않는다** — 리듀서가 없고 만들면 `apply()`의 두 번째
  구현이다. **마감은 절대시각이 아니라 「남은 ms」**로 싣고(시계 어긋남), **단계가 바뀔
  때만 다시 잰다**(통마다 새로 재면 배치 중 30초가 되살아난다).
- **`Playback`은 상태를 만들지 않는다 — `BattleTransport`가 「도착」시킨다.**
  내 의도·상대의 수·시간 진행·마감 **넷이 전부** 온라인에서 「도착한다」로 바뀌므로 이음매
  하나로 모았다. `ready()`는 **로컬만 듣는다** — 뜻이 「진행시켜 달라」가 아니라
  **「나는 밀리지 않았다」**이고 서버는 기다려 주지 않는다. **연출이 도는 동안에는 단계가
  굳어 있다**(`settlePending`) — `phase`는 화면이 읽는 값이라, 아무것도 안 흐르는데
  `advancing`이라고 말하면 거짓말이다(실제로 WT 게이지가 멈춘 채 그 이름을 달고 있었다).
- **전투 단계의 실시간 제한은 `packages/rules/src/timing.ts` 하나가 정한다.**
  재는 주체가 클라와 서버 **둘**이 되므로 상수가 두 벌이면 **어긋나도 화면은 아무 말도
  안 한다.** **값만 두고 시계는 안 들인다** — 지금 몇 시인지를 읽는 자리는 클라의
  `App.tsx`와 서버의 `clock.ts` 하나씩이다. (`types.ts`의 주석이 「배치 60초 · 정찰 15초」로
  낡아 있던 것을 이때 고쳤다 — 숫자를 두 번 적으면 한쪽만 낡는다.)
- **전선의 `phase`는 「받는 사람의 걸음」이다 — `waiting`이 사는 자리.** 「나는 준비를
  마쳤고 상대를 기다린다」는 **한쪽만의 사실**이라 공유 `BattleState.phase`에 넣을 수
  없다(넣으면 아직 배치 중인 상대가 판을 못 만진다). 권위 상태는 `deploy` 그대로 두고
  **떼면서** 관점을 넣는다 — `toWire(state, side)`. 떼는 자리는 여전히 하나이고
  `BattlePhase` 하나가 재생기의 걸음 하나로 곧게 떨어진다. `waiting`은 H1까지
  **아무도 만들지 않는 값**이었다(`case 'waiting'`이 한 번도 안 돌았다).
- **방의 규칙은 `packages/server/src/room-logic.ts` 하나가 정하고, 시각을 인자로 받는다.**
  배치 30초·정찰 30초·제어 20초·넘기기 3번·재접속 유예 60초·유휴 2분·이탈 표 네 줄은
  전부 **브라우저 둘을 띄우고 분 단위로 기다려야** 보인다 — 껍데기(`BattleRoom.ts`)에
  적으면 검사가 아예 도는 적이 없다. `step(room, event, nowMs)`가 시각을 받으므로
  가짜 시계로 한 판을 통째로 민다. 껍데기가 하는 일은 자리 배정·`step()`·`out` 뿌리기·
  250ms 시계 넷뿐이고 **판정은 한 줄도 안 한다.**
- **시간 셋의 「관계」가 규칙이다 — 하나만 고치면 화면은 아무 말도 안 한다.**
  `WAITING_MS === RECONNECT_GRACE_MS`(같은 기다림) ·
  `RECONNECT_GRACE_MS <= CONTROL_MS × SKIP_TO_WIN`(돌아온 사람은 3번에 닿기 전에 도착) ·
  `ROOM_IDLE_MS > RECONNECT_GRACE_MS`(돌아오는 중인 사람이 먼저 잘리지 않는다).
  `timing.ts`의 `TIMING_INVARIANTS`가 적고 회귀가 고정한다.
- **성립하지 않은 판은 엔진의 결말이 아니다.** 배치 중 이탈·양쪽 이탈·양쪽 유휴는
  서버가 마지막 통에 `close: RoomClose`를 싣고, 받는 쪽은 `applyBattleResult`를
  **아예 안 부른다**(환불만). 결과 화면은 재사용하되 승/무/패 제목·보상 칸·예상 승률을
  **안 띄운다** — 띄우면 「무승부를 당했다」로 읽히고 그건 일부러 안 준 것이다.
  **정찰 이후 이탈은 여기 안 온다**(서버가 `surrender`를 적용한 평범한 결말이다).
  **사라진 쪽은 `refund`에 없다** — 넣으면 「끊는 것이 거절보다 싸진다」.
- **버전 짝은 회귀가 못 잡는다 — 소켓에서만 죽는다.** Colyseus 서버와 `colyseus.js`의
  메이저가 어긋나면 타입 검사도 `npm test`도 통과하고 좌석 예약에서 죽는다. 지금은
  `@colyseus/core@0.16.22` + `@colyseus/ws-transport@0.16.5` + `colyseus.js@0.16.22`로
  고정돼 있고, **`colyseus` 메타 패키지는 안 쓴다**(ESM 진입점이 redis·인증까지 전부
  import한다). 올릴 때는 `npm run smoke:online`으로 확인한다.
- **같은 계약을 채운다는 것은 메서드 이름이 같다는 뜻이 아니다.** `OnlineTransport`가
  `open`/`send`/`ready`/`close`를 다 갖췄는데 화면이 아무것도 안 그렸다 —
  `LocalTransport`는 `open()`에서 **첫 통을 내보내는데** 이쪽은 안 내보냈다.
  재생기는 `initial`로 시작하되 `onChange`가 한 번은 와야 화면이 자기를 맞춘다.
- **패키지를 import하면 서버가 뜨면 안 된다.** `packages/server/src/index.ts`는
  **배럴**이고 부팅은 `main.ts`다. 합쳐 두면 방만 빌리려는 스모크가 포트를 잡는다.
- **소켓 메시지는 리스너를 거는 시점이 늦으면 놓친다.** `OnlineTransport`의 `early` 큐가
  피하던 경주(H1)가 대기열(H2b)에서 그대로 재현됐다 — `Promise.all([join, join])` 뒤에
  `onMessage('matched', …)`를 걸면, 상대가 같은 틱에 도착했을 때 리스너 없이 지나가
  버려 `await`가 영원히 안 풀린다. **`send()`보다 먼저 `onMessage()`를 건다**가 규칙이다.
- **「대기열 앞으로 보낸다」는 상대편도 함께 정해야 한다.** 거절 뒤 "거절당한 쪽은
  대기열 앞으로"만 구현하고 거절한 쪽도 함께 앞에 넣었더니, **나중에 넣는 쪽이
  앞자리를 차지해** 순서가 뒤집혔다(H2b, `queue-logic.ts` `decline()`). 대기열·랭킹처럼
  **순서가 뜻을 갖는 자료구조**를 고칠 때는 넣는 항목 하나만이 아니라 **나머지가
  어디로 가는지**까지 같이 본다.
- **「나간다」를 부르는 자리가 둘이면 반드시 멱등해야 한다.** 검색 타임아웃과 화면
  언마운트가 각각 대기열 `room.leave()`를 불러 **두 번** 나갔더니 브라우저가 콘솔에
  「이미 닫히는 중」이라고 남겼다(H2b) — 스모크가 콘솔 오류로 잡는다. `left` 플래그로
  감싼 단일 진입점(`leaveQueue()`) 하나로 모았다.
- **Node 타입 스트리핑은 「타입만 지우면 되는」 문법만 받는다.** 데코레이터 ·
  파라미터 프로퍼티(`constructor(readonly x: T)`) · `enum` · `namespace`가 전부 막힌다.
  그래서 Colyseus를 **방·전송·재접속으로만** 쓰고 `@colyseus/schema`(`@type` 데코레이터)를
  안 쓰는 것은 설계 취향이 아니라 **이 저장소의 물리적 제약**이다 — 겸해서 `BattleState`가
  두 벌이 되는 것도 막는다.
- **「그 상태에 도달할 수 없는 검사」를 교훈을 알면서 다시 만들었다.** `pump()`의
  「연출 중에는 큐를 안 꺼낸다」 잠금은 **로컬에서 큐가 늘 비어 있어** 한 번도 시험되지
  않았다(지우고 돌려도 전부 통과했다). 고친 방법은 **회귀 안에 가짜 판정 주체**를 두어
  한 판을 통째로 밀어 넣는 것이다 — **흉내 통로가 반드시 개발용 URL일 필요는 없다.**
  그리고 **검사가 안 잡는 것은 주장하지 말고 적어 둔다**: 같은 함수의 재진입 잠금은
  깨도 안 잡히는데(순서는 같고 깊이만 재귀로 쌓인다) 주석이 「이 함수의 요점」이라고
  과장하고 있었다.
- **`state`는 이미 적용이 끝난 상태다.** 「맞기 전」을 보여 주려면 연출 층이 그 차이를
  들고 있어야 한다 (`PoseDirector`의 `hpPending` · `holdAt`). 화면이 이전 상태를
  기억하게 만들지 말 것 — 온라인 재접속에서 무너진다.
- **연출은 카메라가 도착한 뒤에 시작한다.** `poses.ts`의 `look()`이 커서를 밀므로
  각 `case`에서 **`show()`보다 먼저** 불러야 한다. 뒤집으면 자세가 이미 시작된
  자리에 큐가 놓여 「줌인 도는 동안 이미 때리고 있는」 어긋남이 돌아온다.
- **스모크 검사는 화면 글자가 아니라 `data-*` 속성으로 건다.** 「적 책략은 가린다」를
  「적군」이라는 **글자**로 골라내고 있다가, 그 글자를 빼자 검사가 조용히 무력화됐다.
- **「오라(aura)」와 「시각 효과(visualEffect)」는 다른 것이다.** `aurasOn()` ·
  `auraIncomingHalf`는 여포·허저의 **반경 판정**이고, `assets/SpecialStatus/`의 그림은
  화면에 겹쳐 그리는 **연출**이다. 엑셀 시트 이름만 「오라매핑」으로 남아 있다 —
  코드에서 `aura`가 보이면 언제나 반경 판정 쪽이다.
- **카드 액자의 9분할 자리는 두 곳에 있다.** `tools/build_frames.py`의 `SLICE`와
  `style.css`의 `.uc-frame` `border-image-slice`가 **같은 값이어야** 카드가 흰 종이
  안에 앉는다. 한쪽만 고치면 카드가 나무 위로 넘친다 — 도구가 돌 때마다 CSS 한 줄을
  찍어 주고, 스모크가 「카드가 종이 안에 있는가」를 좌표로 잡는다.
- **전역 CSS에 흔한 이름을 새로 쓰지 않는다.** `.panel`은 전투 화면의 플로팅 커맨드
  패널이 이미 쓰고 있고 `position: absolute`에 폭이 `--board`의 배수다 — 메타 화면의
  판때기에 같은 이름을 붙였더니 오른쪽 아래에 좁게 붙었다. CSS는 이름이 겹쳐도 오류가
  없어 **화면을 봐야만 안다.** 새 화면의 것은 `.place-panel`처럼 접두사를 붙인다.
  **접두사를 붙여도 같은 파일 안에서 겹칠 수 있다** — `.ofc-head`를 표 머리(grid)와
  상세 머리(flex)에 함께 붙여 한쪽이 무너졌다(`.ofc-thead` / `.ofc-bio`로 갈랐다).
  그리고 `.scr .foot`처럼 **선택자가 둘인 기존 규칙은 새 한 클래스를 이긴다.**
  **뜻이 같아도 이름이 겹치면 그냥 겹친다** — 도시 관리 화면(41쪽)의 `.city-info`는
  메인 화면의 `.city`와 부딪힌다. `.cty-`로 갈랐다.
  **이름이 안 겹쳐도 부모가 다르면 뜻이 달라진다** — `.foot`을 배경 화면
  (`ScreenChrome`)에서 쓰면 `.scr .foot`의 `margin-top: auto`가 `.place-body`의 것과
  서로를 밀어내 글자가 세로로 짜부라진다. 배경 화면의 단추는 **판때기 안**에 둔다.
- **배경 위에 놓는 화면에서 팝업은 「내용을 올리는」 규칙에서 빼야 한다.**
  `.scr-bg > *` · `.scr-dim > *`가 자식 **전부**에 `position: relative`를 걸어
  `.modal-back`의 `absolute`를 덮으면, 팝업이 화면 한가운데가 아니라 **내용 맨 아래에
  흘러 붙는다.** 스모크가 「떠 있는가」만 보면 못 잡는다 — **「있는가」와 「제자리에
  있는가」는 다른 검사다.**
- **띠로 이어 그린 그림은 등분해 자르지 않는다.** `assets/Backgrounds/`의 진짜 칸
  경계는 등분한 자리에서 1~2px 어긋나 있어, 등분하면 **옆 칸이 딸려 온다**
  (`build_backgrounds.py`의 `cut_points()` 참조). 화면에서는 「끝에 이상한 줄이
  있네」로만 보인다.
- **화면 배경의 시간대·도시 레벨 경계는 `screens/backdrop.ts` 하나가 정한다.**
  `bandForHour()`는 시계를 스스로 읽지 않는다 — 밤 그림은 밤에 접속해야 보여서,
  시각을 밖에서 넣을 수 있어야 테스트로 고정된다. **출력 확장자(`.jpg`)는 그 파일과
  `tools/build_backgrounds.py`가 함께 바뀌어야 한다** — 한쪽만 고치면 그림만 조용히 404다.
- **화면을 꽉 채우는 그림은 프레임이 가장 클 때(700px)로 확인한다.** 480px 스크린샷에서는
  300px짜리를 늘려도 안 깨져 보인다 — 배경을 저해상도로 붙여 놓고 못 알아챘던 자리다.
- **움직이는 배경은 제 층(`.scr-art`)에 있고, 쌓임 순서 셋은 함께 본다.** 그림을 화면
  div(`.scr-bg`)의 배경으로 되돌리면 `transform`에 **제목·단추까지 딸려 흔들린다.**
  순서는 **그림 0 → 어둠(`::before`) 1 → 내용 2**이고, 하나만 지우면 어둠이 글자를
  덮거나 그림이 어둠을 덮는다. 확대(`DRIFT_ZOOM`)는 장식이 아니라 **밀어낼 여백**이라
  `밀리는 거리 < (배율−1)/2`를 자세마다 지켜야 한다 — 넘으면 반대쪽에 바탕색 띠가 뜬다.
- **매칭이 정한 상대는 「값」으로 넘긴다 — 전투 화면이 다시 만들지 않는다.**
  `MatchScreen`이 고른 `MatchOpponent`가 `BattleScreen`까지 그대로 실린다. 예전에는
  전투 화면이 AI를 **다시 뽑아**, 「찾았습니다!」가 보여 준 상대와 실제로 싸우는 상대가
  **같은 시드로 두 번 만든다는 데 기대고** 있었다. 값으로 들고 오면 그 어긋남이 없고,
  서버가 상대를 주는 날에도 화면이 안 바뀐다.
- **AI 상대도 `battlePower`로 뽑는다 — 등급 점수는 레벨을 못 본다.** 옛 `makeAiPicks`는
  Lv9 S급 셋(1211)과 Lv1 S급 셋(797)을 같은 상대로 봤다. 온라인 매칭이 `MATCH_BAND`로
  고르는 것과 **같은 눈금**이라야 「바뀌는 것은 상대가 사람인지뿐」이 성립한다.
  도달 범위 밖이면 **던지지 않고 가장 가까운 값**으로 물러나고 **잰 값을 그대로 싣는다** —
  막으면 「전투력을 올려서 출전을 못 한다」가 되고, 화면은 예상 승률로 실제 격차를 보여 준다.
- **참가비는 AI는 [전투준비] 클릭 즉시, 온라인은 `onReady`가 실제로 오는 순간에 낸다**
  (H2b가 온라인 쪽을 바꿨다 — 클릭해도 상대가 아직 확인 전이거나 그때 거절할 수 있어서다).
  거절(「다시 찾기」) −1은 참가비 위에 얹힌다. 경계 판정은 화면이 아니라
  `canDeclineMatch()`이고, `grain > cost`가 아니라
  **`grain − MATCH_DECLINE_GRAIN >= cost`**로 적는다(패널티가 바뀌어도 따라오게).
  **거절은 온라인에만 있다** — AI는 거절당할 상대가 없고, 그때는 **없는 이유를 적는다.**
  매칭만 하고 뒤로 가면 **한 톨도 안 나간다**(안 그러면 거절과 이중으로 물린다).
- **「아직 안 붙은 갈래」에 걸린 검사는 도는 적이 없다.** 「다시 찾기」가 온라인에만
  있던 F 시절, 온라인이 언제나 빈손이라 완료 조건 두 줄이 **한 번도 실행되지 않았다.**
  「기본값과 같은 값을 본다」의 사촌인데 이번 것은 **검사할 상태에 도달할 수 없는**
  형태였다 — 갈래를 흉내 낼 통로(그때는 `?match=online`, H2b에서 진짜 대기열이 붙으며
  지웠다)를 검사와 **함께** 만들었다. 그리고 **판정 층과 화면 층을 각각 깨 봐야**
  둘 다 산 것을 안다.
- **가운데만 재는 회귀는 끝이 어긋난 것을 놓친다.** AI 상대 맞추기가 목표 여섯 점에서는
  통과하고 **전 구간을 훑자 바닥 근처에서 31이 벗어났다**(구간 ±20). 「회귀는 관측 범위
  **밖**에서 틀린다」(D)의 짝이고 이번 것은 **범위 안의 끝**이다. 훑기를 촘촘히 하면
  느려지는데, 그 느림은 대개 **규칙 쪽이 매번 다시 만들고 매번 다 정렬해서**다.
- **단추의 이름은 줄바꿈하지 않는다 — 접히는 쪽은 언제나 사연이다.** `.btn .lbl`에
  `nowrap`이 없어 「튜토리얼 시나리 / 오」가 됐다. **안내문은 제 단추 바로 밑에 둔다** —
  끝에 몰아 두면 어느 단추 이야기인지 모른다. 둘 다 700px 눈검사에서만 보였다.
- **지형 그림은 「칸」에, 지속형 링은 「유닛」에 붙는다.** `battle/terrain.ts`는 판에
  붙박이로 깔리고(`state.terrain`을 그대로 따라간다), `battle/visualEffect.ts`의 링은
  유닛 컨테이너 안에 있어 같이 움직인다. 성지 칸에서는 **둘 다** 뜨는데 뜻이 다르다 —
  칸은 「여기가 성지다」, 링은 「이 유닛이 그 효과를 받는 중이다」.
- **계정 API(`packages/server-api`)는 Colyseus(`packages/server`)와 완전히 분리된
  프로세스다.** 계정 CRUD를 대전 방에 얹으면 "Colyseus는 방·전송·재접속만 쓴다"가
  깨진다. 둘 다 `@samchess/meta`를 부르지만 서로 import하지 않는다.
- **Supabase의 Direct connection(`db.<ref>.supabase.co`)은 기본이 IPv6 전용이다.**
  IPv6 아웃바운드가 없는 환경에서는 DNS가 정상 응답해도 `ENOTFOUND`로 막힌다 —
  **Transaction Pooler**(`aws-0-<리전>.pooler.supabase.com:6543`) 문자열을 쓴다. 서버가
  짧은 연결을 여닫는 쓰임에도 pooler 쪽이 맞다.
- **`.env`의 자리표시자(`[YOUR-PASSWORD]`)는 글자만이 아니라 감싼 기호까지 지운다.**
  대괄호째 남기면 Postgres가 완전히 다른 비밀번호로 본다 — URL 파싱으로는 안 잡히고
  값을 디코드해 문자 코드를 직접 봐야 드러난다.
- **`const`로 좁힌 널 체크는 클로저 경계를 못 넘는다.** 모듈 최상단에서
  `if (!X) throw`로 걷어낸 타입이 **다른 함수 안에서 그 변수를 참조하면** 다시
  `T | undefined`로 돌아온다(TS가 클로저 캡처를 보수적으로 본다). 반환 타입이
  non-null인 헬퍼(`requireEnv()`)로 감싼다 — `server-api/src/auth.ts` 참조.
- **계정 저장이 비동기가 되면 로컬은 캐시로, 정본은 서버로 갈린다** (H3a).
  `client/src/meta/storage.ts`의 `loadProfile()`/`saveProfile()`은 `server-api`를
  부르는 비동기 함수이고, `localStorage`는 서버가 안 닿을 때만 읽는 **읽기 전용**
  캐시다 — 오프라인 중 쓴 것을 캐시에 쌓으면 나중에 서버 값과 갈라진다. **저장은
  반드시 호출 순서대로 나간다** — 부르고 기다리지 않는 저장(화면이 막히면 안
  된다)이 잦으면 `PUT`이 여러 개 겹치는데, 네트워크는 보낸 순서대로 도착한다고
  보장하지 않는다. 쓰기 큐(앞 저장의 응답을 기다린 뒤 다음을 보낸다)로 막는다 —
  안 그러면 늦게 보낸 것이 먼저 land해 최신 상태를 옛 값이 덮어쓴다. 실제로 재설계·
  재성장을 빠르게 반복하는 스모크에서 레벨이 간헐적으로 한 단계 낮게 나오는 것으로 잡혔다.
- **되접기(`migrateProfile()`)는 서버 읽기 자리에서도 다시 일어난다.** `PUT`은
  저장 전에 항상 되접으므로 DB에는 옛 형식이 안 남을 것 같지만, **DB 행을 직접
  건드린 경우**(수동 SQL, 마이그레이션 실수)는 예외다. `profileStore.getProfile()`이
  되접은 결과가 저장된 것과 다르면 **그 자리에서 되쓴다** — 안 하면 읽을 때마다
  같은 되접기를 반복하며 `version`이 저장에는 안 올라간다.
- **`@fastify/cors`의 기본 `methods`를 믿지 않는다.** 이 서버에서는 `GET,HEAD,POST`로만
  잡혀 `PUT`이 빠졌다(라우트를 훑어 추론하는 방식이 등록 순서에 따라 갈리는 듯하다).
  빠지면 브라우저가 프리플라이트는 204로 통과시키고 **진짜 PUT은 보내지도 않고
  조용히 막는다** — 서버 로그에는 아무 흔적도 안 남아 "왜 저장이 안 되지"로
  한참 헤매게 된다. `methods`를 항상 명시한다.
- **비동기 전환은 "마운트 시점에 이미 상태가 있다"고 가정한 effect를 깬다.**
  `useState(() => loadProfile())`(동기 초기화)를 비동기 `loadProfile().then(...)`로
  바꾸면, `[]` 의존성으로 마운트 때 한 번만 도는 다른 effect는 **그 상태가 아직
  `null`인 채로** 실행되고 다음 기회(다음 렌더 트리거, 또는 인터벌)까지 헛돈다.
  군량 시간 충전 tick이 이렇게 깨졌다 — 로그인 직후 도시 화면에 들어가면 "군량이
  안 찬다"로 보였다. 의존성에 그 상태의 존재 여부(`[Boolean(profile)]`)를 더해
  **상태가 처음 채워지는 순간에도** 한 번 더 돌게 고쳤다.
- **온라인 접속에 실제 네트워크 인증 왕복이 붙으면 "빠른 길" 통로의 타임아웃이
  너무 빡빡해질 수 있다.** `QueueRoom.onAuth`가 Supabase `/auth/v1/user`를 실제로
  호출하면서 `?match=fast`(스모크용 AI 폴백, 원래 400ms)가 그 왕복만으로 넘칠 수
  있게 됐다 — 개발용 통로의 값도 실제 지연이 늘면 다시 재야 한다.
- **"클라이언트가 계산해서 PUT하는 값을 서버가 재검증한다"는 PUT 안에서 다시
  풀 수 없다 — PUT은 전체 블록을 받을 뿐 「왜 바뀌었는가」를 모른다** (H3b, 2026-08-21).
  참가비·거절 군량·환불은 애초에 클라이언트가 임의로 주장하는 값이 아니라
  **Colyseus(`packages/server`)가 이미 권위 있게 결정해 둔 사건**(방이 열렸다·
  거절이 처리됐다·방이 접혔다)이다. 그래서 그 사건이 일어나는 바로 그 순간,
  Colyseus **셸**(`QueueRoom`·`BattleRoom`, `room-logic.ts`/`queue-logic.ts`는
  순수하게 그대로 둔다)이 `server-api`의 `POST /internal/grain`을 직접 불러
  재계산시킨다 — 사용자 토큰이 아니라 **서버 간 공유 비밀**(`INTERNAL_API_SECRET`)로
  인증한다. `packages/server`가 `packages/server-api`를 "얹은" 것이 아니라 **호출**한
  것이라 "Colyseus는 방·전송·재접속만 쓴다"(§5-54)와 부딪히지 않는다고 판단했다 —
  이 경계를 또 건드리게 되면 같은 잣대로 다시 볼 것. **실패해도 판은 그대로
  돈다**(`server-api`가 죽어 있어도 게임이 멈추면 안 된다, §5-61과 같은 결) —
  `smoke:online`이 `server-api` 없이 통과하는 것이 이 경로를 실제로 확인한다.
  ⚠ **`PUT /profile`은 여전히 `grain`을 통째로 신뢰한다** — 이번 재검증은 온라인
  매칭의 **정상 클라이언트 흐름**만 막았고, 직접 API 호출로는 여전히 군량을 불릴 수
  있다. 시간 기반 군량 충전(`syncGrain`)까지 포함한 경제 전체를 서버 권위로 옮기려면
  훨씬 큰 작업이 필요하다 — 알고 남긴 공백이다. **H3d(2026-08-21)가 이 공백을
  닫았다** — 아래 참조.
- **판정 주체가 클라이언트에 있어야 한다고 해서 결과까지 클라이언트를 믿어야 하는
  것은 아니다 — 룰 엔진이 결정적이면 "판이 끝난 뒤 재생해서 검증"이 성립한다**
  (AI 대전 서버 이관, H3c, 2026-08-21). AI 대전을 서버 권위로 옮기는 첫 안은
  Colyseus `BattleRoom`에 봇 자리를 앉히는 것이었다 — 온라인과 완전히 같은 실시간
  방·시계다. **동시 접속자가 늘수록 AI 대전 한 판마다 방 하나를 그 판이 끝날 때까지
  붙들어야 해서 부하로 쌓인다**는 지적에 기각했다. 대신 AI 대전은 지금처럼
  `LocalTransport`(클라이언트 프로세스)가 실시간으로 돌리되, **판이 끝난 뒤에만**
  서버가 개입한다 — 사람이 낸 의도의 순서만(`LocalTransport.getIntentLog()`)
  서버로 보내면, 룰 엔진이 `Date.now()`/`Math.random()`을 안 쓰는 결정적 함수라
  AI의 수(`takeTurn()`)·시간 진행(`advanceTime()`)까지 서버가 **같은 시드로 처음부터
  다시 계산**해 낼 수 있다(`@samchess/rules`의 `replayLocalMatch()`). 판마다 살아
  있는 상태가 필요 없는 **한 번 계산하고 끝나는 요청**이라 부하가 판 길이·동시
  접속자 수에 얽매이지 않는다. **사람 쪽 로스터·배치 프리셋은 서버가 가진 계정에서
  직접 다시 만든다** — 클라이언트가 능력치·레벨을 자칭할 수 없다. 무승부(실측
  0.02%)는 "택1을 고르기 전까지 반영하지 않는다"는 규칙 때문에 이 경로를 안 쓰고
  지금처럼 클라이언트가 로컬로 반영한다 — 위조 여지가 거기에만 좁게 남는 낮은
  심각도의 공백이다. 실패하면(서버 다운) 예전 방식(로컬 계산 + `PUT`)으로 물러난다
  — "서버가 꺼져 있어도 게임은 돈다"가 여기도 선다.
- **온라인 대전은 이미 판정 주체가 서버 자신이라, 재생조차 필요 없다** (`PUT
  /profile`의 grain 신뢰 문제 마감, H3d, 2026-08-21). H3c는 "판이 끝난 뒤 재생해서
  검증"이었다 — 판정 주체(`LocalTransport`)가 클라이언트에 있었기 때문이다.
  온라인은 다르다: `BattleRoom`이 `room-logic.ts`의 `step()`으로 이미 실시간
  판정을 해 왔고, `room.battle.phase === 'finished'`가 되는 순간의 `room.battle`
  자체가 권위 있는 결과다. 그래서 `BattleRoom`(셸)이 그 전환을 감지해 `server-api`의
  `POST /internal/battle-result`를 **직접** 부른다 — H3b가 참가비·환불에 쓴
  "Colyseus 셸이 server-api를 직접 호출한다" 패턴 그대로다(재생 패턴이 아니다).
  양쪽 진영에 각각 부르고, 그 응답(`rewards`)을 그 좌석에게만 새 메시지
  (`'settled'`)로 돌려준다 — `ServerMsg`(`@samchess/rules`의 `wire.ts`)에는 안
  얹는다, rules가 meta(계정 보상)를 알면 계층이 깨지므로. 클라이언트는 그 통보를
  받으면 로컬 계산을 버리고 `loadProfile()`로 **다시 읽는다** — 자기가 계산한
  값을 정본으로 삼지 않는다는 뜻이다. **무승부(`winner === null`)는 여기 안
  온다** — 사람이 택1을 고른 뒤에만 반영되므로(GDD §6.4), 판정 주체가 이미
  사라진 뒤 `POST /battle/draw-result`(AI·온라인 공통)로 간다 — 거기서만
  `picks`·`kills`·`power`가 여전히 클라이언트 값이다, H3c가 AI 무승부에 남긴
  것과 같은 낮은 심각도의 공백. **`waitForSettled()`의 리스너는 `OnlineTransport`
  생성자에서 건다** — "소켓 메시지는 리스너를 거는 시점이 늦으면 놓친다"(H2b가
  이미 밟은 지뢰)를 여기서 반복하지 않기 위해서다. **못 받으면(타임아웃·서버
  다운) 로컬 `applyBattleResult` + `PUT`으로 물러난다** — 그 순간에만 치팅
  표면이 예전 수준으로 돌아간다(§5-61과 같은 결).
- **`PUT /profile`은 더는 `grain`·`grainAt`을 안 믿는다** (H3d). 시간 충전의
  정본은 `profileStore.getProfile()`이 매번 서버 자신의 시계로 다시 계산하는
  것 하나이고(GET을 부를 때마다, 그리고 내부 함수들이 전부 `getProfile()`을
  거치므로 공짜로 최신값 위에서 계산된다), 전투 보상의 정본은 서버가 직접
  계산해 `saveProfileTrusted()`로 쓰는 것 하나다(AI 재생·온라인 정산·무승부
  택1 셋 다 `battleResult.ts`의 `settleOutcome()` 하나로 모인다). `PUT`으로
  들어오는 `grain`은 그래서 **조용히 버리고 서버가 가진 값으로 되쓴다** — 지킬
  기존 행이 없는 최초 1회 이전만 예외다(지킬 정본이 아직 없다). **`grain`을
  써서 얻는 보상이 있으면(무승부의 군량 택1처럼) 반드시 서버 쪽 반영 경로를
  새로 만들어야 한다** — 안 만들고 로컬 `applyBattleResult` + `PUT`에만 기대면
  `PUT`이 그 변화를 조용히 버려 보상이 사라진다(`/battle/draw-result`를 새로
  만든 이유가 이것이다). `saveProfile()`(클라이언트 PUT 전용, grain 보호)과
  `saveProfileTrusted()`(서버가 이미 계산한 값 저장, grain 안 보호)를 갈라 둔
  것도 같은 이유 — 이름이 비슷해서 잘못 골라 쓰면 다시 뚫린다.
- **`packages/meta`에 새 모듈을 추가할 때 「meta는 순수 함수, 시계·난수는 밖에서
  받는다」를 코드를 다 쓴 뒤에도 스스로 다시 어길 수 있다.** 상점 가챠(트랙 9
  선작업, `gacha.ts`)의 첫 구현이 계정의 가챠 시드를 `Date.now()`/`Math.random()`으로
  자체 생성했다 — `index.ts` 머리말에 "I/O·`Date.now()`·`Math.random()`을 안 쓴다"고
  이미 적혀 있는데도 그랬다. `applyBattleResult(profile, outcome, seed)`가 보상 카드
  추첨에 쓰던 것과 같은 자리로 고쳤다 — **시드가 필요하면 함수 인자로 받는다**
  (`drawGacha(profile, count, newSeed)`, 이미 저장된 시드가 있으면 무시).
- **큰 유한 배열(가챠의 25,200장)은 저장하지 않고 `{seed, drawn}`만 저장한 뒤
  필요할 때 같은 시드로 다시 셔플한다.** 전투가 로그 대신 `seed`·`rngCursor`만
  들고 다니는 것과 같은 패턴이다(`packages/rules/src/rng.ts`의 `shuffle`). 배열
  자체를 저장·복원하면 "재접속마다 안 섞인다"는 전제를 지키는 코드를 매번 새로
  써야 하고, 저장 용량도 계정마다 수만 개 항목을 그대로 지게 된다.
- **밸런스·경제 상수를 GDD에 숫자로 옮겨 적지 않고, 이미 있는 계산 함수에서
  파생시킨다.** 가챠 슬롯 수(장수 1명당 200장)는 `growth.json`의 레벨업 누적
  카드 수(100장) × 배수(2)인데, 그 100을 GDD나 코드에 다시 적지 않고
  `cardsSpentOn(GROWTH.maxLevel)`(기존 함수)을 그대로 불러 쓴다 — 아래 §4.3
  레벨업 카드 표·증축 자재 표가 겪은 것과 같은 사고를 가챠에서 반복하지 않기 위해서다.

## 기획 수치의 정본은 엑셀이다

GDD가 낡아 어긋난 적이 **두 번** 있다 — §4.3 레벨업 카드 표(누적 116 vs **100**),
§5 증축 자재 표(누적 370 vs **225**). 둘 다 코드는 처음부터 JSON을 읽고 있어
**동작은 멀쩡했고 실행으로는 안 잡혔다.** 수치가 의심스러우면
`packages/data/generated/*.json`(← 엑셀)을 보고, GDD는 따라 적는 곳으로 고친다.
**작업 계획의 완료 조건도 낡는다** — C2의 「도시 전적 합 = 장수 전적 합」이 반대로
적혀 있었다(같을 수 없다. 한 판에 3~5명이 뛴다). 코드와 회귀가 정본이다.

## 기획 수치를 바꿀 때

GDD의 「§12 미결 항목」에 결정 이력이 있다. 아래는 이미 확정된 것이라 임의로 바꾸지 않는다.

- 맵 `20행 × 25열` 고정
- `WT = 190 − 통솔력` — 스킬 지속시간 `time 90/190/290/490`이 1/2/3/5 사이클로 떨어지는 근거
- 크리티컬 ×2, 확률 `clamp(0,100)`
- 위협 범위 Rock 41 / Queen 39 / Bishop 37 / Pawn 33 / King 25 / Knight 25

수치를 바꾸면 `packages/rules/test/data.test.ts`가 먼저 깨진다. 테스트를 고치기 전에
그 변경이 의도된 것인지 확인한다.

## 무관한 폴더

`c:\Users\user\Documents\Hackers_IELTS_Listening_Basic_MP3_free\DGGL\Games\SamHero\` 에
**삼국지 영걸전**(KOEI, 1993) DOS 게임 원본이 있다. 이 프로젝트는 원래 거기서 시작했으나
2026-07-31에 분리되었고 **이제 아무 의존 관계가 없다.** DGGL 런처가 그 폴더를 직접 참조하므로
파일을 옮기거나 수정하지 않는다.
