/**
 * 서버가 **지금 몇 시인지 읽는 유일한 자리**.
 *
 * 클라의 `App.tsx`와 짝이다 — 규칙 층(`room-logic.ts` · `@samchess/meta`)은 시각을
 * **인자로 받고** 스스로 읽지 않는다. 그래야 배치 30초 · 제어 20초 · 재접속 유예
 * 60초 · 유휴 2분이 **가짜 시계로** 회귀에 걸린다(브라우저 둘을 띄워야 보이는 것들이다).
 *
 * 값(눈금)은 `@samchess/rules`의 `timing.ts`가 정한다 — 여기 숫자는 없다.
 */
export const now = (): number => Date.now();
