# 2026-09-22 태학 — 책략 개량 연구

트랙 11h의 마지막 건물. 태학은 그림과 현황 한 줄뿐이었고 GDD §12에 「태학 세부 사양」이 미해결로
남아 있었다. 이 세션은 **사양을 기획자와 정한 뒤** 데이터 · meta · server-api · 화면까지 한 번에 붙였다.

## 1. 사양이 뒤집힌 경위

옛 사양은 「장수를 보내 무·지·통을 훈련 — 1시간 · Up당 +2」였다. 현황을 정리해 보여 주자 기획자가
**「프로필에 `무 +0` `지 +0` `통 +0`이 덕지덕지 붙으면 디자인도 정책 관리도 복잡해진다」**며 방향을
바꿨다 — **책략을 연구해 개량형을 익히게** 이끈다.

오간 결정(전부 기획자 확정):

| | 결정 |
|---|---|
| 단위 | **계정.** 연구하면 원본을 익힌 장수 전원이 개량형을 쓴다 |
| 주제 | 태학 레벨마다 3중 택1 · ~~순서 무관~~(둘째 지정으로 **Lv1부터 차례로**) · 레벨마다 1개 → 태학 Lv5에서 개량 5개 |
| 진행 | 동시 1개 · 실제 1시간 · **무료** · 취소 허용 |
| 완료 | 전투가 아닌 화면에서 축하 팝업 + 「N명에게 일괄 적용」 안내 |
| 되돌리기 | 둔갑천서 같은 상점 아이템, 금화 10냥 — **이번엔 서버 로직만.** 되돌린 뒤 **전에 끝냈던 레벨까지는 즉시**(둘째·셋째 지정) |
| 전투력 | 반영 안 함 · AI 상대 개량 없음 · 적 개량형 숨김 |

### 수치 검토에서 바뀐 것

기획자 초안 16종을 엔진의 실제 Effect 정의와 대조해 셋을 짚었다.

- **증폭+를 Pawn이 쓰면 한 번 공격에 두 번이 다 소모된다** — 충전이 「타격마다」 빠진다
  (`state.ts`의 `consumeCharge`). 기획자 답: **Pawn 자체를 「원거리 공격」으로 하향하고 싶었다 —
  엔진 일이라 별도 트랙.** 그래서 이 문제는 그 트랙이 함께 푼다(GDD §12에 올렸다).
- **탈진+(3일마다 −2)는 처음 이틀 동안 원본보다 약하다** — 첫 피해가 주기 끝에 온다. 기획자 답:
  장기적으로 강하니 그대로.
- **진화+(MP 1→0)는 아무도 안 고를 선택지** — 기획자 답: **빼고** Lv3~5를 다시 짜서 전 레벨 3중 택1로.

## 2. 구조 — 바꿔 끼우는 자리는 하나

- 개량형은 **별도 책략 id**(`{원본}-plus`)다. **`TACTICS`(16)에는 안 넣었다** — 레벨업 화면이
  `tacticsForLevel()`로 그 배열을 훑는다. 엔진이 찾는 `tacticById`에만 31종이 함께 들어,
  **룰 엔진 · AI · 전투 화면은 한 줄도 안 바뀌었다.** `level`·`school`은 원본을 잇는다(AI 정렬).
- 장수의 성장 스택은 **원본 id 그대로.** `toRosterEntries()`가 `upgradeTactics()`로 갈아 끼운다.
  성장 스택에 개량형을 적으면 되돌리기가 장수 전원을 훑어야 하고 `checkGrowth`가 그 id를 모른다.
- 끝난 연구는 `syncCity()`가 거둔다(`collectResearch`, 대장간 주문과 같은 자리). `academy`는 서버 소유.
- 이름은 원본 + 「+」 — 열 언어 공짜. 설명만 새로 적었다(설명이 원본과 글자까지 같으면 원본 번역을 잇는다).
  그림(일회성 연출 · 「선공」 표시)도 원본을 잇는다(`extend_vfx_for_upgrades`).
- 엑셀 태학 줄은 옛 사양(「훈련 보정 2/4/6/8/10」)이다. 엑셀은 읽기 전용이라 `fix_academy_effect()`가
  「연구 주제 1~5」로 덮어쓰고, 엑셀이 갱신되면 **스스로 알린다.** 기획자가 엑셀을 고쳐야 한다.

### 바뀐 자리 한눈에

| 층 | 파일 | 한 일 |
|---|---|---|
| 데이터 | `tools/extract_data.py` | `ACADEMY_RESEARCH_MINUTES`(60) · `ACADEMY_TOPICS` · `TACTIC_UPGRADES`(15종 + 아홉 언어 설명) · `build_tactic_upgrades()`(주제 표 빈틈 · 표 불일치 · MP가 원본보다 비싼 개량을 검사) · `fix_academy_effect()` · `extend_vfx_for_upgrades()` |
| 데이터 | `packages/data/generated/tacticUpgrades.json`(새로) · `buildings.json` · `visualEffects.json` | 추출 결과 — 직접 고치지 않았다 |
| 데이터 | `packages/data/src/index.ts` | `TACTIC_UPGRADES` · `ACADEMY_TOPICS` · `ACADEMY_RESEARCH_MINUTES` · `upgradeIdOf` · `TacticData.base/academyLevel` · `tacticById`에 31종 · `BuildingEffectKey`의 `trainingBonus` → `researchTopics` |
| meta | `academy.ts`(새로) | 칸 형편(`academySlots`) · 시작/취소/거두기/확인/되돌리기 · `researchedUpgrades(profile, atMs)` · `upgradeTactics()` · `officersUsingUpgrade()` · 이유 코드 `ACADEMY_REASONS` |
| meta | `types.ts` · `city.ts` · `roster.ts` · `authority.ts` · `migrate.ts` · `index.ts` | `AcademyState` 필드 · `syncCity`가 거둔다 · `researchTopics` · 전투 로스터에 개량형 · 서버 소유 · 되접기(`readAcademy`) |
| server-api | `profileStore.ts` · `routes.ts` · `aiBattle.ts` | `applyAcademyAction` · `POST /academy/research` · `/academy/cancel` · `/academy/ack` · `/academy/reset` · `/dev/grant {finishResearch}` · **재생 기준 시각 버그 수정** |
| 클라이언트 | `AcademyScreen.tsx`(새로) · `AcademyNotice.tsx`(새로) · `tacticDiff.ts`(새로, 둘째 지정) | 태학 화면(현황판 · [연구하기] · [완료된 연구] · 취소 확인) · 전역 축하 팝업 · 원본↔개량 설명의 바뀐 곳 가르기 |
| 클라이언트 | `App.tsx` · `BuildingScreen.tsx` · `OfficerDetailScreen.tsx` · `buildingText.ts` · `meta/city.ts` · `style.css` | 팝업 배선과 「끝나는 순간 정산」 시계 · 라우팅 · 개량형 이름 표시 · 현황 줄 「책략 개량 연구 · 1/3」 · 서버 호출 넷 · `acd-` 스타일과 공통 판때기 묶음에 `.scr-building-academy` |
| 문구 | `i18n/strings/*.json` 열 개 | 46키 추가 · 2키 교체(`place.academy.sub` · `city.bld.academy`) |
| 테스트 | `meta/test/academy.test.ts`(새로, 첫 21건 → 최종 23건) · `client/test/tacticDiff.test.ts`(새로, 4건) · `authority.test.ts` · `client/test/reasonStrings.test.ts` · `visualEffect.test.ts` | 태학 규칙 전반 · 서버 소유 목록 · 열 언어 문구 · 「선공」 표시 목록 |
| 도구 | `tools/academy_ux.ts`(새로) · `package.json` | `npm run smoke:academy-ux` |
| 문서 | `Design/GDD.md` §5.12 신설 · §5.2/§5.4/§5.10/§12 · `HANDOFF.md` · `CLAUDE.md` | 사양 · 진행 상황 · 새 규칙 둘 |

## 3. 어디서 넘어졌나

### `doneAt` — 「언제 끝났나」가 재생 검증의 뜻이다

AI 대전은 판이 끝난 뒤 서버가 같은 시드로 재생한다. 전투 **도중에** 연구가 끝나면 클라이언트는 원본,
서버는 개량형으로 돌려 **정상 플레이가 거부된다.** 부상이 같은 문제를 이미 풀어 두었다 — 전투를 만들
때 판의 시작 시각을 넘기고 **그때까지 끝나 있던 것만** 싣는다. 그러려면 끝난 시각이 결정적이어야 해서
`doneAt`을 **정산 시각이 아니라 시작 + 1시간**으로 찍는다.

### 그 시각을 서버가 버리고 있었다

부상과 같은 길을 타려고 `aiBattle.ts`를 열어 보니 `settleAiBattle`이 `toRosterEntries(profile, picks)` —
**시각 없이** 부르고 있었다. 라우트는 `startedAt`을 받아 `...{ startedAt }`으로 `AiBattleRequest`에
넣는데, **그 타입에 필드가 없었다.** 스프레드는 초과 속성 검사를 안 받아 타입 검사도 회귀도 통과했다.
결과: **부상을 안고 싸운 AI 판은 재생이 어긋나 있었다**(서버는 부상 없이 재생). 필드를 타입에 적고
`[지금 − 2시간, 지금]`으로 눌러 넘기게 고쳤다(GDD §12 결정 이력의 규칙 그대로). CLAUDE.md에 남겼다.

### 순환 import의 TDZ

`academy.ts`가 `profile.ts`의 `RESPEC_GOLD`를 모듈 최상단 상수로 받으면 profile → city → academy →
profile 순환에서 초기화 전에 읽힐 수 있다. 같은 출처(`ECONOMY.respecItemGold`)를 직접 읽었다.

### 문구 파일을 JSON으로 다시 찍으면 절 구분이 사라진다

열 언어에 46키를 넣는 스크립트가 처음에 `JSON.stringify`로 파일을 다시 썼다 — `ko.json`의 **빈 줄
(절 구분)이 전부 사라졌다.** 되돌리고 **글자 단위로** 고쳤다(있는 키는 그 줄의 값만, 새 키는 끝에).

### UX 스크립트가 맞은 동작에 걸렸다

- 「개발용 지급 꺼짐」 갈래에서 새로고침하면 **메인**에 내려앉고, 축하 팝업이 **거기서** 뜬다 —
  사양 그대로인데 스크립트는 태학 화면을 기대했다. 기대를 고쳤다.
- 도시 그림 층이 흘러가듯 움직여(`.scr-art` 드리프트) Playwright가 성문·핫스팟을 「안정되지 않았다」로
  끝없이 기다렸다. `force: true`로 누른다.

## 4. 검증

- typecheck ✓ · `npm test` **686/686** (태학 회귀 21건 · 이유 코드/문구 열 언어 2건 새로)
- 서버 소유 목록 회귀가 `academy`를 요구해 **의도한 변경으로** 고쳤다(우긴 값과 다른 값도 넣었다)
- `tools/academy_ux.ts`(`npm run smoke:academy-ux`) 완주 — 칸 형편 · 시작 · 진행 중 잠금 · 취소 ·
  완료 팝업 · 서버 `notice` 비움 · 끝낸 레벨 잠금 · **메인에서 새로고침 없이 팝업**(끝나기 5초 전으로 당김)
- `server-api`를 새 경로로 다시 띄웠다(옛 프로세스는 `/academy/*`에 404였다 — 「404는 못 닿음이 아니다」)

## 5. 둘째 지정 — 화면 피드백 (같은 날)

기획자가 태학 화면을 보고 준 피드백 넷을 반영했다.

| 지정 | 한 일 |
|---|---|
| [연구하기]는 「연구하기 (Lv1)」 — **그 레벨의 주제 셋만** | `nextResearch()`(meta)가 열 레벨을 정한다. 연구 중 · 태학 레벨 부족 · 다 끝냄이면 [연구하기]가 꺼지고 이유를 단추 밑에 적는다 |
| 되돌리면 Lv1부터 — **전에 끝냈던 레벨까지는 기다림 없이** (처음엔 「태학 레벨까지」 → 셋째 지정으로 좁혔다) | `AcademyState.instantUntil`(되돌리기만 적는다) · `isInstantResearch()` · 즉시 연구는 `doneAt = 지금`이고 축하 팝업도 똑같이 뜬다. 버튼이 「연구 시작 (즉시 완료)」로 바뀐다 |
| 한 줄 = `[지원책] 이름 (MP 1 → 2)` · 바뀐 곳은 취소선 + 빨강 | `screens/tacticDiff.ts` — 설명 둘을 토막(숫자·라틴 낱말·공백·글자 하나)으로 잘라 LCS로 맞춘다. 한중일은 글자 단위, 영어는 낱말 단위로 저절로 갈린다. 회귀가 15종 × 열 언어에서 「이어 붙이면 양쪽 문장이 그대로 돌아온다」를 고정한다 |
| [연구] 단추 대신 **체크 나무판** · 아래 [연구 시작 (1시간 소요) ⏳] · 그 밑 [닫기] — 병기 지급의 장수 선택 판처럼 | `OfficerPickModal`의 틀(`.ofcpick-back` · `.scr-officers` 장부 판 · 제목 줄 [X] · `.ofc-pickacts` · `.frg-back`)과 레벨업 책략 택1의 두루마리(`.lv-tactic-row`) · 체크 나무판(`.lv-check`)을 **그대로** 입었다. 모래시계는 그림 자산이 없어 인라인 SVG다. 시작 확인 팝업은 뺐다 — 체크 + [연구 시작]이 이미 두 번의 의사 표시다 |

- **「순서 무관」이 뒤집혔다.** 화면이 다음 레벨 하나만 보여 주는데 규칙이 아무 레벨이나 받으면 API로만
  건너뛸 수 있어 규칙도 차례로 바꿨다(`canStartResearch`의 `academy.order`).
- **즉시 범위를 「끝냈던 레벨 수」로 좁혔다**(셋째 지정). 처음 구현한 「태학 레벨까지」는 태학 Lv5에서 두 레벨만
  끝낸 채 되돌려 **안 해 본 셋의 대기까지 10냥으로 건너뛸** 수 있어 기획자에게 짚었고, 좁히기로 했다.
  `applyResetAcademy()`가 `done.length`를 적는다(차례로 연구하므로 끝낸 수 = 끝낸 가장 높은 레벨). 진행 중이던
  연구는 안 센다.
- 학파 표식 문구(`officer.support`/`illusion`)가 한국어에만 있어 `academy.school.*`를 열 언어로 새로 뒀다.
  안 쓰게 된 13키(`academy.confirm.*` 등)는 열 파일에서 지웠다.
- 되돌리기 뒤의 즉시 절은 상점 칸이 없어 스크립트가 서버의 같은 함수(`applyResetAcademy`)를 직접 부른다.
- 새 규칙을 받으려고 `server-api`를 한 번 더 다시 띄웠다.

### 셋째 지정 — 즉시 범위를 좁혔다

둘째 지정을 그대로 옮긴 「되돌린 순간의 **태학 레벨**까지 즉시」는 태학 Lv5에서 두 레벨만 끝낸 채 되돌려
**안 해 본 세 레벨의 대기까지 금화 10냥으로 건너뛰는** 길을 열었다. 결과 보고에 그 점을 적자 기획자가
**「전에 끝냈던 레벨 수까지」**로 좁혔다.

- `applyResetAcademy()`가 `instantUntil = done.length`를 적는다 — 차례로 연구하므로 끝낸 수가 곧 끝낸
  가장 높은 레벨이다. **진행 중이던 연구는 안 센다**(끝낸 것이 없으면 즉시가 없다).
- 회귀: 태학 Lv5 · 둘 끝냄 → 되돌림 → Lv1·2 즉시 · Lv3은 1시간. `academy_ux`에 「끝냈던 Lv2는 즉시 ·
  안 해 본 Lv3은 모래시계」 절을 더했다.

### 둘째·셋째 지정의 검증

- typecheck ✓ · `npm test` **693/693** (태학 회귀 23건 · 설명 가르기 4건 · 이유 코드/문구 열 언어)
- `tools/academy_ux.ts` 완주 — 「연구하기 (Lv1)」 · 주제 셋 · 학파 표식 · `(MP 1 → 2)` · ~~1~~2 · 체크 전
  [연구 시작] 꺼짐 · 체크 → 시작(모래시계) · 진행 중 [연구하기] 꺼짐과 이유 · 취소 · 완료 팝업 ·
  [완료된 연구] · 「연구하기 (Lv2)」 · 메인에서 새로고침 없이 팝업 · 되돌리기 → Lv1·Lv2 즉시 · Lv3은 1시간
- 스크린샷으로 판 모양을 확인했다 — 병기 지급 판과 같은 틀 · 레벨업 두루마리 · 체크 나무판. 취소선 숫자가
  새 숫자에 붙어 안 보여 `.acd-old`에 작은 여백을 줬다
- 같은 브랜치에 다른 세션이 **Pawn 원거리 개편**을 먼저 올렸다(`a4bb5e5` · `3d322c7`) — 이 문서가
  남겨 두었던 「증폭+ 이중 소모」는 그 개편으로 풀렸다

## 6. 남은 것

- 연구 되돌리기의 **상점 칸**(서버 경로 `POST /academy/reset`은 섰다 — 즉시 범위 규칙도 그 안에 있다)
- 엑셀 「도시 건물」 태학 줄 고치기 — 기획자
- ~~**Pawn 개편** — 별도 엔진 트랙~~ → 같은 날 다른 세션이 반영했다
- 온라인 대전 로스터를 클라이언트가 보내는 기존 공백 — 개량형도 자칭할 수 있다(태학이 만든 구멍은 아니다)
