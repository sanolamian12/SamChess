# 2026-09-18 병원 화면 — 치료실 현황 · 입원 · 개발용 강제 부상 (트랙 11h)

트랙 11h(건물 넷의 내부 화면) 중 **병원**을 채웠다. 규칙(`packages/meta/src/city.ts`의
`applyHeal`·`freeRooms`)과 서버(`POST /city/heal`)는 2026-09-04에 이미 섰고, **사람이 볼
화면만 없었다.** 산 너머의 병원 이름표를 누르면 「아직 열리지 않았다」 한 줄뿐이었다.

기획자 요청은 셋이었다 — 위에 **운영 현황판**(치료실 수 · 누가 들어와 있는지), 아래에
**명령 단추 판**, 그리고 시험용으로 **장수를 강제로 다치게 하는 길.** 시간은 **분 단위**로,
1분이 안 남으면 「1분 이내」. 현황판은 「치료실 1」「치료실 2」처럼 줄마다 쿨타임이나
「비었음」을 보여 준다.

## 1. 규칙부터 다시 확인했다 — 「넣는 즉시 회복」이 아니다

기획자는 「치료실에 넣으면 즉시 회복되고 방에 쿨타임이 돈다 — 그러면 누가 입원했는지
표기가 어렵지 않나」라고 기억하고 있었다. 확인해 보니 그것은 **2026-09-04의 첫 안**이었고,
경계 두 점(Lv1 15분 · Lv3 1분 이내)을 함께 만족하지 못해 **「치료 1분 + 쿨 5분」**으로
정정된 상태였다(GDD §5.7, `history/2026-09-04_도시건물_부상_확장도시.md`).

그래서 환자 이름이 보이는 것은 **입원 후 첫 1분뿐**이고, 나머지 5분은 누가 쓰고 나갔는지
모르는 쿨타임이다 — 나은 장수의 `healingAt`은 `syncCity()`가 지운다. 이 사실을 알리고
「지금 규칙 유지(A)」와 「즉시 회복으로 되돌림(B, Lv3 3v3이 0분이 된다)」을 물었고,
**A로 확정했다.**

## 2. 치료실 줄은 meta가 낸다 — `hospitalWards(profile, nowMs)`

한 줄은 셋 중 하나다 — `healing`(환자 · 낫는 시각 · 방이 비는 시각) / `cooldown`(비는
시각) / `empty`. 병원 레벨만큼 줄이 선다.

★ **번호는 그릴 때 매긴다.** 저장 형식에는 room의 정체성이 없다(`hospitalBusy`는 해제
시각의 목록일 뿐 — `freeRooms()`의 주석). 바쁜 방이 먼저(먼저 비는 방이 위), 빈 방이
뒤다. 그래서 **「치료실 1」이 비면 아래 줄이 한 칸 올라온다.** 번호를 붙박으려면 저장
형식에 자리를 적어야 하는데 그 값은 이 한 화면 말고 아무도 안 쓴다 — **기획자에게 이
부작용을 보여 주고 받아들이기로 했다.**

환자와 방은 `healingAt + ROOM_CYCLE_MS === 해제 시각`으로 짝짓는다. `applyHeal()`이 두
값을 같은 `nowMs`로 적으므로 정확히 같다. 같은 순간 둘이 들어가도 짝을 **하나씩 소비**한다.

회귀 4건(`city.test.ts`의 「병원 현황판」) — 빈 병원 · 치료 중→쿨타임→비었음 · 줄 순서와
짝짓기 · 치료 중 다시 다침(환자는 빠지고 방은 쿨타임으로 남는다). **짝짓기를 일부러
깨면(`shift()` → `[0]`) 정확히 그 검사가 실패**하는 것을 확인했다.

## 3. 화면 — `HospitalScreen.tsx`

대장간(`ForgeScreen`)과 같은 틀이고, `BuildingScreen`이 `forge`를 넘기듯 `hospital`을 넘긴다.

- **현황판** — `병원 Lv3 · 치료실 3개 · 부상 장수 N명` + 치료실 줄. 치료 중은 붉게, 쿨타임은
  흐리게, 빈 방은 옥색.
- **명령 판** — [입원시키기]·[뒤로 가기]. 안 되는 이유(병원 없음 · 부상 장수 없음 · 빈 방
  없음 + 다음 방이 비는 시각)는 **제 단추 바로 밑에** 적는다.
- **입원 팝업** — 판때기는 대장간 확인 팝업(`.modal.frg-confirm`)을 빌렸다. 줄마다 이름 ·
  `Lv · 무·지·통 −10` · **자연 회복까지 N분** · [입원]. 기다리는 사람이 먼저, 그 안에서 자연
  회복이 먼 사람이 위. 빈 방이나 기다리는 사람이 떨어지면 팝업이 닫힌다.
- **시간** — 분 단위 **올림**(「0분」이 안 나온다), 1분 미만은 「1분 이내」. 5초마다 다시 그린다.
- **입원은 못 닿으면 물러나지 않는다** — `roster`·`hospitalBusy`가 서버 소유(A2)라 로컬로
  `applyHeal` + `PUT`하면 되쓰인다(화면에서만 입원한 유령). `null`은 `server.offline`.
- 화풍은 대장간 규칙(목판·장부·단추·제목 바)에 `.scr-building-hospital`을 **나란히 걸어**
  입고, 병원에만 있는 것은 `hsp-` 접두사다.

## 4. 개발용 강제 부상 — `/dev/grant`의 `injure`

`roster`가 서버 소유라 화면에서 부상을 찍어 `PUT`하면 **조용히 되쓰인다.** 그래서 장터의
금화·카드 지급과 같은 길(`/dev/grant`, `SAMCHESS_DEV_GRANTS=1`일 때만)에 `injure:
OfficerId[]`(10명까지, 아는 장수만)를 더했다. 서버가 **자기 시계로** 전투 퇴각과 같은
`applyInjuries()`를 부른다 — 헌제는 규칙이 건너뛴다. 화면 단추는 병원 맨 아래 개발용
줄의 [장수 3명 부상시키기](건강한 장수 앞에서 셋).

⚠ `.env`에는 `SAMCHESS_DEV_GRANTS=1`이 없다. 이번에는 떠 있던 8787 `server-api`(수정 전
코드)를 끄고 **환경변수로만** 켜서 다시 띄웠다. `.env`는 안 건드렸다.

## 5. ★ 몽골어에서 두 군데가 깨졌다

`tools/hospital_ux.ts`(새로 — 병원 Lv3 · 부상 둘 · **환자 없는 쿨타임 방 하나**를 서버
쪽에 심고 강제 부상 → 입원 → 방이 다 참까지 돈다)를 `LANG_UI=mn`으로 돌려 잡았다.

1. **입원 팝업에서 장수 이름이 사라졌다.** `1fr auto auto` 줄에서 긴 「Өөрөө эдгэрэх
   хүртэл 60 мин」(자연 회복까지)이 폭을 다 가져가 이름 칸이 **0폭**이 됐다(말줄임이
   걸려 있어 글자가 통째로 안 보였다). 남은 시간을 **이름 아래 셋째 줄**로 내렸다.
2. **현황판 치료 중 줄이 말줄임으로 잘려 남은 시간이 안 보였다** — 잘리는 쪽이 늘 문장
   끝(시간)이다. 말줄임을 걷고 줄바꿈을 허용했다.

한국어 스크린샷에서는 둘 다 멀쩡했다 — **긴 번역으로 한 번 돌리지 않으면 안 보이는
자리**의 또 한 예다(09-17 부대 현황의 「Шархтай」, 09-18 표 머리와 같은 결).

곁가지 — 확인 도구의 「0분이 보이는가」 검사를 `/0분/`으로 적었다가 **「60분」에 걸려**
멀쩡한 화면을 실패로 찍었다. `(^|\D)0분`으로 고쳤다.

## 6. 시간이 실제로 흐르는가

기획자가 「강제 부상 시점부터 시간이 줄어드는가」를 물었다. 앞의 확인은 「60분」「10분」이
**보이는 것**까지였고 **줄어드는 것**은 안 봤다. 확인 도구에 `CHECK_TICK=1`(65초 기다림,
기본은 끔)을 더해 `60,60,60,60,10 → 59,59,59,59,9`를 확인했다 — 올림이라 방금 다친
장수는 **꼬박 1분이 지나야** 59분이 된다. 기획자도 자기 화면에서 개발용 단추로 부상을
만들고, **치료 1분이 끝나자 치료실 1·2가 쿨타임 줄로 바뀌는 것**을 확인했다.

(기획자 화면의 계정을 찾으려고 Supabase 사용자 목록을 조회하려 했으나 개인정보 조회로
권한이 거부됐다 — 개발용 단추를 쓰도록 안내했다.)

## 문구

새 키 20개(`hospital.*`)를 **열 언어 전부** 채웠다. 아홉 언어에 없던 `place.hospital`(병원
제목)도 함께 넣었다. es/pt는 저장소의 결대로 악센트 없이, 레벨 약칭은 `Nv`.

## 검증

- `npm run typecheck` ✓ · `npm test` **603/603**
- `tools/hospital_ux.ts` — 한국어 · 몽골어 완주, `CHECK_TICK=1` 완주, 콘솔 오류 0

## 남은 것

- 트랙 11h의 나머지 — **태학 · 농지**는 여전히 그림과 현황 한 줄뿐이다(태학은 사양부터).
- `smoke:meta`에 병원 절은 아직 없다(11i). 개발용 부상이 `SAMCHESS_DEV_GRANTS=1`을 요구하므로
  스모크에 넣으려면 서버 쪽 심기(`saveProfileTrusted`)로 가는 편이 맞다 — `hospital_ux.ts`가 그렇게 한다.

---

# 같은 날 후속 — 현황판 통일 · 궁궐 현황판 · 도시 관리 정리 · 이유 문구 다국어

병원을 확인한 기획자가 **건물 화면들의 현황판을 한 꼴로** 맞추자고 했고, 궁궐·도시 관리·
건물 관리까지 이어서 손봤다. 전부 기획자가 화면을 보고 지정한 것이다.

## 병원 손질

- 명령 판의 [뒤로 가기]를 뺐다 — 나가는 문은 제목 바의 뒤로 화살표 하나다.
- [입원시키기]는 **입원할 수 있을 때만** 옥색 목판(`assets/icons/button_primary.png` →
  `ui/btn-primary.png`, 이미 구워져 있었다).
- 몽골어만 치료 중 줄의 「(남은 시간)」을 **다음 줄로** — 「치료 중 · 이름」과 「(시간)」을
  두 문구(`hospital.ward.healing` · `.time`)로 갈라, `lang === 'mn'`일 때만 `<br>`을 넣는다.
- ★ **개발용 줄이 스위치를 몰랐다.** 내가 「스위치」라고 부른 것은 서버의
  `SAMCHESS_DEV_GRANTS`였는데, 기획자는 화면의 [개발용 [장수 3명 부상시키기]] 줄로 읽었고
  그 줄은 스위치를 끈 뒤에도 늘 그려져 있었다(누르면 「꺼져 있다」만 받는다). 화면은 서버의
  설정을 알 길이 없어 **`GET /dev/status`**(`{grants}`, 인증 없음)를 새로 두고, 켜져 있을 때만
  그린다. 못 닿거나 낡은 서버(404)면 숨는다. 장터의 개발용 단추는 그대로다.

## 현황판의 「{건물} Lv{n}」

- 대장간 `Lv4` → **「대장간 Lv4」**(`forge.summary.level`, 열 언어가 각자 `place.forge`를 앞에).
- 병영 현황판 맨 앞(군량 아이콘 왼쪽)에 **「병영 Lv1」**(`barracks.summary.level`).
- 병원 몽골어의 약칭 「Түв」를 대장간과 같은 「Lv」로 맞췄다. es/pt는 모두 「Nv」.
  ⚠ 처음에 병영의 es/pt가 「Lv」로 들어갔다 — 대장간 문구가 이제 이름으로 시작해
  `startswith('Nv')` 판별이 빗나갔다.

## 궁궐 첫 화면 — 현황판 신설 (두 번 고쳐 확정)

```
             궁궐실험            [이름 변경]
 도시 Lv4             황제 : 옹립
 궁궐 Lv2             등용 장수 : 6 / 110 명
```

- 첫 안은 「도시 관리의 정보 칸을 옮겨 오되 왼쪽 단 도시 · 오른쪽 단 궁궐」이었고, 기획자가
  **네 칸 · 두 단 · 왼쪽 정렬**(윗줄 도시 레벨·황제, 아랫줄 궁궐 레벨·등용 장수)로 다시 정했다.
  글꼴은 「병영 Lv1」과 같다.
- **[이름 변경]을 도시 관리에서 옮겨 왔다**(팝업·`renameCityOnServer` 째로). 판 오른쪽 벽에
  붙고 붉은 목판(`button_forcedcancel.png` → `ui/btn-forcedcancel.png`, 대장간 [제작 취소]와
  같은 그림)이다. `PlaceScreen`이 계정을 바꿀 길이 없어 `onChange`를 받게 했다.
- ★ **이름이 가운데가 아니었다** — flex로 이름과 단추를 함께 가운데 두면 단추가 이름을
  52px 왼쪽으로 민다. 세 칸 격자(`1fr auto 1fr`)의 가운데 칸을 이름이 차지하게 했다.
- ★ **팝업은 판 밖에 둔다** — 판(`.scr-bg > *`로 `position: relative`) 안에 두면 판 크기만
  한 팝업이 된다. 조각(`<>`)으로 판 옆, 화면 바로 밑에 둔다. 확인 도구가 「팝업이 화면
  가운데인가」를 좌표로 본다(「있는가」와 「제자리인가」는 다른 검사).

## 도시 관리 — 두 줄 · 바닥 명령 판

- 이름·[이름 변경]·황제 줄을 뺐다(궁궐로 갔다).
- 두 줄 — 「도시 레벨 Lv4」 · 「건축 자재 75 / 25 (증축 조건 충족)」. 괄호는 **자재가 다
  모였을 때만**이고, **자재 조건만** 본다(장수가 모자라면 단추 밑에 따로 뜬다 — 기획자에게
  알렸다). 한때 「(Lv5 필요 건축 자재 : 25)」를 레벨 줄에 붙였다가 이 꼴로 바뀌어 그 키는 지웠다.
- 「보유 장수가 모자란다」는 판 밖 주황 글씨였던 것을 **[도시 증축] 바로 밑, 판 안** 짙은 붉은
  글씨로(`.cty-whys` — 옛 `.cty-alert` 규칙은 걷었다).
- 명령 판을 **화면 바닥**에 — `.scr-city .place-body`가 남는 높이를 채우는 세로 flex라
  `.scr-city .place-panel.cty-acts { margin-top: auto }` 하나로 된다. 팝업 안의 `.cty-acts`(단추
  줄)는 판이 아니라 안 걸린다.
- **건물 관리**에도 같은 판으로 바닥 [뒤로 가기](도시 관리로).

## ★ 규칙의 이유 문구를 번역한다 — `MetaResult.code`

도시 관리의 정보 문구(황제·건축 자재·등용 장수 등 9키)가 **한국어에만** 있었다 — 도시 관리
안쪽이라 가려져 있다가 궁궐 첫 화면에 나오며 드러나 아홉 언어를 채웠다. 그런데 **「보유 장수가
모자란다」는 문구 파일이 아니라 규칙이 짓는 한국어 문장**이라 열 언어에서 그대로 한국어였다.

- `MetaResult` 실패에 선택 항목 **`code`·`params`**를 더했다. `reason`(한국어)은 그대로 —
  서버가 400으로 돌려주는 말이고 로그·API 소비자가 읽는다.
- `canUpgradeCity()`의 여섯 이유(상한·최대 레벨·건설 기회·보유 장수·S·A급·자재)에 코드를 붙이고
  목록 **`CITY_UPGRADE_REASONS`**를 내보낸다.
- 화면은 `reasonText()`(`client/src/i18n/reason.ts`) — 코드가 있으면 `reason.{code}`, 없으면 원문.
- 회귀 둘 — meta는 「실패마다 목록 안 코드가 붙고 `params`의 숫자가 원문에 있다」, client는
  **「코드마다 열 언어 문구가 있다」**(`reasonStrings.test.ts`). 영어 한 키를 일부러 지워
  **정확히 그 자리에서 실패**하는 것을 확인했다 — `t()`는 없으면 한국어로 물러나 화면이 아무 말도
  안 하기 때문에 이 검사가 유일한 경보다.
- ⚠ **나머지 규칙의 이유(건설·가챠·레벨업 …)와 서버가 거부한 이유는 여전히 한국어 원문**이다.
  같은 방식으로 넓히면 된다.

## 검증

- `npm run typecheck` ✓ · `npm test` **605/605** · `smoke:meta` 완주(✓66) —
  `smoke_meta.ts`가 황제를 도시 관리에서 읽고 있어 **궁궐 현황판에서** 읽게, 자재 검사는
  「0 / 10」 꼴로 고쳤다.
- `tools/palace_ux.ts`(새로) — 도시 Lv4 · 궁궐 Lv2 · 자재 75 · 헌제를 심는다(새 계정은 두
  레벨이 같아 칸이 바뀌어도 안 보인다). 네 칸 차례 · 왼쪽 끝 맞춤 · 이름 가운데(4px) · 단추가
  벽에 붙음 · 단추 그림 · 팝업 위치 · 도시 관리의 빠진 줄 · 「75 / 25」+충족(그리고 **자재 5면
  괄호가 없다** — 늘 띄워도 통과하지 않게) · 이유가 단추 바로 밑 · **한국어가 아닌 화면에
  한국어 이유가 없다** · 두 명령 판이 바닥 · 건물 관리 [뒤로 가기] 왕복. 한국어·몽골어 완주.
- `hospital_ux.ts`는 `/dev/status`로 스위치를 물어 **개발용 줄이 그와 같이 보이거나 숨는가**를
  보고, 꺼져 있으면 부상을 서버 쪽에서 심는다. `squad_ux.ts`·`forge_play.ts`로 병영·대장간을 찍었다.
- 계정 서버(8787)는 `/dev/status`를 싣느라 **스위치 없이** 다시 띄웠다(`grants: false`).
