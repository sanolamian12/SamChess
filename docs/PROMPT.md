# 삼국지 SD 캐릭터 카드 — Gemini 티칭(가이드) 프롬프트

원작 삼국지 인물 초상화 3장 → 귀여운 2등신 캐릭터 3명이 든 카드 1장(1376×768)으로 변환.

## 이 문서 사용법 (2단계 대화)

Gemini 새 채팅을 열고 **두 번에 나눠** 준다.

- **[1단계] 스타일 학습** — 아래 「티칭 프롬프트」를 붙이고, **정규형 예시 output 1장을 함께 첨부**한다.
  Gemini가 "이 스타일 이해했다"고 답하면 성공. (이 단계에서는 그림을 그리지 않는다.)
- **[2단계] 실제 생성** — 인물 초상화 3장을 첨부하고 이름을 알려준다. 그러면 그때 카드를 그린다.
  → 이 단계 문구는 맨 아래 「2단계 트리거 문장」 참고.

**예시로 첨부할 파일**: 반드시 **3인 완성본**을 원본 그대로(풀해상도) 준다.
가장 깔끔하고 귀여운 추천본 → `images/17/output` 또는 `images/20/output` 의 PNG.
(별점을 원치 않으면 `images/1/output`처럼 별점 없는 것을 고르거나, 티칭 프롬프트의 별점 줄을 지운다.)

---

## ★ 티칭 프롬프트 (1단계 — 예시 이미지와 함께 붙여넣기)

```
너는 지금부터 '삼국지 모바일 게임'의 캐릭터 셀렉트 화면을 그리는 픽셀아트 아티스트다.
방금 첨부한 이미지는 내가 원하는 결과물의 "정답 예시(레퍼런스)"다.
지금은 그림을 그리지 마라. 먼저 이 예시의 화풍을 아래 설명과 대조하며 완벽히 학습하고,
이해했다고만 답하라. 이후 내가 인물 초상화 3장과 이름을 주면, 그때 이 예시와
"똑같은 스타일"로 카드 1장을 그린다.

── 이 예시를 이렇게 읽어라 (다음에 그릴 때 반드시 지킬 규칙) ──

[1] 캔버스
- 크기: 가로 1376 × 세로 768 픽셀, 정확히 이 크기의 가로형 이미지 1장.
- 테두리: 화면 가장자리까지 꽉 채우는 직사각 금색 이중선 프레임(네 모서리에 작은 장식).
  둥근 모서리 카드나 여백 있는 카드가 아니다 — 프레임이 캔버스 끝에 붙는다.

[2] 화풍 (★★ 가장 중요)
- 16비트 레트로 게임풍 "도트 픽셀아트". 각진 픽셀 계단이 눈에 보이고, 굵은 검정 외곽선,
  면 단위 셀셰이딩, 제한된 색 팔레트, 따뜻한 채도.
- 매끈한 벡터·일러스트·카툰·에어브러시·3D 렌더 느낌은 절대 금지.
  반드시 픽셀이 도드라지는 도트 그림이어야 한다.

[3] 캐릭터 비율 (★★ 가장 중요)
- 머리 : 목 아래 몸 전체 = 1 : 1 ~ 1 : 1.5.
  즉 전체 키가 "머리 약 2~2.5개" 높이인 초귀여운 SD(등신) 캐릭터.
- 머리를 크고 몸을 작고 짧게. 절대 사실적인 3~4등신으로 키우지 마라.

[4] 얼굴 (★★ 가장 중요)
- 둥글둥글하고 귀여운 얼굴. 큰 머리, 부드럽고 통통한 볼, 단순화된 큰 눈, 작은 코/입.
- 사납거나 사실적인 성인 얼굴 비율 금지. 아기자기하고 순한 인상으로.

[5] 구도
- 캐릭터 3명을 세로 구분선 2개로 나뉜 3칸에 각각 1명씩, 정면 전신 입상으로.
- 각 캐릭터 발밑에 작은 잔디 더미와 회색 돌 1개.
- 세 명의 크기·눈높이·발 위치를 서로 맞춰 통일감 있게.

[6] 배경
- 낡은 양피지에 손으로 그린 고대 중국(후한) 지도. 베이지·세피아 종이색,
  강·바다는 흐린 청록. 은은하게 — 캐릭터보다 튀지 않게.

[7] 로고
- 좌상단 구석에 작은 게임 로고: 붉은 한자 "三國志" 아래 작은 영문 "MOBILE GAME".

[8] 이름표 / 별점
- 각 캐릭터 아래 나무·양피지 명패에 한글 이름. (예시에 상단 명패도 있으면 상단에도 동일하게.)
- 이름표·별점의 위치와 형태는 "첨부한 예시에 있는 그대로" 따른다.
  ※ 별점을 넣지 않으려면: 명패에는 이름 글자만 넣고 별·등급 표시는 그리지 마라.
- 이름 자리에는 실제 한글 이름만. 대괄호 [ ]·꺾쇠 < > 같은 기호는 절대 그리지 마라.

── 초상화를 캐릭터로 바꾸는 방법 ──
- 내가 줄 초상화는 원작 삼국지의 사실적 인물 얼굴이다. 이걸 위 예시 화풍의
  귀여운 2등신 캐릭터로 "재해석"해라(그대로 붙여넣기가 아니라 새로 그리기).
- 각 인물의 얼굴·수염·투구/관모·복색을 알아보게 유지하되, 그 인물의 정체성에 맞는
  시대 복식과 시그니처 무기/지물을 들려라
  (무장 = 갑옷 + 무기, 책사 = 도포 + 부채/죽간, 궁수 = 활 등).
- 첨부 초상화 3장의 왼→오 순서를 카드 3칸의 왼→오 순서에 그대로 매핑.

── 그리기 직전 자가 점검 ──
출력 전 스스로 확인하고, 하나라도 아니면 고쳐서 다시 그려라:
(a) 이미지 사이즈가 1376×768 픽셀 인가  
(b) 도트 픽셀 질감인가  
(c) 머리:몸 비율은 1:1~1.2 로 귀여운가
(d) 얼굴이 둥글고 귀여운가  
(e) 프레임이 가장자리까지 꽉 찼는가.

지금은 위 스타일을 이해했는지만 답하고, 내가 초상화 3장을 줄 때까지 대기하라.
```

---

## 2단계 트리거 문장 (초상화 3장 첨부하며 보낼 말)

```
앞서 학습한 예시 스타일 그대로, 첨부한 이 초상화 3장을 카드 1장으로 그려줘.
왼→오 순서 그대로. 명패 이름은 왼쪽부터: 마속 / 우금 / 악진.
(1376×768, 도트 픽셀아트, 머리:몸 1:1~1.5 둥근 귀여운 얼굴 — 예시와 똑같이.)
```

- `마속 / 우금 / 악진` 자리에 실제 이름을 **괄호 없이** 적는다.
- 결과가 예시와 어긋나면: 어긋난 항목만 콕 집어 재지시한다
  (예: "머리를 더 크게, 2등신으로", "매끈해졌어 — 각진 도트 픽셀로", "프레임을 가장자리까지").

---

## English 버전 (모델이 영어에서 더 안정적일 때)

### Teaching prompt (Step 1 — send WITH the example image)
```
From now on you are a pixel-art artist drawing the character-select screen of a
"Three Kingdoms mobile game". The image I just attached is the REFERENCE — the exact
result style I want. Do NOT draw yet. First study its art style against the notes below
and confirm you understand. Later I will send 3 character portraits + names, and THEN you
draw one card in this EXACT same style.

── Read the reference like this (rules you MUST follow when drawing) ──

[1] CANVAS
- Exactly 1376 x 768 px, one landscape image.
- A rectangular gold double-line border filling the canvas EDGE-TO-EDGE (small corner ornaments).
  NOT a rounded card, NOT a card with margins — the frame touches the canvas edges.

[2] ART STYLE (MOST IMPORTANT)
- 16-bit retro-game DOT PIXEL ART: visible blocky/stair-stepped pixels, thick black outlines,
  flat cel-shading, limited palette, warm saturation.
- Absolutely NO smooth vector / illustration / cartoon / airbrush / 3D look. It MUST read as pixel art.

[3] PROPORTIONS (MOST IMPORTANT)
- Head : body-below-neck = 1 : 1 to 1 : 1.5  (total height ≈ 2 to 2.5 heads).
- Big head, small short body. NEVER a realistic 3–4-head-tall figure.

[4] FACE (MOST IMPORTANT)
- Round, cute face: big head, soft chubby cheeks, simplified big eyes, small nose/mouth.
- No fierce or realistic adult proportions — keep it adorable and gentle.

[5] COMPOSITION
- 3 characters, one per panel, split by two vertical dividers; front-facing full-body standing.
- A small grass tuft with one grey rock under each. Match size, eye level and foot line across all three.

[6] BACKGROUND
- Hand-drawn ancient-China (Later Han) map on aged parchment; beige/sepia, faint teal water. Keep it subtle.

[7] LOGO
- Top-left: red "三國志" with small "MOBILE GAME" beneath.

[8] NAME PLATES / STARS
- Korean name on a wooden/parchment plate under each character (and on top too if the reference has it).
- Copy the plate/stars layout EXACTLY from the attached reference.
  (To omit stars: put only the name text, draw no stars.)
- Put ONLY the real Korean name — never draw brackets [ ] or < >.

── How to convert portraits ──
- The portraits I give are realistic Three-Kingdoms faces. REDRAW each as a cute 2-head chibi in the
  style above (not a paste-in). Keep face/beard/helmet/cap/colors recognizable, dress them in period
  costume with their signature weapon/prop (warrior = armor+weapon, strategist = robe+fan/scroll, archer = bow).
- Map the 3 portraits left→right onto the 3 panels left→right.

── Self-check before drawing ──
(a) 1376×768?  (b) dot-pixel texture?  (c) head:body 1:1–1.5 and cute?  (d) round cute face?
(e) frame edge-to-edge?  Fix and redraw if any is "no".

For now, only confirm you understand. Wait until I send the 3 portraits.
```

### Trigger (Step 2 — send with the 3 portraits)
```
Now draw ONE card from these 3 attached portraits in the EXACT style you just learned.
Same left→right order. Name plates left→right: 마속 / 우금 / 악진.
(1376×768, dot pixel art, head:body 1:1–1.5, round cute faces — identical to the reference.)
```

---

---

# 궁궐 UI 패널 배경 — 두루마리/목판 프레임 (고유기술 팝업 등)

**용도**: `.modal[data-modal="settings"]`가 이미 쓰는 것과 같은 9분할
`border-image` 프레임(`panel_settings.png`)을, 고유기술 팝업
(`.ofc-skill-modal`, `assets/icons/panel_skill.png`)에도 새로 입힌다.
이런 팝업/패널 배경 그림을 새로 하나 더 받아야 할 때는 이 절을 그대로
재사용하고 **자리 이름(파일명)만** 바꾼다.

## 이 문서 사용법

이미지 생성 AI(Gemini 등)에 아래 프롬프트를 그대로 준다. 예시 이미지가
있으면(예: 이미 만든 `panel_settings.png`) 함께 첨부해 "이 화풍 그대로,
다만 아래 조건에 맞는 새 패널로"라고 덧붙이면 더 안정적이다.

## ★ 프롬프트

```
삼국지 배경의 모바일 게임에서 쓸 UI 팝업 패널 배경 그림을 그려줘.

[1] 캔버스
- 세로로 긴 직사각형 1장 (예: 900×1400px 안팎, 정확한 비율은 자유— 어차피
  CSS가 9분할로 늘려 쓴다).
- 그림이 캔버스 가장자리까지 꽉 찬다. 여백이나 둥근 모서리, 그림자 없이
  네모 반듯한 판 하나가 캔버스를 통째로 채운다.

[2] 화풍 (★ 가장 중요)
- 낡은 두루마리(양피지/종이)이거나 옻칠한 고급 목판 — 고대 중국 후한~삼국
  시대 궁궐에서 쓸 법한 재질. 손으로 그린 수채화 느낌의 질감(매끈한
  벡터·3D 렌더 금지).
- 색은 짙은 밤색·세피아·고동색 계열 바탕에 금색·청동색 장식선. 전체적으로
  어둡고 차분한 톤 — 그 위에 크림색·금색 글자를 얹을 것이므로 **바탕이
  너무 밝거나 알록달록하면 안 된다.**

[3] 테두리 (★ 가장 중요 — 9분할로 늘어난다)
- 네 변에 골고루 두꺼운 테두리 장식(금박 무늬, 옻칠 나뭇결, 못대가리 등
  반복 가능한 문양)을 두른다. 위·아래 테두리가 특히 두껍게(그림 전체 세로
  길이의 약 15%), 좌우는 그보다 살짝 얇게(약 16%의 폭 — 즉 세로가 가로보다
  비율상 더 큰 테두리).
- **테두리 문양은 이어 붙였을 때(반복/늘림) 자연스러워야 한다** — 특정
  위치에만 있는 독특한 장식(용 조각 하나, 글자 낙관 등)을 테두리 중간에
  넣지 마라. 네 귀퉁이에만 작은 장식(구름무늬·매듭 등)을 넣는 것은 괜찮다.

[4] 가운데 (★ 가장 중요 — 늘려서 배경으로 그대로 쓴다)
- 테두리 안쪽 가운데 영역은 인물·글자·큰 무늬 없이 **낡은 종이/목판의
  결·얼룩·은은한 구름무늬 정도의 잔잔한 질감**만 있어야 한다. 세로·가로로
  늘어나도 어색하지 않은, 패턴이 없는 균일한 질감이어야 한다 —
  가운데에 그림(용·봉황·인물 등)을 크게 그려 넣으면 늘어나면서 심하게
  일그러진다.

[5] 참고
- 이미 있는 환경설정 팝업 배경(`panel_settings.png`, 세로 두루마리
  9분할 프레임)과 같은 세트로 보여야 한다 — 재질·색감·장식 밀도를
  맞춰라.

── 그리기 직전 자가 점검 ──
(a) 세로로 긴 사각형이 캔버스 가장자리까지 꽉 찼는가
(b) 두루마리/목판 질감이고 벡터·3D가 아닌가
(c) 바탕이 어두운 세피아·밤색 계열이라 크림·금색 글자가 잘 보이겠는가
(d) 테두리 문양이 반복해도 자연스러운가, 중간에 독특한 장식이 끼어있지 않은가
(e) 가운데 영역이 패턴 없이 잔잔한 질감뿐인가
```

## 적용하는 법

1. 위 프롬프트로 받은 그림을 `panel_skill.png`로 `assets/icons/`에 넣는다
   (레포에 커밋되는 폴더가 아니다 — 로컬에만 있으면 된다).
2. `npm run ui` (`tools/build_ui.py`)를 돌리면 `packages/client/public/ui/panel-skill.png`로
   구워진다.
3. 고유기술 팝업(`.ofc-skill-modal`, `style.css`)이 이미 그 파일을 참조하도록
   배선돼 있다 — 그림만 넣으면 바로 반영된다. 잘려 보이면 `style.css`의
   `.modal.ofc-skill-modal`의 `border-image` 슬라이스 값(`15% 16%`)을
   실제 그림 테두리 두께에 맞춰 눈대중으로 조정한다(`.modal[data-modal="settings"]`와
   같은 방식 — 자·표가 따로 있는 게 아니다).

---

## 팁 / 자주 나는 실패

- **가장 흔한 실패 3종**
  ① 매끈한 일러스트로 나옴 → "각진 도트 픽셀아트, 매끈한 일러스트 금지"를 다시 강조.
  ② 키가 커지고 안 귀여움 → "머리:몸 1:1~1.5, 머리 크게, 둥근 얼굴"을 다시 강조.
  ③ 둥근 카드/여백 → "프레임을 캔버스 가장자리까지 꽉 채워"라고 재지시.
- **참조가 있을 땐 긴 설명이 이미지와 경쟁**해 모델이 자기 스타일로 회귀한다. 그래서 2단계 트리거는
  짧게 유지하고, 화풍은 예시 이미지에 맡긴다.
- 이름은 오식별 방지를 위해 직접 알려주는 게 안전하다(초상화만으로 인물 추정에 맡기지 말 것).
- 한 메시지에 예시 1장 + 초상화 3장을 같이 주면 예시까지 변환하려 든다 → 반드시 1·2단계를 나눠라.
- Gemini 이미지 모델은 생성마다 편차가 남는다. 픽셀 단위 완전 동일이 필요하면, 새로 생성하는 대신
  기존 output 1장을 열어 얼굴/이름만 부분 편집(인페인팅)하는 방식이 가장 확실하다.

---

# 능력치 줄 명패 — HP·MP·AT 한 줄짜리 가로 액자

**용도**: 레벨업 「고르기」의 HP·MP·AT 세 줄(`.lv-stat`, `style.css`의
`.scr-officers .lv-stat`)에 깔 가로로 긴 배경 액자. 위 「궁궐 UI 패널 배경」과
같은 9분할 `border-image` 기법이지만 **모양이 정반대다** — 그쪽은 세로로 긴
패널, 이쪽은 **납작하고 가로로 긴 명패** 한 줄이다.

첫 시도(`stat_frame_raw.png`, 정사각 금테 액자)를 그대로 씌워 봤더니 세로가
납작한 줄에서 네 귀퉁이 장식이 짓눌려 예쁘지 않았다(2026-09-03) — 그래서 지금은
**배경 그림 없이** 양피지 위에 글자만 얹어 두고, 이 프롬프트로 받은 그림이
오면 다시 입힌다.

## ★ 프롬프트

```
삼국지 배경의 모바일 게임에서 쓸 UI 명패(가로로 긴 한 줄짜리 배경) 그림을 그려줘.

[1] 캔버스
- 가로로 길고 납작한 직사각형 1장 (예: 900×160px 안팎 — 가로:세로 약 5:1 ~ 6:1).
- 그림이 캔버스 가장자리까지 꽉 찬다. 여백·둥근 모서리·드롭섀도 없이
  네모 반듯한 명패 하나가 캔버스를 통째로 채운다.

[2] 화풍
- 낡은 나뭇결 목판 또는 옻칠한 명패 — 고대 중국 후한~삼국시대 분위기.
  손으로 그린 수채/과슈 질감(매끈한 벡터·3D 렌더 금지).
- 이 그림 위에 「HP   10   +5 →   15」 같은 **짙은 먹색 글자**가 얹힌다.
  그러니 바탕은 **밝고 차분한 색**이어야 한다 — 밝은 나무색·연한 베이지·
  옅은 갈색 계열. 어둡거나 알록달록하면 글자가 안 보인다.
- 이미 있는 두루마리 패널(`panel_ledger.png`)·목판 버튼(`btn_secondary.png`)과
  같은 세트로 보여야 한다 — 재질·색감·장식 밀도를 맞춰라.

[3] 테두리 (★ 9분할로 늘어난다)
- 네 변에 얇고 균일한 테두리 장식(가는 금색·청동색 선, 나뭇결 몰딩 등).
  **위·아래 테두리는 그림 세로 길이의 약 12~15%, 좌우는 가로 길이의 약 4~6%**
  — 납작한 그림이라 좌우 테두리는 비율상 아주 얇아야 한다.
- 테두리 문양은 늘려 붙여도 자연스러운 **반복 가능한 결**이어야 한다.
  가운데에만 있는 독특한 장식(용 한 마리, 낙관 등)을 변 중간에 넣지 마라.
- 좌우 끝(양 마구리)에만 작은 장식(못대가리, 매듭, 두루마리 봉 끝 같은
  마감)을 넣는 것은 괜찮다 — 9분할에서 그 자리는 안 늘어난다.

[4] 가운데 (★ 늘려서 배경으로 그대로 쓴다)
- 테두리 안쪽은 글자·무늬·그림 없이 **잔잔한 나뭇결/종이 질감**만.
  가로로 크게 늘어나므로 패턴이 없고 균일해야 한다.

── 그리기 직전 자가 점검 ──
(a) 가로로 납작한(5:1 안팎) 직사각형이 캔버스를 꽉 채웠는가
(b) 밝은 나무·베이지 계열이라 짙은 먹색 글자가 잘 보이겠는가
(c) 좌우 테두리가 위아래보다 비율상 얇은가
(d) 가운데가 패턴 없이 잔잔한 질감뿐인가
(e) 매끈한 벡터·3D가 아니라 손그림 질감인가
```

## 적용하는 법

1. 받은 그림을 `assets/icons/stat_frame_raw.png`로 넣는다(지금 있는 정사각
   액자를 덮어쓰면 된다 — 레포에 커밋되는 폴더가 아니다).
2. `npm run ui`를 돌리면 `packages/client/public/ui/stat-frame.png`로 구워진다
   (`tools/build_ui.py`의 배선은 이미 있다).
3. `style.css`의 `.scr-officers .lv-stat`에 **주석으로 넣어 둔 두 줄**의 주석만
   풀면 바로 입혀진다. 테두리가 잘리거나 뭉개져 보이면 그 줄의
   `border-image` 슬라이스(`22%`)와 `border-width`를 실제 그림 테두리 두께에
   맞춰 조정한다 — 위 팝업 패널 절과 같은 방식(눈대중)이다.

---

# 증축 완료 팝업 배경 — 축하 판 (2026-09-04)

**용도**: 도시 증축이 끝나면 뜨는 축하 팝업(`.cty-done`, `CityScreen.tsx`의
`UpgradeDoneModal`)의 9분할 `border-image` 배경. 「궁궐 UI 패널 배경」과 같은
기법이지만 **뜻이 다르다** — 그쪽은 정보를 읽는 차분한 판, 이쪽은 **한 번 크게
축하하는 판**이다. 판 가운데에 오른 레벨이 큰 금색 숫자(`Lv2`)로 들어간다.

**받는 파일**: `assets/icons/panel_done.png`

## ★ 프롬프트

```
삼국지 배경의 모바일 게임에서 쓸 "축하 팝업" 배경 그림을 그려줘.
도시를 한 단계 증축(업그레이드)했을 때 뜨는 판이다.

[1] 캔버스
- 세로로 살짝 긴 직사각형 1장 (예: 900×1100px 안팎. 정확한 비율은 자유 —
  어차피 CSS가 9분할로 늘려 쓴다).
- 그림이 캔버스 가장자리까지 꽉 찬다. 여백·둥근 모서리·드롭섀도 없이
  네모 반듯한 판 하나가 캔버스를 통째로 채운다.

[2] 화풍 (★ 가장 중요)
- 고대 중국 후한~삼국시대 궁궐의 **경사스러운 방** 느낌. 붉은 옻칠 목판에
  금박 장식, 또는 붉은 비단을 두른 액자. 손으로 그린 수채/과슈 질감
  (매끈한 벡터·3D 렌더·사진 합성 금지).
- 색은 **짙은 주홍·팥색 바탕 + 금색 장식**. 그 위에 크림색 글자와
  큰 금색 숫자가 얹히므로 바탕이 너무 밝거나 노랗지 않아야 한다.
- 이미 있는 두루마리 패널(`panel_settings.png`·`panel_ledger.png`)과 **같은
  세계의 물건**으로 보여야 한다 — 재질·붓질·장식 밀도를 맞추되, 색만
  경사스러운 붉은 계열로 간다.

[3] 테두리 (★ 9분할로 늘어난다)
- 네 변에 두꺼운 테두리 장식(금박 넝쿨무늬, 옻칠 몰딩, 구름무늬 등
  **반복 가능한** 문양). 위·아래는 세로 길이의 약 15%, 좌우는 가로 길이의
  약 13%.
- 변 **중간**에 한 번만 나오는 큰 장식(용 한 마리, 낙관, 글자)은 넣지 마라 —
  늘어나면서 일그러진다. 네 귀퉁이의 작은 장식(매듭·구름·못대가리)은 좋다.

[4] 가운데 (★ 늘려서 배경으로 그대로 쓴다)
- 테두리 안쪽은 **글자·인물·큰 무늬 없이** 잔잔한 질감만 — 옻칠 결, 비단 짜임,
  아주 은은한 방사형 광채 정도까지는 좋다. 가로세로로 늘어나도 어색하지 않게
  균일해야 한다.
- 특히 **가운데 위쪽 1/3은 더 비워 둔다** — 거기에 큰 금색 숫자(Lv2)가 얹힌다.

[5] 분위기
- 폭죽·색종이·별가루 같은 현대적 축하 소품은 금지. 시대에 맞는 경사의 표현
  (붉은 비단, 금박, 은은한 광채)만 쓴다.

── 그리기 직전 자가 점검 ──
(a) 세로로 살짝 긴 사각형이 캔버스 가장자리까지 꽉 찼는가
(b) 붉은 옻칠·금박의 경사스러운 판인가 (차분한 세피아 두루마리가 아니라)
(c) 크림색 글자와 큰 금색 숫자가 그 위에서 잘 보이겠는가
(d) 테두리 문양이 반복해도 자연스러운가, 변 중간에 큰 장식이 끼어있지 않은가
(e) 가운데가 패턴 없이 잔잔하고, 위쪽 1/3이 비어 있는가
```

## 적용하는 법 — **붙였다** (2026-09-04)

받은 그림 두 안(`panel_done.png` 짙은 주홍 + 금박 넝쿨 / `panel_done2.png` 붉은
나뭇결 + 금박 모란) 중 **첫째**를 골랐다. 둘째는 바로 아래 로딩 판(`panel_busy`)과
재질·문양이 거의 같아 **나란히 보면 「축하」와 「기다림」이 구별되지 않는다.**
바꾸려면 `tools/build_ui.py`의 `FRAMES`에서 **그 한 줄의 키만** `panel_done2`로
고치면 된다 — CSS도 화면도 그대로다.

슬라이스는 그림에서 금박 띠가 끝나는 자리를 재서 `10% 12%`로 넣었다(잰 값 8%/10%에
조금 여유). 테두리가 뭉개져 보이면 그 값과 `border-width`를 눈대중으로 조정한다.

---

# 로딩 가리개 — 판 + 애니메이션 스프라이트 (2026-09-04)

**용도**: 서버 왕복(자재 구매 · 도시 증축 · 건물 짓기) 중에 뜨는 가리개
(`BusyVeil.tsx`). 지금은 CSS로 그린 **옥색 원형 고리**가 도는데, 그것만 게임의
화풍에서 떠 있다. **그림 두 장**을 받아 갈아 끼운다.

| 파일 | 무엇 |
|---|---|
| `assets/icons/panel_busy.png` | 가리개 가운데의 **작은 판**(9분할, 그 위에 그림+글자) |
| `assets/icons/loading_spin.png` | **6프레임 가로 스프라이트** — 무한 반복되는 로딩 애니메이션 |

## ★ 프롬프트 ① — 가리개 판 (`panel_busy.png`)

```
삼국지 배경의 모바일 게임에서 쓸 작은 UI 판 그림을 그려줘.
화면 한가운데에 떠서 "잠시 기다리는 중"을 알리는 작은 판이다.

[1] 캔버스
- 가로가 살짝 긴 직사각형 1장 (예: 800×600px 안팎).
- 그림이 캔버스 가장자리까지 꽉 찬다. 여백·둥근 모서리·드롭섀도 없이
  네모 반듯한 판 하나가 캔버스를 통째로 채운다.

[2] 화풍
- 낡은 두루마리 종이 또는 옻칠 목판 — 고대 중국 후한~삼국시대.
  손으로 그린 수채/과슈 질감(매끈한 벡터·3D 렌더 금지).
- 색은 **짙은 밤색·먹색 계열에 금색 얇은 선** — 어두운 가리개 위에 뜨는
  판이라 주위보다 살짝 밝되, 그 위에 크림색 글자가 얹히므로 너무 밝으면 안 된다.
- 이미 있는 팝업 판(`panel_settings.png`)과 같은 세트로 보여야 한다.

[3] 테두리 (★ 9분할로 늘어난다)
- 네 변에 **얇고 균일한** 테두리(가는 금·청동 선, 몰딩). 위·아래는 세로의
  약 16%, 좌우는 가로의 약 12%.
- 반복해도 자연스러운 결만. 변 중간에 큰 장식 금지. 네 귀퉁이의 작은
  장식(못대가리·매듭)은 좋다.

[4] 가운데 (★ 늘려서 배경으로 쓴다)
- 글자·그림 없이 잔잔한 종이/목판 질감만. 여기에 로딩 애니메이션 그림과
  「도시를 증축하는 중..」 같은 한 줄 글자가 얹힌다.

── 자가 점검 ──
(a) 가로로 살짝 긴 사각형이 캔버스를 꽉 채웠는가  (b) 손그림 질감인가
(c) 크림색 글자가 잘 보일 만큼 바탕이 어두운가  (d) 테두리가 반복 가능한가
(e) 가운데가 비어 있는가
```

## ★ 프롬프트 ② — 로딩 애니메이션 (`loading_spin.png`) ★ 이게 핵심

```
모바일 게임의 "로딩 중" 애니메이션을, 가로로 이어 붙인 6프레임 스프라이트
시트 한 장으로 그려줘. 삼국지(고대 중국 후한~삼국시대) 배경의 게임이다.

[1] 캔버스와 배치 (★ 가장 중요)
- 정사각형 프레임 6장을 **왼쪽부터 오른쪽으로 나란히** 이어 붙인 가로로 긴
  그림 1장. 예: 한 프레임 256×256px → 전체 1536×256px.
- **각 프레임은 정확히 같은 크기의 정사각 칸**이고, 그림은 각 칸의 한가운데에
  같은 크기로 놓인다. 칸마다 크기·위치가 달라지면 재생할 때 그림이 덜덜 떨린다.
- 프레임 사이에 구분선·번호·여백 장식을 그리지 마라. 칸은 서로 딱 붙는다.
- **배경은 완전 투명(알파)**. 흰색·검정 배경을 칠하지 마라.

[2] 무엇을 그리나 (★ 가장 중요)
- 아래 중 하나를 골라 6프레임 루프로 만든다(가장 잘 나오는 것 하나만):
  ① **붓과 먹** — 붓이 원을 그리며 금빛 먹선이 조금씩 이어지다 한 바퀴를
     완성하고 사라진다.
  ② **죽간(대나무 두루마리)** — 말려 있던 죽간이 한 칸씩 펼쳐졌다가 되말린다.
  ③ **팔괘/나침반** — 청동 원반의 고리가 한 칸씩 회전한다.
- **1번 프레임과 6번 프레임이 자연스럽게 이어져야 한다** — 무한 반복되므로
  마지막 다음에 첫 프레임이 곧바로 온다.
- 움직임은 **회전이나 진행 한 방향**으로 단순하게. 프레임마다 다른 그림을
  그리지 말고, 같은 물체가 조금씩 변하는 6단계여야 한다.

[3] 화풍
- 고대 중국의 물건답게 — 먹·금박·청동·대나무. 손으로 그린 수채/과슈 질감
  (매끈한 벡터·3D 렌더·현대적 로딩 스피너 금지).
- 어두운 가리개 위에 얹히므로 **밝고 따뜻한 색**(금색·크림·옅은 청록)이
  주가 되어야 한다. 짙은 먹색만으로 그리면 어둠에 묻힌다.
- 작게(화면에서 한 변 50px 남짓) 보이므로 **형태가 단순하고 굵어야** 한다 —
  가는 선과 잔무늬는 뭉개진다.

── 자가 점검 ──
(a) 정사각 6칸이 가로로 딱 붙어 이어졌는가 (전체 6:1 비율)
(b) 배경이 완전 투명인가
(c) 여섯 칸에서 그림의 크기·중심이 같은가 (떨리지 않겠는가)
(d) 6번 다음에 1번이 와도 자연스러운가
(e) 작게 줄여도 알아볼 만큼 형태가 굵고 단순한가
```

## 적용하는 법 — **붙였다** (2026-09-04)

받은 그림은 판(571×427, 옻칠 목판 + 금박 구름매듭)과 **금빛 팔괘 원반**이 도는
6프레임이다. 슬라이스 `11% 9%`.

> 스프라이트는 **두 번째 것**으로 갈았다(2026-09-04, 2957×522 → 768×128). 첫 그림은
> 한 칸이 56px이라 아래 지뢰를 밟았고, 새 그림은 한 칸이 493px이라 128칸에 꽉 찬다.
> **프레임 수(6)는 그대로**라 CSS는 안 건드렸다.

> ★ **스프라이트를 굽는 도구가 한 군데 틀려 있었다** — `build_sprite()`가 칸을 늘
> 128px로 잡는데 `resize_alpha()`는 **키우지 않으므로**, 원본 한 칸이 56px인 이
> 그림은 128칸의 **왼쪽 위 구석**에 붙었다(재생하면 귀퉁이에서만 깜빡인다).
> 칸을 그림 크기에 맞추도록 고쳤다 — 없는 화소를 지어내지 않는 쪽이다. 레벨업
> 도장은 원본이 커서 이 갈래를 한 번도 안 지났다.

1. 두 그림을 `assets/icons/panel_busy.png` · `assets/icons/loading_spin.png`로 넣는다.
2. `npm run ui` → `public/ui/panel-busy.png` · `public/icons/loading-spin.png`
   (6프레임을 128px 정사각으로 다시 이어 붙인 768×128) 로 구워진다.
   **프레임을 각자 트리밍하지 않고 여섯 장의 합집합 상자로 한 번에** 자르므로
   재생 중 떨림이 없다(`build_sprite()` — 레벨업 도장이 이미 쓰는 길이다).
3. `style.css`의 `.busy-box`·`.busy-spin` 아래 **주석 블록 둘**의 주석을 푼다.
   프레임 수를 6이 아닌 값으로 받았다면 **세 곳이 함께** 바뀐다 —
   `build_ui.py`의 `SPRITES`, CSS의 `background-size: 600%`, `steps(6)`,
   그리고 끝 지점 `120%`(= `100 × N/(N−1)`). 하나만 고치면 중간 프레임이
   겹쳐 보인다(레벨업 도장에서 실측했다).

---

# 건물 관리 — [짓기] · [증축] 두 단추 (2026-09-04)

**용도**: 건물 관리 화면(`BuildingsScreen.tsx`)의 줄 끝 단추. 지금은 둘 다 같은
참나무 목판이라 **글자를 읽어야만** 구별된다. 뜻이 다르므로 그림도 갈라 준다.

| 파일 | 어떤 뜻 |
|---|---|
| `assets/icons/button_build.png` | **[짓기]** — 없던 건물을 새로 세운다 |
| `assets/icons/button_upgrade.png` | **[증축]** — 이미 있는 건물을 한 칸 올린다 |

두 장은 **한 세트로 보여야 하고**, 크기·테두리 두께·글자 자리가 같아야 한다 —
같은 표의 같은 열에 세로로 나란히 놓이기 때문이다.

## ★ 프롬프트

```
삼국지 배경의 모바일 게임에서 쓸 작은 버튼 배경 그림 **2장**을 그려줘.
한 세트이고, 서로 나란히 놓였을 때 "같은 물건의 두 상태"로 보여야 한다.

[1] 캔버스 (두 장 모두 동일)
- 가로로 긴 납작한 직사각형 (예: 480×200px 안팎 — 가로:세로 약 2.4:1).
- 그림이 캔버스 가장자리까지 꽉 찬다. 여백·둥근 모서리·드롭섀도 없이
  버튼 하나가 캔버스를 통째로 채운다.
- **두 장의 캔버스 크기와 테두리 두께가 정확히 같아야 한다** — 화면에서
  세로로 나란히 놓이므로 하나만 두꺼우면 줄이 어긋나 보인다.

[2] 화풍
- 고대 중국 후한~삼국시대의 목공/석공 물건. 손으로 그린 수채/과슈 질감
  (매끈한 벡터·3D 렌더 금지).
- 이미 있는 참나무 목판 버튼(`button_secondary.png`)과 **같은 세계의 물건**이어야
  한다 — 붓질과 나뭇결의 밀도를 맞춰라.
- 버튼 위에 **짙은 먹색 한글 두 글자**(「짓기」/「증축」)가 얹힌다. 그러니
  가운데는 **밝고 차분한 색**이어야 한다. 어둡거나 알록달록하면 글자가 안 보인다.
  ※ 그림에 글자를 직접 그려 넣지 마라 — 글자는 화면이 얹는다.

[3] 두 장의 차이 (★ 가장 중요)
- **①「짓기」용** : 갓 켜낸 **새 목재**의 느낌. 밝은 살구빛 나무색, 켜낸 단면과
  못·장부촉 같은 조립의 흔적, 모서리에 대패질 자국. "이제 세운다"는 인상.
- **②「증축」용** : 이미 선 건물을 **한 칸 올리는** 느낌. 같은 목판이되 조금
  더 짙고 손때가 오른 색에, 가장자리에 **금박(또는 청동) 얇은 덧테**를 둘러
  격이 한 단 높아 보이게. "이미 있는 것을 더 낫게"라는 인상.
- 두 장의 차이는 **색조와 덧테 정도까지만**이다. 모양·크기·질감이 달라지면
  한 세트로 안 보인다.

[4] 테두리 (★ 9분할로 늘어난다)
- 네 변에 균일한 테두리 결. 위·아래는 세로의 약 20%, 좌우는 가로의 약 18%.
- 변 중간에 한 번만 나오는 장식은 금지(늘어나면 일그러진다). 좌우 끝의 작은
  마감(못대가리·쇠장식)은 좋다.

[5] 가운데
- 글자·무늬 없이 잔잔한 나뭇결만. 가로로 늘어나므로 균일해야 한다.

── 자가 점검 ──
(a) 두 장의 크기·비율·테두리 두께가 같은가
(b) 가운데가 밝아 짙은 먹색 글자가 잘 보이겠는가
(c) 「짓기」는 새 목재, 「증축」은 금박 덧테로 한 단 높아 보이는가
(d) 둘이 나란히 놓였을 때 한 세트로 보이는가
(e) 그림 안에 글자를 그려 넣지 않았는가
```

## 적용하는 법 — **붙였다** (2026-09-04)

**두 번째 그림으로 갈았고, 쓰는 방법도 함께 바뀌었다** (2026-09-04). 처음엔 한 장에
둘이 나란히 그려져 와서 도구가 갈랐지만(그 `SPLITS` 갈래는 파일이 둘로 나뉘며
걷어냈다 — 안 도는 갈래를 남기지 않는다), 지금은 `button_build.png`(연장 궤짝) ·
`button_upgrade.png`(도르래와 벽돌) 두 장이다.

- **9분할을 안 쓴다.** 새 그림은 **왼쪽 그림 액자 + 오른쪽 판자**라, 9분할로 두르면
  액자가 세로로 짓눌리거나 가로로 늘어난다. 통째로 깔고(`background-size: 100% 100%`)
  **상자 비율을 그림에 맞춘다**(약 2.45:1).
- 글자는 판자 위에 앉힌다 — 액자가 폭의 **37%**(그림에서 잰 값)라 왼쪽 여백을 그만큼.
  판자가 그림의 세로 한가운데보다 조금 아래라 `padding-top`으로 그만큼 내린다
  (두 그림의 값이 서로 다르다 — 잰 값 그대로다).
- 줄이 「안 지음(`data-level="0"`)」이면 [짓기] 그림, 이미 지었으면 [증축] 그림이
  붙는다 — **화면 코드는 안 건드렸다.**
